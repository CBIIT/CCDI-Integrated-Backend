queries:
  - name: filesInList
    index:
      - files_table
    filter:
      type: pagination
      defaultSortField: file_name
      ignoreIfEmpty:
        - id
    result:
      type: object_array
# Indices settings
indices:
  # Participants with nested lists of diagnoses, files, genetic analyses, etc.
  - index_name: cohorts
    id_field: id
    mapping: # Properties are listed in the same order as they appear in ccdi-dcc-model.yml
      keyword:
        - id
        - guid # Identical to id in this index. Returned in cohort metadata JSON
        - participant_id
        - race
        - sex_at_birth
        - occupation
        - crdc_id
        - consent_group_guid
        - study_guid
        - clinical_measure_files.clinical_measure_file_id
        - clinical_measure_files.file_name
        - clinical_measure_files.data_category
        - clinical_measure_files.file_type
        - clinical_measure_files.file_description
        - clinical_measure_files.md5sum
        - clinical_measure_files.file_mapping_level
        - clinical_measure_files.file_access
        - clinical_measure_files.acl
        - clinical_measure_files.authz
        - clinical_measure_files.file_url
        - clinical_measure_files.dcf_indexd_guid
        - clinical_measure_files.checksum_algorithm
        - clinical_measure_files.checksum_value
        - clinical_measure_files.participant_list
        - clinical_measure_files.guid
        - clinical_measure_files.crdc_id
        - diagnoses.diagnosis_id
        - diagnoses.submitted_diagnosis
        - diagnoses.diagnosis
        - diagnoses.diagnosis_category
        - diagnoses.diagnosis_classification_system
        - diagnoses.diagnosis_basis
        - diagnoses.diagnosis_comment
        - diagnoses.disease_phase
        - diagnoses.tumor_spatial_extent
        - diagnoses.submitted_anatomic_site
        - diagnoses.anatomic_site
        - diagnoses.toronto_childhood_cancer_staging
        - diagnoses.tumor_stage_clinical_t
        - diagnoses.tumor_stage_clinical_n
        - diagnoses.tumor_stage_clinical_m
        - diagnoses.tumor_stage_source
        - diagnoses.tumor_grade
        - diagnoses.tumor_grade_source
        - diagnoses.year_of_diagnosis
        - diagnoses.laterality
        - diagnoses.guid
        - exposures.exposure_id
        - exposures.alcohol_history
        - exposures.alcohol_intensity
        - exposures.asbestos_exposure
        - exposures.coal_dust_exposure
        - exposures.environmental_tobacco_smoke_exposure
        - exposures.radon_exposure
        - exposures.respirable_crystalline_silica_exposure
        - exposures.smoking_frequency
        - exposures.time_between_waking_and_first_smoke
        - exposures.tobacco_smoking_status
        - exposures.type_of_smoke_exposure
        - exposures.type_of_tobacco_used
        - exposures.guid
        - family_relationships.family_relationship_id
        - family_relationships.relationship
        - family_relationships.family_id
        - family_relationships.guid
        - laboratory_tests.laboratory_test_id
        - laboratory_tests.laboratory_test_method
        - laboratory_tests.laboratory_test_name
        - laboratory_tests.specimen
        - laboratory_tests.test_result_text
        - laboratory_tests.test_result_modifier
        - laboratory_tests.test_result_numeric
        - laboratory_tests.test_result_unit
        - laboratory_tests.guid
        - medical_histories.medical_history_id
        - medical_histories.medical_history_category
        - medical_histories.medical_history_condition
        - medical_histories.guid
        - radiology_files.radiology_file_id
        - radiology_files.file_name
        - radiology_files.data_category
        - radiology_files.file_type
        - radiology_files.file_description
        - radiology_files.md5sum
        - radiology_files.file_mapping_level
        - radiology_files.file_access
        - radiology_files.acl
        - radiology_files.authz
        - radiology_files.file_url
        - radiology_files.dcf_indexd_guid
        - radiology_files.anatomic_site
        - radiology_files.image_modality
        - radiology_files.deidentification_method
        - radiology_files.magnetic_field_strength
        - radiology_files.scanner_manufacturer
        - radiology_files.scanner_model
        - radiology_files.imaging_frequency
        - radiology_files.repetition_time
        - radiology_files.echo_time
        - radiology_files.inversion_time
        - radiology_files.flip_angle
        - radiology_files.pixel_spacing
        - radiology_files.slice_thickness
        - radiology_files.echoPulseSequenceCategoryCode
        - radiology_files.diffusionBValue
        - radiology_files.diffusionDirectionalityCode
        - radiology_files.resonantNucleusCode
        - radiology_files.acquisitionContrastCode
        - radiology_files.inversionRecoveryIndicator
        - radiology_files.pulseSequenceName
        - radiology_files.multipleSpinEchoIndicator
        - radiology_files.phaseContrastIndicator
        - radiology_files.timeOfFlightContrastIndicator
        - radiology_files.arterialSpinLabelingContrastCode
        - radiology_files.steadyStatePulseSequenceCode
        - radiology_files.echoPlanarPulseSequenceIndicator
        - radiology_files.saturationRecoveryIndicator
        - radiology_files.spectrallySelectedSuppressionCode
        - radiology_files.mrImageReconstructionProtocolElement
        - radiology_files.complexImageComponentCode
        - radiology_files.guid
        - radiology_files.crdc_id
        - survivals.survival_id
        - survivals.last_known_survival_status
        - survivals.first_event
        - survivals.event_free_survival_status
        - survivals.follow_up_category
        - survivals.follow_up_other
        - survivals.adverse_event
        - survivals.comorbidity
        - survivals.comorbidity_method_of_diagnosis
        - survivals.risk_factor
        - survivals.cause_of_death
        - survivals.guid
        - synonyms.synonym_id
        - synonyms.repository_of_synonym_id
        - synonyms.associated_id
        - synonyms.domain_description
        - synonyms.domain_category
        - synonyms.data_location
        - synonyms.guid
        - treatments_chemotherapy.treatment_chemotherapy_id
        - treatments_chemotherapy.treatment_type
        - treatments_chemotherapy.chemotherapy_type
        - treatments_chemotherapy.chemotherapy_agent
        - treatments_chemotherapy.chemotherapy_class
        - treatments_chemotherapy.chemotherapy_subclass
        - treatments_chemotherapy.dose
        - treatments_chemotherapy.dose_unit
        - treatments_chemotherapy.dose_route
        - treatments_chemotherapy.dose_frequency
        - treatments_chemotherapy.protocol
        - treatments_chemotherapy.guid
        - treatments_other.treatment_other_id
        - treatments_other.treatment_type
        - treatments_other.other_treatment_type
        - treatments_other.guid
        - treatments_radiation.treatment_radiation_id
        - treatments_radiation.treatment_type
        - treatments_radiation.radiation_therapy_type
        - treatments_radiation.beam_radiation_type
        - treatments_radiation.total_radiation_dose
        - treatments_radiation.total_radiation_dose_unit
        - treatments_radiation.anatomic_site
        - treatments_radiation.laterality_of_radiation_field
        - treatments_radiation.radiation_therapy_fraction
        - treatments_radiation.fraction_radiation_dose
        - treatments_radiation.fraction_radiation_dose_unit
        - treatments_radiation.guid
        - treatment_responses.treatment_response_id
        - treatment_responses.response
        - treatment_responses.response_category
        - treatment_responses.response_system
        - treatment_responses.guid
        - treatments_surgery.treatment_surgery_id
        - treatments_surgery.treatment_type
        - treatments_surgery.surgery_type
        - treatments_surgery.anatomic_site
        - treatments_surgery.resection_margin_status
        - treatments_surgery.guid
        - cytogenomic_files.cytogenomic_file_id
        - cytogenomic_files.file_name
        - cytogenomic_files.data_category
        - cytogenomic_files.file_type
        - cytogenomic_files.file_description
        - cytogenomic_files.md5sum
        - cytogenomic_files.file_mapping_level
        - cytogenomic_files.file_access
        - cytogenomic_files.acl
        - cytogenomic_files.authz
        - cytogenomic_files.file_url
        - cytogenomic_files.dcf_indexd_guid
        - cytogenomic_files.cytogenomic_platform
        - cytogenomic_files.guid
        - cytogenomic_files.crdc_id
        - methylation_array_files.methylation_array_file_id
        - methylation_array_files.file_name
        - methylation_array_files.data_category
        - methylation_array_files.file_type
        - methylation_array_files.file_description
        - methylation_array_files.md5sum
        - methylation_array_files.file_mapping_level
        - methylation_array_files.file_access
        - methylation_array_files.acl
        - methylation_array_files.authz
        - methylation_array_files.file_url
        - methylation_array_files.dcf_indexd_guid
        - methylation_array_files.methylation_platform
        - methylation_array_files.reporter_label
        - methylation_array_files.guid
        - methylation_array_files.crdc_id
        - pathology_files.pathology_file_id
        - pathology_files.file_name
        - pathology_files.data_category
        - pathology_files.file_type
        - pathology_files.file_description
        - pathology_files.md5sum
        - pathology_files.file_mapping_level
        - pathology_files.file_access
        - pathology_files.acl
        - pathology_files.authz
        - pathology_files.file_url
        - pathology_files.dcf_indexd_guid
        - pathology_files.image_modality
        - pathology_files.license
        - pathology_files.magnification
        - pathology_files.fixation_embedding_method
        - pathology_files.staining_method
        - pathology_files.deidentification_method
        - pathology_files.slim_url
        - pathology_files.guid
        - pathology_files.crdc_id
        - generic_files.generic_file_id
        - generic_files.file_name
        - generic_files.data_category
        - generic_files.file_type
        - generic_files.file_description
        - generic_files.md5sum
        - generic_files.file_mapping_level
        - generic_files.file_access
        - generic_files.acl
        - generic_files.authz
        - generic_files.file_url
        - generic_files.dcf_indexd_guid
        - generic_files.guid
        - generic_files.crdc_id
        - sequencing_files.sequencing_file_id
        - sequencing_files.file_name
        - sequencing_files.data_category
        - sequencing_files.file_type
        - sequencing_files.file_description
        - sequencing_files.md5sum
        - sequencing_files.file_mapping_level
        - sequencing_files.file_access
        - sequencing_files.acl
        - sequencing_files.authz
        - sequencing_files.file_url
        - sequencing_files.dcf_indexd_guid
        - sequencing_files.library_id
        - sequencing_files.library_selection
        - sequencing_files.library_strategy
        - sequencing_files.library_layout
        - sequencing_files.library_source_material
        - sequencing_files.library_source_molecule
        - sequencing_files.design_description
        - sequencing_files.platform
        - sequencing_files.instrument_model
        - sequencing_files.reference_genome_assembly
        - sequencing_files.checksum_algorithm
        - sequencing_files.checksum_value
        - sequencing_files.custom_assembly_fasta_file_for_alignment
        - sequencing_files.sequence_alignment_software
        - sequencing_files.cDNA_read
        - sequencing_files.cell_barcode_read
        - sequencing_files.single_cell_entity
        - sequencing_files.dissociation_method
        - sequencing_files.end_bias
        - sequencing_files.library_construction_method
        - sequencing_files.library_strand
        - sequencing_files.protocols_link
        - sequencing_files.reverse_transcription_primer
        - sequencing_files.spike_in
        - sequencing_files.UMI_barcode_read
        - sequencing_files.guid
        - sequencing_files.crdc_id
        - genetic_analyses.genetic_analysis_id
        - genetic_analyses.alteration_category
        - genetic_analyses.genomic_source_category
        - genetic_analyses.reported_significance
        - genetic_analyses.reported_significance_system
        - genetic_analyses.test
        - genetic_analyses.method
        - genetic_analyses.result
        - genetic_analyses.platform
        - genetic_analyses.analysis_type
        - genetic_analyses.analysis_method
        - genetic_analyses.reference_genome
        - genetic_analyses.chromosome
        - genetic_analyses.cytoband
        - genetic_analyses.exon
        - genetic_analyses.gene_symbol
        - genetic_analyses.fusion_partner_gene
        - genetic_analyses.fusion_partner_transcript
        - genetic_analyses.fusion_partner_exon
        - genetic_analyses.translocation_partner_chromosome
        - genetic_analyses.translocation_partner_cytoband
        - genetic_analyses.alteration_region
        - genetic_analyses.alteration_location_role
        - genetic_analyses.transcript
        - genetic_analyses.hgvs_coding
        - genetic_analyses.hgvs_protein
        - genetic_analyses.hgvs_genome
        - genetic_analyses.alteration
        - genetic_analyses.alteration_type
        - genetic_analyses.alteration_effect
        - genetic_analyses.iscn
        - genetic_analyses.status
        - genetic_analyses.zygosity
        - genetic_analyses.external_references
        - genetic_analyses.alteration_description
        - genetic_analyses.guid
        - samples.sample_id
        - samples.submitted_anatomic_site
        - samples.anatomic_site
        - samples.sample_tumor_status
        - samples.tumor_spatial_extent
        - samples.sample_description
        - samples.laterality
        - samples.guid
        - samples.crdc_id
      integer:
        - clinical_measure_files.file_size
        - exposures.cigarettes_per_day
        - exposures.tobacco_smoking_onset_year
        - exposures.tobacco_smoking_quit_year
        - radiology_files.file_size
        - cytogenomic_files.file_size
        - methylation_array_files.file_size
        - pathology_files.file_size
        - generic_files.file_size
        - sequencing_files.file_size
        - sequencing_files.number_of_bp
        - sequencing_files.number_of_reads
        - sequencing_files.avg_read_length
        - sequencing_files.cDNA_read_size
        - sequencing_files.cDNA_read_offset
        - sequencing_files.cell_barcode_offset
        - sequencing_files.cell_barcode_size
        - sequencing_files.total_number_of_input_cells
        - sequencing_files.UMI_barcode_offset
        - sequencing_files.UMI_barcode_size
        - genetic_analyses.age_at_genetic_analysis
        - genetic_analyses.alteration_region_number
        - samples.participant_age_at_collection
      float:
        - diagnoses.age_at_diagnosis
        - exposures.age_at_exposure
        - exposures.pack_years_smoked
        - exposures.years_smoked
        - exposures.alcohol_days_per_week
        - exposures.alcohol_drinks_per_day
        - laboratory_tests.age_at_laboratory_test
        - laboratory_tests.sensitivity
        - radiology_files.participant_age_at_imaging
        - survivals.age_at_last_known_survival_status
        - survivals.age_at_event_free_survival_status
        - treatments_chemotherapy.age_at_treatment_start
        - treatments_chemotherapy.age_at_treatment_end
        - treatments_other.age_at_treatment_start
        - treatments_other.age_at_treatment_end
        - treatments_radiation.age_at_treatment_start
        - treatments_radiation.age_at_treatment_end
        - treatment_responses.age_at_response
        - treatments_surgery.age_at_treatment_start
        - treatments_surgery.age_at_treatment_end
        - sequencing_files.coverage
        - genetic_analyses.allelic_ratio
        - genetic_analyses.vaf_numeric
        - genetic_analyses.dna_index_numeric
        - samples.percent_tumor
        - samples.percent_necrosis
    initial_query:
      query: |
        MATCH
          (p:participant)-[:of_participant]->(cg:consent_group),
          (cg)-[:of_consent_group]->(stu:study)
        WITH p, cg, stu
        SKIP $skip LIMIT $limit
        RETURN
          p.guid AS id,
          p.guid AS guid,
          cg.guid AS consent_group_guid,
          stu.guid AS study_guid
      page_size: 5000
    update_queries:
      - name: "participant_properties"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          RETURN
            p.participant_id AS participant_id,
            p.race AS race,
            p.sex_at_birth AS sex_at_birth,
            p.occupation AS occupation,
            p.guid AS id,
            p.crdc_id AS crdc_id
        page_size: 5000
      - name: "clinical_measure_files"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:clinical_measure_file)-[:of_clinical_measure_file]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              clinical_measure_file_id: item.clinical_measure_file_id,
              file_name: item.file_name,
              data_category: item.data_category,
              file_type: item.file_type,
              file_description: item.file_description,
              file_size: item.file_size,
              md5sum: item.md5sum,
              file_mapping_level: item.file_mapping_level,
              file_access: item.file_access,
              acl: item.acl,
              authz: item.authz,
              file_url: item.file_url,
              dcf_indexd_guid: item.dcf_indexd_guid,
              checksum_algorithm: item.checksum_algorithm,
              checksum_value: item.checksum_value,
              participant_list: item.participant_list,
              guid: item.guid,
              crdc_id: item.crdc_id
            }] AS clinical_measure_files
          RETURN p.guid AS id, clinical_measure_files
        page_size: 5000
      - name: "diagnoses"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:diagnosis)-[:of_diagnosis]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              diagnosis_id: item.diagnosis_id,
              submitted_diagnosis: item.submitted_diagnosis,
              diagnosis: item.diagnosis,
              diagnosis_category: item.diagnosis_category,
              diagnosis_classification_system: item.diagnosis_classification_system,
              diagnosis_basis: item.diagnosis_basis,
              diagnosis_comment: item.diagnosis_comment,
              disease_phase: item.disease_phase,
              tumor_spatial_extent: item.tumor_spatial_extent,
              submitted_anatomic_site: item.submitted_anatomic_site,
              anatomic_site: item.anatomic_site,
              age_at_diagnosis: item.age_at_diagnosis,
              toronto_childhood_cancer_staging: item.toronto_childhood_cancer_staging,
              tumor_stage_clinical_t: item.tumor_stage_clinical_t,
              tumor_stage_clinical_n: item.tumor_stage_clinical_n,
              tumor_stage_clinical_m: item.tumor_stage_clinical_m,
              tumor_stage_source: item.tumor_stage_source,
              tumor_grade: item.tumor_grade,
              tumor_grade_source: item.tumor_grade_source,
              year_of_diagnosis: item.year_of_diagnosis,
              laterality: item.laterality,
              guid: item.guid
            }] AS diagnoses
          RETURN p.guid AS id, diagnoses
        page_size: 5000
      - name: "exposures"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:exposure)-[:of_exposure]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              exposure_id: item.exposure_id,
              age_at_exposure: item.age_at_exposure,
              pack_years_smoked: item.pack_years_smoked,
              years_smoked: item.years_smoked,
              alcohol_days_per_week: item.alcohol_days_per_week,
              alcohol_drinks_per_day: item.alcohol_drinks_per_day,
              alcohol_history: item.alcohol_history,
              alcohol_intensity: item.alcohol_intensity,
              asbestos_exposure: item.asbestos_exposure,
              cigarettes_per_day: item.cigarettes_per_day,
              coal_dust_exposure: item.coal_dust_exposure,
              environmental_tobacco_smoke_exposure: item.environmental_tobacco_smoke_exposure,
              radon_exposure: item.radon_exposure,
              respirable_crystalline_silica_exposure: item.respirable_crystalline_silica_exposure,
              smoking_frequency: item.smoking_frequency,
              time_between_waking_and_first_smoke: item.time_between_waking_and_first_smoke,
              tobacco_smoking_onset_year: item.tobacco_smoking_onset_year,
              tobacco_smoking_quit_year: item.tobacco_smoking_quit_year,
              tobacco_smoking_status: item.tobacco_smoking_status,
              type_of_smoke_exposure: item.type_of_smoke_exposure,
              type_of_tobacco_used: item.type_of_tobacco_used,
              guid: item.guid
            }] AS exposures
          RETURN p.guid AS id, exposures
        page_size: 5000
      - name: "family_relationships"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:family_relationship)-[:of_family_relationship]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              family_relationship_id: item.family_relationship_id,
              relationship: item.relationship,
              family_id: item.family_id,
              guid: item.guid
            }] AS family_relationships
          RETURN p.guid AS id, family_relationships
        page_size: 5000
      - name: "laboratory_tests"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:laboratory_test)-[:of_laboratory_test]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              laboratory_test_id: item.laboratory_test_id,
              age_at_laboratory_test: item.age_at_laboratory_test,
              laboratory_test_method: item.laboratory_test_method,
              laboratory_test_name: item.laboratory_test_name,
              specimen: item.specimen,
              test_result_text: item.test_result_text,
              test_result_modifier: item.test_result_modifier,
              test_result_numeric: item.test_result_numeric,
              test_result_unit: item.test_result_unit,
              sensitivity: item.sensitivity,
              guid: item.guid
            }] AS laboratory_tests
          RETURN p.guid AS id, laboratory_tests
        page_size: 5000
      - name: "medical_histories"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:medical_history)-[:of_medical_history]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              medical_history_id: item.medical_history_id,
              medical_history_category: item.medical_history_category,
              medical_history_condition: item.medical_history_condition,
              guid: item.guid
            }] AS medical_histories
          RETURN p.guid AS id, medical_histories
        page_size: 5000
      - name: "radiology_files"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:radiology_file)-[:of_radiology_file]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              radiology_file_id: item.radiology_file_id,
              file_name: item.file_name,
              data_category: item.data_category,
              file_type: item.file_type,
              file_description: item.file_description,
              file_size: item.file_size,
              md5sum: item.md5sum,
              file_mapping_level: item.file_mapping_level,
              file_access: item.file_access,
              acl: item.acl,
              authz: item.authz,
              file_url: item.file_url,
              dcf_indexd_guid: item.dcf_indexd_guid,
              anatomic_site: item.anatomic_site,
              participant_age_at_imaging: item.participant_age_at_imaging,
              image_modality: item.image_modality,
              deidentification_method: item.deidentification_method,
              magnetic_field_strength: item.magnetic_field_strength,
              scanner_manufacturer: item.scanner_manufacturer,
              scanner_model: item.scanner_model,
              imaging_frequency: item.imaging_frequency,
              repetition_time: item.repetition_time,
              echo_time: item.echo_time,
              inversion_time: item.inversion_time,
              flip_angle: item.flip_angle,
              pixel_spacing: item.pixel_spacing,
              slice_thickness: item.slice_thickness,
              echoPulseSequenceCategoryCode: item.echoPulseSequenceCategoryCode,
              diffusionBValue: item.diffusionBValue,
              diffusionDirectionalityCode: item.diffusionDirectionalityCode,
              resonantNucleusCode: item.resonantNucleusCode,
              acquisitionContrastCode: item.acquisitionContrastCode,
              inversionRecoveryIndicator: item.inversionRecoveryIndicator,
              pulseSequenceName: item.pulseSequenceName,
              multipleSpinEchoIndicator: item.multipleSpinEchoIndicator,
              phaseContrastIndicator: item.phaseContrastIndicator,
              timeOfFlightContrastIndicator: item.timeOfFlightContrastIndicator,
              arterialSpinLabelingContrastCode: item.arterialSpinLabelingContrastCode,
              steadyStatePulseSequenceCode: item.steadyStatePulseSequenceCode,
              echoPlanarPulseSequenceIndicator: item.echoPlanarPulseSequenceIndicator,
              saturationRecoveryIndicator: item.saturationRecoveryIndicator,
              spectrallySelectedSuppressionCode: item.spectrallySelectedSuppressionCode,
              mrImageReconstructionProtocolElement: item.mrImageReconstructionProtocolElement,
              complexImageComponentCode: item.complexImageComponentCode,
              guid: item.guid,
              crdc_id: item.crdc_id
            }] AS radiology_files
          RETURN p.guid AS id, radiology_files
        page_size: 5000
      - name: "survivals"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:survival)-[:of_survival]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              survival_id: item.survival_id,
              last_known_survival_status: item.last_known_survival_status,
              age_at_last_known_survival_status: item.age_at_last_known_survival_status,
              first_event: item.first_event,
              event_free_survival_status: item.event_free_survival_status,
              age_at_event_free_survival_status: item.age_at_event_free_survival_status,
              follow_up_category: item.follow_up_category,
              follow_up_other: item.follow_up_other,
              adverse_event: item.adverse_event,
              comorbidity: item.comorbidity,
              comorbidity_method_of_diagnosis: item.comorbidity_method_of_diagnosis,
              risk_factor: item.risk_factor,
              cause_of_death: item.cause_of_death,
              guid: item.guid
            }] AS survivals
          RETURN p.guid AS id, survivals
        page_size: 5000
      - name: "synonyms"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:synonym)-[:of_synonym]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              synonym_id: item.synonym_id,
              repository_of_synonym_id: item.repository_of_synonym_id,
              associated_id: item.associated_id,
              domain_description: item.domain_description,
              domain_category: item.domain_category,
              data_location: item.data_location,
              guid: item.guid
            }] AS synonyms
          RETURN p.guid AS id, synonyms
        page_size: 5000
      - name: "treatments_chemotherapy"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              treatment_chemotherapy_id: item.treatment_chemotherapy_id,
              treatment_type: item.treatment_type,
              chemotherapy_type: item.chemotherapy_type,
              age_at_treatment_start: item.age_at_treatment_start,
              age_at_treatment_end: item.age_at_treatment_end,
              chemotherapy_agent: item.chemotherapy_agent,
              chemotherapy_class: item.chemotherapy_class,
              chemotherapy_subclass: item.chemotherapy_subclass,
              dose: item.dose,
              dose_unit: item.dose_unit,
              dose_route: item.dose_route,
              dose_frequency: item.dose_frequency,
              protocol: item.protocol,
              guid: item.guid
            }] AS treatments_chemotherapy
          RETURN p.guid AS id, treatments_chemotherapy
        page_size: 5000
      - name: "treatments_other"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:treatment_other)-[:of_treatment_other]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              treatment_other_id: item.treatment_other_id,
              treatment_type: item.treatment_type,
              other_treatment_type: item.other_treatment_type,
              age_at_treatment_start: item.age_at_treatment_start,
              age_at_treatment_end: item.age_at_treatment_end,
              guid: item.guid
            }] AS treatments_other
          RETURN p.guid AS id, treatments_other
        page_size: 5000
      - name: "treatments_radiation"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:treatment_radiation)-[:of_treatment_radiation]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              treatment_radiation_id: item.treatment_radiation_id,
              treatment_type: item.treatment_type,
              radiation_therapy_type: item.radiation_therapy_type,
              age_at_treatment_start: item.age_at_treatment_start,
              age_at_treatment_end: item.age_at_treatment_end,
              beam_radiation_type: item.beam_radiation_type,
              total_radiation_dose: item.total_radiation_dose,
              total_radiation_dose_unit: item.total_radiation_dose_unit,
              anatomic_site: item.anatomic_site,
              laterality_of_radiation_field: item.laterality_of_radiation_field,
              radiation_therapy_fraction: item.radiation_therapy_fraction,
              fraction_radiation_dose: item.fraction_radiation_dose,
              fraction_radiation_dose_unit: item.fraction_radiation_dose_unit,
              guid: item.guid
            }] AS treatments_radiation
          RETURN p.guid AS id, treatments_radiation
        page_size: 5000
      - name: "treatment_responses"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:treatment_response)-[:of_treatment_response]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              treatment_response_id: item.treatment_response_id,
              response: item.response,
              age_at_response: item.age_at_response,
              response_category: item.response_category,
              response_system: item.response_system,
              guid: item.guid
            }] AS treatment_responses
          RETURN p.guid AS id, treatment_responses
        page_size: 5000
      - name: "treatments_surgery"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (child:treatment_surgery)-[:of_treatment_surgery]->(p)
          WITH
            p,
            [item IN collect(DISTINCT child) WHERE item IS NOT NULL | {
              treatment_surgery_id: item.treatment_surgery_id,
              treatment_type: item.treatment_type,
              surgery_type: item.surgery_type,
              age_at_treatment_start: item.age_at_treatment_start,
              age_at_treatment_end: item.age_at_treatment_end,
              anatomic_site: item.anatomic_site,
              resection_margin_status: item.resection_margin_status,
              guid: item.guid
            }] AS treatments_surgery
          RETURN p.guid AS id, treatments_surgery
        page_size: 5000

  # Study-level cohort metadata excluding the participant path through consent_group.
  - index_name: studies_for_cohorts
    id_field: "guid"
    mapping: # Properties follow their node order in ccdi-dcc-model.yml tag 1.0.0.
      keyword:
        - guid
        - study_id
        - dbgap_accession
        - study_name
        - study_acronym
        - study_description
        - external_url
        - experimental_strategy_and_data_subtype
        - study_phase
        - study_data_types
        - promotion_status
        - crdc_id
        - clinical_measure_files.clinical_measure_file_id
        - clinical_measure_files.file_name
        - clinical_measure_files.data_category
        - clinical_measure_files.file_type
        - clinical_measure_files.file_description
        - clinical_measure_files.md5sum
        - clinical_measure_files.file_mapping_level
        - clinical_measure_files.file_access
        - clinical_measure_files.acl
        - clinical_measure_files.authz
        - clinical_measure_files.file_url
        - clinical_measure_files.dcf_indexd_guid
        - clinical_measure_files.checksum_algorithm
        - clinical_measure_files.checksum_value
        - clinical_measure_files.participant_list
        - clinical_measure_files.guid
        - clinical_measure_files.crdc_id
        - generic_files.generic_file_id
        - generic_files.file_name
        - generic_files.data_category
        - generic_files.file_type
        - generic_files.file_description
        - generic_files.md5sum
        - generic_files.file_mapping_level
        - generic_files.file_access
        - generic_files.acl
        - generic_files.authz
        - generic_files.file_url
        - generic_files.dcf_indexd_guid
        - generic_files.guid
        - generic_files.crdc_id
        - publications.publication_id
        - publications.pubmed_id
        - publications.guid
        - study_admins.study_admin_id
        - study_admins.organism_species
        - study_admins.adult_or_childhood_study
        - study_admins.file_types_and_format
        - study_admins.guid
        - study_arms.study_arm_id
        - study_arms.study_arm_description
        - study_arms.clinical_trial_identifier
        - study_arms.clinical_trial_repository
        - study_arms.guid
        - study_fundings.study_funding_id
        - study_fundings.funding_agency
        - study_fundings.grant_id
        - study_fundings.funding_source_program_name
        - study_fundings.guid
        - study_personnels.study_personnel_id
        - study_personnels.personnel_name
        - study_personnels.personnel_type
        - study_personnels.email_address
        - study_personnels.institution
        - study_personnels.orcid
        - study_personnels.guid
        - study_personnels.crdc_id
        - study_statuses.study_status_id
        - study_statuses.repository_name
        - study_statuses.version
        - study_statuses.study_repository_start
        - study_statuses.study_repository_released
        - study_statuses.external_url
        - study_statuses.release_status
        - study_statuses.guid
        - consent_groups.consent_group_id
        - consent_groups.consent_group_name
        - consent_groups.consent_group_suffix
        - consent_groups.guid
        - cell_lines.cell_line_id
        - cell_lines.source
        - cell_lines.guid
        - cell_lines.samples.sample_id
        - cell_lines.samples.anatomic_site
        - cell_lines.samples.sample_tumor_status
        - cell_lines.samples.tumor_spatial_extent
        - cell_lines.samples.sample_description
        - cell_lines.samples.laterality
        - cell_lines.samples.guid
        - cell_lines.samples.crdc_id
        - cell_lines.samples.sequencing_files.sequencing_file_id
        - cell_lines.samples.sequencing_files.file_name
        - cell_lines.samples.sequencing_files.data_category
        - cell_lines.samples.sequencing_files.file_type
        - cell_lines.samples.sequencing_files.file_description
        - cell_lines.samples.sequencing_files.md5sum
        - cell_lines.samples.sequencing_files.file_mapping_level
        - cell_lines.samples.sequencing_files.file_access
        - cell_lines.samples.sequencing_files.acl
        - cell_lines.samples.sequencing_files.authz
        - cell_lines.samples.sequencing_files.file_url
        - cell_lines.samples.sequencing_files.dcf_indexd_guid
        - cell_lines.samples.sequencing_files.library_id
        - cell_lines.samples.sequencing_files.library_selection
        - cell_lines.samples.sequencing_files.library_strategy
        - cell_lines.samples.sequencing_files.library_layout
        - cell_lines.samples.sequencing_files.library_source_material
        - cell_lines.samples.sequencing_files.library_source_molecule
        - cell_lines.samples.sequencing_files.design_description
        - cell_lines.samples.sequencing_files.platform
        - cell_lines.samples.sequencing_files.instrument_model
        - cell_lines.samples.sequencing_files.reference_genome_assembly
        - cell_lines.samples.sequencing_files.checksum_algorithm
        - cell_lines.samples.sequencing_files.checksum_value
        - cell_lines.samples.sequencing_files.custom_assembly_fasta_file_for_alignment
        - cell_lines.samples.sequencing_files.sequence_alignment_software
        - cell_lines.samples.sequencing_files.cDNA_read
        - cell_lines.samples.sequencing_files.cDNA_read_size
        - cell_lines.samples.sequencing_files.cDNA_read_offset
        - cell_lines.samples.sequencing_files.cell_barcode_offset
        - cell_lines.samples.sequencing_files.cell_barcode_read
        - cell_lines.samples.sequencing_files.cell_barcode_size
        - cell_lines.samples.sequencing_files.single_cell_entity
        - cell_lines.samples.sequencing_files.dissociation_method
        - cell_lines.samples.sequencing_files.end_bias
        - cell_lines.samples.sequencing_files.total_number_of_input_cells
        - cell_lines.samples.sequencing_files.library_construction_method
        - cell_lines.samples.sequencing_files.library_strand
        - cell_lines.samples.sequencing_files.protocols_link
        - cell_lines.samples.sequencing_files.reverse_transcription_primer
        - cell_lines.samples.sequencing_files.spike_in
        - cell_lines.samples.sequencing_files.UMI_barcode_offset
        - cell_lines.samples.sequencing_files.UMI_barcode_read
        - cell_lines.samples.sequencing_files.UMI_barcode_size
        - cell_lines.samples.sequencing_files.guid
        - cell_lines.samples.sequencing_files.crdc_id
      integer:
        - study_period_start
        - study_period_stop
        - clinical_measure_files.file_size
        - generic_files.file_size
        - study_statuses.number_of_participants
        - study_statuses.number_of_samples
        - study_statuses.number_of_files
        - study_statuses.total_size_of_data_files
        - cell_lines.samples.sequencing_files.file_size
        - cell_lines.samples.sequencing_files.number_of_bp
        - cell_lines.samples.sequencing_files.number_of_reads
      float:
        - cell_lines.cell_line_passage_number
        - cell_lines.samples.participant_age_at_collection
        - cell_lines.samples.percent_tumor
        - cell_lines.samples.percent_necrosis
        - cell_lines.samples.sequencing_files.avg_read_length
        - cell_lines.samples.sequencing_files.coverage
    initial_query:
      query: |
        MATCH (stu:study)
        WITH stu
        SKIP $skip LIMIT $limit
        RETURN stu.guid AS guid
      page_size: 100
    update_queries:
      - name: "study_properties"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit
          RETURN
            stu.study_id AS study_id,
            stu.dbgap_accession AS dbgap_accession,
            stu.study_name AS study_name,
            stu.study_acronym AS study_acronym,
            stu.study_description AS study_description,
            stu.external_url AS external_url,
            stu.experimental_strategy_and_data_subtype AS experimental_strategy_and_data_subtype,
            stu.study_phase AS study_phase,
            stu.study_period_start AS study_period_start,
            stu.study_period_stop AS study_period_stop,
            stu.study_data_types AS study_data_types,
            stu.promotion_status AS promotion_status,
            stu.guid AS guid,
            stu.crdc_id AS crdc_id
        page_size: 100
      - name: "clinical_measure_files"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (file:clinical_measure_file)-[:of_clinical_measure_file]->(stu)
          WITH
            stu,
            [item IN collect(DISTINCT file) WHERE item IS NOT NULL | {
              clinical_measure_file_id: item.clinical_measure_file_id,
              file_name: item.file_name,
              data_category: item.data_category,
              file_type: item.file_type,
              file_description: item.file_description,
              file_size: item.file_size,
              md5sum: item.md5sum,
              file_mapping_level: item.file_mapping_level,
              file_access: item.file_access,
              acl: item.acl,
              authz: item.authz,
              file_url: item.file_url,
              dcf_indexd_guid: item.dcf_indexd_guid,
              checksum_algorithm: item.checksum_algorithm,
              checksum_value: item.checksum_value,
              participant_list: item.participant_list,
              guid: item.guid,
              crdc_id: item.crdc_id
            }] AS clinical_measure_files
          RETURN stu.guid AS guid, clinical_measure_files
        page_size: 100
      - name: "generic_files"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (file:generic_file)-[:of_generic_file]->(stu)
          WITH
            stu,
            [item IN collect(DISTINCT file) WHERE item IS NOT NULL | {
              generic_file_id: item.generic_file_id,
              file_name: item.file_name,
              data_category: item.data_category,
              file_type: item.file_type,
              file_description: item.file_description,
              file_size: item.file_size,
              md5sum: item.md5sum,
              file_mapping_level: item.file_mapping_level,
              file_access: item.file_access,
              acl: item.acl,
              authz: item.authz,
              file_url: item.file_url,
              dcf_indexd_guid: item.dcf_indexd_guid,
              guid: item.guid,
              crdc_id: item.crdc_id
            }] AS generic_files
          RETURN stu.guid AS guid, generic_files
        page_size: 100
      - name: "publications"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (publication:publication)-[:of_publication]->(stu)
          WITH
            stu,
            [item IN collect(DISTINCT publication) WHERE item IS NOT NULL | {
              publication_id: item.publication_id,
              pubmed_id: item.pubmed_id,
              guid: item.guid
            }] AS publications
          RETURN stu.guid AS guid, publications
        page_size: 100
      - name: "study_admins"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (study_admin:study_admin)-[:of_study_admin]->(stu)
          WITH
            stu,
            [item IN collect(DISTINCT study_admin) WHERE item IS NOT NULL | {
              study_admin_id: item.study_admin_id,
              organism_species: item.organism_species,
              adult_or_childhood_study: item.adult_or_childhood_study,
              file_types_and_format: item.file_types_and_format,
              guid: item.guid
            }] AS study_admins
          RETURN stu.guid AS guid, study_admins
        page_size: 100
      - name: "study_arms"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (study_arm:study_arm)-[:of_study_arm]->(stu)
          WITH
            stu,
            [item IN collect(DISTINCT study_arm) WHERE item IS NOT NULL | {
              study_arm_id: item.study_arm_id,
              study_arm_description: item.study_arm_description,
              clinical_trial_identifier: item.clinical_trial_identifier,
              clinical_trial_repository: item.clinical_trial_repository,
              guid: item.guid
            }] AS study_arms
          RETURN stu.guid AS guid, study_arms
        page_size: 100
      - name: "study_fundings"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (study_funding:study_funding)-[:of_study_funding]->(stu)
          WITH
            stu,
            [item IN collect(DISTINCT study_funding) WHERE item IS NOT NULL | {
              study_funding_id: item.study_funding_id,
              funding_agency: item.funding_agency,
              grant_id: item.grant_id,
              funding_source_program_name: item.funding_source_program_name,
              guid: item.guid
            }] AS study_fundings
          RETURN stu.guid AS guid, study_fundings
        page_size: 100
      - name: "study_personnel"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (study_personnel:study_personnel)-[:of_study_personnel]->(stu)
          WITH
            stu,
            [item IN collect(DISTINCT study_personnel) WHERE item IS NOT NULL | {
              study_personnel_id: item.study_personnel_id,
              personnel_name: item.personnel_name,
              personnel_type: item.personnel_type,
              email_address: item.email_address,
              institution: item.institution,
              orcid: item.orcid,
              guid: item.guid,
              crdc_id: item.crdc_id
            }] AS study_personnels
          RETURN stu.guid AS guid, study_personnels
        page_size: 100
      - name: "study_statuses"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (study_status:study_status)-[:of_study_status]->(stu)
          WITH
            stu,
            [item IN collect(DISTINCT study_status) WHERE item IS NOT NULL | {
              study_status_id: item.study_status_id,
              repository_name: item.repository_name,
              version: item.version,
              study_repository_start: item.study_repository_start,
              study_repository_released: item.study_repository_released,
              external_url: item.external_url,
              release_status: item.release_status,
              number_of_participants: item.number_of_participants,
              number_of_samples: item.number_of_samples,
              number_of_files: item.number_of_files,
              total_size_of_data_files: item.total_size_of_data_files,
              guid: item.guid
            }] AS study_statuses
          RETURN stu.guid AS guid, study_statuses
        page_size: 100
      - name: "consent_groups"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (consent_group:consent_group)-[:of_consent_group]->(stu)
          WITH
            stu,
            [item IN collect(DISTINCT consent_group) WHERE item IS NOT NULL | {
              consent_group_id: item.consent_group_id,
              consent_group_name: item.consent_group_name,
              consent_group_suffix: item.consent_group_suffix,
              guid: item.guid
            }] AS consent_groups
          RETURN stu.guid AS guid, consent_groups
        page_size: 100
      - name: "cell_lines"
        query: |
          MATCH (stu:study)
          WITH stu
          SKIP $skip LIMIT $limit

          OPTIONAL MATCH (cell_line:cell_line)-[:of_cell_line]->(stu)
          OPTIONAL MATCH (sample:sample)-[:of_sample]->(cell_line)
          OPTIONAL MATCH (sequencing_file:sequencing_file)-[:of_sequencing_file]->(sample)

          WITH
            stu,
            cell_line,
            sample,
            [item IN collect(DISTINCT sequencing_file) WHERE item IS NOT NULL | {
              sequencing_file_id: item.sequencing_file_id,
              file_name: item.file_name,
              data_category: item.data_category,
              file_type: item.file_type,
              file_description: item.file_description,
              file_size: item.file_size,
              md5sum: item.md5sum,
              file_mapping_level: item.file_mapping_level,
              file_access: item.file_access,
              acl: item.acl,
              authz: item.authz,
              file_url: item.file_url,
              dcf_indexd_guid: item.dcf_indexd_guid,
              library_id: item.library_id,
              library_selection: item.library_selection,
              library_strategy: item.library_strategy,
              library_layout: item.library_layout,
              library_source_material: item.library_source_material,
              library_source_molecule: item.library_source_molecule,
              number_of_bp: item.number_of_bp,
              number_of_reads: item.number_of_reads,
              design_description: item.design_description,
              platform: item.platform,
              instrument_model: item.instrument_model,
              avg_read_length: item.avg_read_length,
              coverage: item.coverage,
              reference_genome_assembly: item.reference_genome_assembly,
              checksum_algorithm: item.checksum_algorithm,
              checksum_value: item.checksum_value,
              custom_assembly_fasta_file_for_alignment: item.custom_assembly_fasta_file_for_alignment,
              sequence_alignment_software: item.sequence_alignment_software,
              cDNA_read: item.cDNA_read,
              cDNA_read_size: item.cDNA_read_size,
              cDNA_read_offset: item.cDNA_read_offset,
              cell_barcode_offset: item.cell_barcode_offset,
              cell_barcode_read: item.cell_barcode_read,
              cell_barcode_size: item.cell_barcode_size,
              single_cell_entity: item.single_cell_entity,
              dissociation_method: item.dissociation_method,
              end_bias: item.end_bias,
              total_number_of_input_cells: item.total_number_of_input_cells,
              library_construction_method: item.library_construction_method,
              library_strand: item.library_strand,
              protocols_link: item.protocols_link,
              reverse_transcription_primer: item.reverse_transcription_primer,
              spike_in: item.spike_in,
              UMI_barcode_offset: item.UMI_barcode_offset,
              UMI_barcode_read: item.UMI_barcode_read,
              UMI_barcode_size: item.UMI_barcode_size,
              guid: item.guid,
              crdc_id: item.crdc_id
            }] AS sequencing_files

          WITH
            stu,
            cell_line,
            [entry IN collect({sample: sample, sequencing_files: sequencing_files})
              WHERE entry.sample IS NOT NULL | {
                sample_id: entry.sample.sample_id,
                anatomic_site: entry.sample.anatomic_site,
                participant_age_at_collection: entry.sample.participant_age_at_collection,
                sample_tumor_status: entry.sample.sample_tumor_status,
                tumor_spatial_extent: entry.sample.tumor_spatial_extent,
                sample_description: entry.sample.sample_description,
                percent_tumor: entry.sample.percent_tumor,
                percent_necrosis: entry.sample.percent_necrosis,
                laterality: entry.sample.laterality,
                guid: entry.sample.guid,
                crdc_id: entry.sample.crdc_id,
                sequencing_files: entry.sequencing_files
              }
            ] AS samples

          WITH
            stu,
            [entry IN collect({cell_line: cell_line, samples: samples})
              WHERE entry.cell_line IS NOT NULL | {
                cell_line_id: entry.cell_line.cell_line_id,
                source: entry.cell_line.source,
                cell_line_passage_number: entry.cell_line.cell_line_passage_number,
                guid: entry.cell_line.guid,
                samples: entry.samples
              }
            ] AS cell_lines

          RETURN stu.guid AS guid, cell_lines
        page_size: 100

  # participant centric index for study and other facets
  - index_name: study_participants_faceted
    id_field: "id"
    mapping:
      keyword:
        - id
        - participant_id
        - race
        - race_str
        - sex_at_birth
        - study_id
        - dbgap_accession
        - study_phase
        - study_acronym
        - study_name
        - survival_filters.last_known_survival_status
        - survival_filters.cause_of_death
        - survival_filters.first_event
        - treatment_filters.treatment_type
        - treatment_filters.treatment_agent
        - treatment_response_filters.response
        - treatment_response_filters.response_category
        - treatment_response_filters.response_system
        - sample_diagnosis_genetic_analysis_file_filters.sample_anatomic_site
        - sample_diagnosis_genetic_analysis_file_filters.sample_tumor_status
        - sample_diagnosis_genetic_analysis_file_filters.tumor_spatial_extent
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_category
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_anatomic_site
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_classification_system
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_basis
        - sample_diagnosis_genetic_analysis_file_filters.disease_phase
        - sample_diagnosis_genetic_analysis_file_filters.reported_significance
        - sample_diagnosis_genetic_analysis_file_filters.reported_significance_system
        - sample_diagnosis_genetic_analysis_file_filters.gene_symbol
        - sample_diagnosis_genetic_analysis_file_filters.alteration
        - sample_diagnosis_genetic_analysis_file_filters.fusion_partner_gene
        - sample_diagnosis_genetic_analysis_file_filters.alteration_type
        - sample_diagnosis_genetic_analysis_file_filters.status
        - sample_diagnosis_genetic_analysis_file_filters.data_category
        - sample_diagnosis_genetic_analysis_file_filters.file_type
        - sample_diagnosis_genetic_analysis_file_filters.file_mapping_level
        - sample_diagnosis_genetic_analysis_file_filters.library_selection
        - sample_diagnosis_genetic_analysis_file_filters.library_source_material
        - sample_diagnosis_genetic_analysis_file_filters.library_source_molecule
        - sample_diagnosis_genetic_analysis_file_filters.library_strategy
      integer:
        - survival_filters.age_at_last_known_survival_status
        - treatment_filters.age_at_treatment_start
        - treatment_filters.age_at_treatment_end
        - treatment_response_filters.age_at_response
        - sample_diagnosis_genetic_analysis_file_filters.participant_age_at_collection
        - sample_diagnosis_genetic_analysis_file_filters.age_at_diagnosis
    # Cypher query will be used to retrieve data from Neo4j, and index into Elasticsearch
    initial_query:
      query: |
        MATCH (p:participant)
        WITH p
        SKIP $skip LIMIT $limit
        RETURN p.guid AS id
      page_size: 5000
    update_queries:
      - name: "study_properties"
        query: |
          MATCH
            // Participant -> Consent Group -> Study
            (p:participant),
            (cg:consent_group)<-[:of_participant]->(p),
            (stu:study)<-[:of_consent_group]-(cg)
          WITH p, stu
          SKIP $skip LIMIT $limit
          RETURN
            p.guid AS id,
            stu.study_id AS study_id,
            stu.dbgap_accession AS dbgap_accession,
            stu.study_phase AS study_phase,
            stu.study_acronym AS study_acronym,
            stu.study_name AS study_name
        page_size: 1000
      - name: "participant_properties"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          RETURN
            p.guid AS id,
            p.participant_id AS participant_id,
            p.race AS race,
            reduce(result = '', x IN p.race |
              CASE
                WHEN result = "" THEN x
                ELSE result + ";" + x
              END
            ) AS race_str,
            p.sex_at_birth AS sex_at_birth
        page_size: 5000
      - name: "survival_filters"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sur:survival)-[:of_survival]->(p)
          WITH
            p.guid AS id,
            count(sur) AS survival_count,
            [item IN collect(
              DISTINCT {
                last_known_survival_status: sur.last_known_survival_status,
                cause_of_death: sur.cause_of_death,
                first_event: sur.first_event,
                age_at_last_known_survival_status: sur.age_at_last_known_survival_status
              }
            ) WHERE item IS NOT NULL] AS survival_filters
          WITH
            id,
            CASE
              WHEN survival_count >0
              THEN survival_filters
              ELSE []
            END AS survival_filters
          RETURN id, survival_filters
        page_size: 5000
      - name: "treatment_filters"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit

          // Build a superlist of treatment filters
          CALL {
            // treatment_chemotherapy
            WITH p
            OPTIONAL MATCH (tchemo:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(p)
            WITH
              p.guid AS id,
              count(tchemo) AS tchemo_count,
              collect(DISTINCT {
                treatment_type: CASE
                  WHEN tchemo.chemotherapy_type IS NULL OR size(tchemo.chemotherapy_type) = 0 THEN ['chemotherapy']
                  ELSE [x IN tchemo.chemotherapy_type WHERE x IS NOT NULL | trim(x)]
                END,
                treatment_agent: tchemo.chemotherapy_agent,
                age_at_treatment_start: tchemo.age_at_treatment_start,
                age_at_treatment_end: tchemo.age_at_treatment_end
              }) AS treatment_filters
            WITH
              id,
              CASE
                WHEN tchemo_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters

            UNION

            // treatment_radiation
            WITH p
            OPTIONAL MATCH (trad:treatment_radiation)-[:of_treatment_radiation]->(p)
            WITH
              p.guid AS id,
              count(trad) AS trad_count,
              [item in collect(
                DISTINCT {
                  treatment_type: trad.treatment_type,
                  treatment_agent: trad.radiation_therapy_type,
                  age_at_treatment_start: trad.age_at_treatment_start,
                  age_at_treatment_end: trad.age_at_treatment_end
                }
              ) WHERE item IS NOT NULL] AS treatment_filters
            WITH
              id,
              CASE
                WHEN trad_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters

            UNION

            // treatment_surgery
            WITH p
            OPTIONAL MATCH (tsurg:treatment_surgery)-[:of_treatment_surgery]->(p)
            WITH
              p.guid AS id,
              count(tsurg) AS tsurg_count,
              [item in collect(
                DISTINCT {
                  treatment_type: tsurg.treatment_type,
                  treatment_agent: tsurg.surgery_type,
                  age_at_treatment_start: tsurg.age_at_treatment_start,
                  age_at_treatment_end: tsurg.age_at_treatment_end
                }
              ) WHERE item IS NOT NULL] AS treatment_filters
            WITH
              id,
              CASE
                WHEN tsurg_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters

            UNION

            // treatment_other
            WITH p
            OPTIONAL MATCH (toth:treatment_other)-[:of_treatment_other]->(p)
            WITH
              p.guid AS id,
              count(toth) AS toth_count,
              [item in collect(
                DISTINCT {
                  treatment_type: toth.treatment_type,
                  treatment_agent: toth.other_treatment_type,
                  age_at_treatment_start: toth.age_at_treatment_start,
                  age_at_treatment_end: toth.age_at_treatment_end
                }
              ) WHERE item IS NOT NULL] AS treatment_filters
            WITH
              id,
              CASE
                WHEN toth_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters
          }
          WITH
            id,
            collect(treatment_filters) AS treatment_filters_superlist
          WITH
            id,
            reduce(all_filters = [], filters IN treatment_filters_superlist | all_filters + filters) AS treatment_filters
          RETURN id, treatment_filters
        page_size: 5000
      - name: "treatment_response_filters"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (tresp:treatment_response)-[:of_treatment_response]->(p)
          WITH
            p.guid AS id,
            count(tresp) AS treatment_response_count,
            [item IN collect(
              DISTINCT {
                age_at_response: tresp.age_at_response,
                response: tresp.response,
                response_category: tresp.response_category,
                response_system: tresp.response_system
              }
            ) WHERE item IS NOT NULL] AS treatment_response_filters
          WITH
            id,
            CASE
              WHEN treatment_response_count > 0
              THEN treatment_response_filters
              ELSE []
            END AS treatment_response_filters
          RETURN id, treatment_response_filters
        page_size: 5000
      - name: "sample_diagnosis_genetic_analysis_file_filters"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit

          // Direct participant samples: self-context
          OPTIONAL MATCH (p)<--(s1:sample)
          WITH
            p,
            collect(DISTINCT { sm: s1, source_sm: s1 }) AS direct_contexts

          // Model-linked samples: p <- Sample_1 <- cell_line/pdx <- Sample_2
          OPTIONAL MATCH (p)<-[:of_sample]-(s1_model:sample)<--(model)<--(s2:sample)
          WHERE model:cell_line OR model:pdx

          WITH
            p,
            [ctx IN (
              direct_contexts
              + collect(DISTINCT { sm: s1_model, source_sm: s1_model })
              + collect(DISTINCT { sm: s2, source_sm: s2 })
              + collect(DISTINCT { sm: s1_model, source_sm: s2 })
              + collect(DISTINCT { sm: s2, source_sm: s1_model })
            )
            WHERE ctx.sm IS NOT NULL AND ctx.source_sm IS NOT NULL] AS sample_contexts

          WITH
            p,
            CASE
              WHEN size(sample_contexts) = 0 THEN [{ sm: null, source_sm: null }]
              ELSE sample_contexts
            END AS sample_contexts

          // Study-level files
          OPTIONAL MATCH (st:study)<--(:consent_group)<--(p)
          OPTIONAL MATCH (st)<--(study_file)
          WHERE study_file:clinical_measure_file
            OR study_file:generic_file

          WITH
            p,
            sample_contexts,
            collect(DISTINCT study_file) AS study_files

          OPTIONAL MATCH (p)<--(participant_file)
          WHERE participant_file:clinical_measure_file
            OR participant_file:generic_file
            OR participant_file:radiology_file

          WITH
            p,
            sample_contexts,
            study_files,
            collect(DISTINCT participant_file) AS participant_files

          OPTIONAL MATCH (p)<--(participant_dg:diagnosis)

          WITH
            p,
            sample_contexts,
            study_files,
            participant_files,
            collect(DISTINCT participant_dg) AS participant_diagnoses

          OPTIONAL MATCH (p)<--(participant_ga:genetic_analysis)

          WITH
            p,
            sample_contexts,
            study_files,
            participant_files,
            participant_diagnoses,
            collect(DISTINCT participant_ga) AS participant_gas

          UNWIND sample_contexts AS ctx

          WITH
            p,
            ctx.sm AS sm,
            ctx.source_sm AS source_sm,
            study_files,
            participant_files,
            participant_diagnoses,
            participant_gas

          // Source sample-level files
          OPTIONAL MATCH (source_sm)<--(sample_file)
          WHERE sample_file:sequencing_file
            OR sample_file:generic_file
            OR sample_file:methylation_array_file
            OR sample_file:pathology_file
            OR sample_file:cytogenomic_file

          WITH
            p,
            sm,
            source_sm,
            study_files,
            participant_files,
            participant_diagnoses,
            participant_gas,
            collect(DISTINCT sample_file) AS sample_files

          // Source sample-level diagnosis
          OPTIONAL MATCH (source_sm)<--(sample_dg:diagnosis)

          WITH
            p,
            sm,
            source_sm,
            study_files,
            participant_files,
            participant_diagnoses,
            participant_gas,
            sample_files,
            collect(DISTINCT sample_dg) AS sample_diagnoses

          // Source sample-level genetic analysis
          OPTIONAL MATCH (source_sm)<--(sample_ga:genetic_analysis)

          WITH
            p,
            sm,
            sample_files + participant_files + study_files AS files,
            sample_diagnoses + participant_diagnoses AS diagnoses,
            collect(DISTINCT sample_ga) + participant_gas AS gas

          CALL {
            WITH files

            UNWIND CASE WHEN size(files) = 0 THEN [null] ELSE files END AS file

            WITH DISTINCT
              file.data_category AS data_category,
              file.file_type AS file_type,
              file.file_mapping_level AS file_mapping_level,
              CASE
                WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_selection
                ELSE null
              END AS library_selection,
              CASE
                WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_source_material
                ELSE null
              END AS library_source_material,
              CASE
                WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_source_molecule
                ELSE null
              END AS library_source_molecule,
              CASE
                WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_strategy
                ELSE null
              END AS library_strategy

            RETURN collect([
              data_category,
              file_type,
              file_mapping_level,
              library_selection,
              library_source_material,
              library_source_molecule,
              library_strategy
            ]) AS file_filter_keys
          }

          CALL {
            WITH diagnoses

            UNWIND CASE WHEN size(diagnoses) = 0 THEN [null] ELSE diagnoses END AS dg

            WITH DISTINCT
              dg.age_at_diagnosis AS age_at_diagnosis,
              dg.anatomic_site AS diagnosis_anatomic_site,
              dg.disease_phase AS disease_phase,
              dg.diagnosis_classification_system AS diagnosis_classification_system,
              dg.diagnosis_category AS diagnosis_category,
              dg.diagnosis_basis AS diagnosis_basis,
              dg.diagnosis AS diagnosis

            RETURN collect([
              age_at_diagnosis,
              diagnosis_anatomic_site,
              disease_phase,
              diagnosis_classification_system,
              diagnosis_category,
              diagnosis_basis,
              diagnosis
            ]) AS diagnosis_filter_keys
          }

          CALL {
            WITH gas

            UNWIND CASE WHEN size(gas) = 0 THEN [null] ELSE gas END AS ga

            WITH DISTINCT
              ga.reported_significance AS reported_significance,
              ga.reported_significance_system AS reported_significance_system,
              ga.gene_symbol AS gene_symbol,
              ga.alteration AS alteration,
              ga.fusion_partner_gene AS fusion_partner_gene,
              ga.alteration_type AS alteration_type,
              ga.status AS status

            RETURN collect([
              reported_significance,
              reported_significance_system,
              gene_symbol,
              alteration,
              fusion_partner_gene,
              alteration_type,
              status
            ]) AS genetic_analysis_filter_keys
          }

          UNWIND file_filter_keys AS file_filter_key
          UNWIND diagnosis_filter_keys AS diagnosis_filter_key
          UNWIND genetic_analysis_filter_keys AS genetic_analysis_filter_key

          WITH DISTINCT
            p.guid AS id,

            // Sample facets
            sm.anatomic_site AS sample_anatomic_site,
            sm.participant_age_at_collection AS participant_age_at_collection,
            sm.sample_tumor_status AS sample_tumor_status,
            sm.tumor_spatial_extent AS tumor_spatial_extent,

            // Diagnosis facets
            diagnosis_filter_key[0] AS age_at_diagnosis,
            diagnosis_filter_key[1] AS diagnosis_anatomic_site,
            diagnosis_filter_key[2] AS disease_phase,
            diagnosis_filter_key[3] AS diagnosis_classification_system,
            diagnosis_filter_key[4] AS diagnosis_category,
            diagnosis_filter_key[5] AS diagnosis_basis,
            diagnosis_filter_key[6] AS diagnosis,

            // Genetic Analysis facets
            genetic_analysis_filter_key[0] AS reported_significance,
            genetic_analysis_filter_key[1] AS reported_significance_system,
            genetic_analysis_filter_key[2] AS gene_symbol,
            genetic_analysis_filter_key[3] AS alteration,
            genetic_analysis_filter_key[4] AS fusion_partner_gene,
            genetic_analysis_filter_key[5] AS alteration_type,
            genetic_analysis_filter_key[6] AS status,

            // File facets
            file_filter_key[0] AS data_category,
            file_filter_key[1] AS file_type,
            file_filter_key[2] AS file_mapping_level,
            file_filter_key[3] AS library_selection,
            file_filter_key[4] AS library_source_material,
            file_filter_key[5] AS library_source_molecule,
            file_filter_key[6] AS library_strategy

          RETURN
            id,
            collect({
              sample_anatomic_site: sample_anatomic_site,
              participant_age_at_collection: participant_age_at_collection,
              sample_tumor_status: sample_tumor_status,
              tumor_spatial_extent: tumor_spatial_extent,
              age_at_diagnosis: age_at_diagnosis,
              diagnosis_anatomic_site: diagnosis_anatomic_site,
              disease_phase: disease_phase,
              diagnosis_classification_system: diagnosis_classification_system,
              diagnosis_category: diagnosis_category,
              diagnosis_basis: diagnosis_basis,
              diagnosis: diagnosis,
              reported_significance: reported_significance,
              reported_significance_system: reported_significance_system,
              gene_symbol: gene_symbol,
              alteration: alteration,
              fusion_partner_gene: fusion_partner_gene,
              alteration_type: alteration_type,
              status: status,
              data_category: data_category,
              file_type: file_type,
              file_mapping_level: file_mapping_level,
              library_selection: library_selection,
              library_source_material: library_source_material,
              library_source_molecule: library_source_molecule,
              library_strategy: library_strategy
            }) AS sample_diagnosis_genetic_analysis_file_filters
        page_size: 5000

  # Data points for KM plot
  # Excludes survival records with bad age_at_last_known_survival_status
  # Excludes survival records associated with bad age_at_diagnosis
  - index_name: km_plot_data
    id_field: id
    mapping:
      keyword:
        - id
      boolean:
        - is_valid
      integer:
        - time
        - event
    initial_query:
      query: |
        MATCH (p:participant)
        WITH p
        SKIP $skip LIMIT $limit

        // Direct participant samples: self-context
        OPTIONAL MATCH (p)<--(s1:sample)
        WITH
          p,
          collect(DISTINCT { sm: s1, source_sm: s1 }) AS direct_contexts

        // Model-linked samples: p <- Sample_1 <- cell_line/pdx <- Sample_2
        OPTIONAL MATCH (p)<-[:of_sample]-(s1_model:sample)<--(model)<--(s2:sample)
        WHERE model:cell_line OR model:pdx

        WITH
          p,
          [ctx IN (
            direct_contexts
            + collect(DISTINCT { sm: s1_model, source_sm: s1_model })
            + collect(DISTINCT { sm: s2, source_sm: s2 })
            + collect(DISTINCT { sm: s1_model, source_sm: s2 })
            + collect(DISTINCT { sm: s2, source_sm: s1_model })
          )
          WHERE ctx.sm IS NOT NULL AND ctx.source_sm IS NOT NULL] AS sample_contexts

        WITH
          p,
          CASE
            WHEN size(sample_contexts) = 0 THEN [{ sm: null, source_sm: p }]
            ELSE sample_contexts
          END AS sample_contexts

        UNWIND sample_contexts AS ctx
        WITH
          p,
          collect(DISTINCT ctx.source_sm) AS source_sms

        // Participant-level diagnoses
        OPTIONAL MATCH (p)<-[:of_diagnosis]-(participant_dg:diagnosis)

        WITH
          p,
          source_sms,
          collect(DISTINCT participant_dg) AS participant_diagnoses

        // Source sample-level diagnoses
        UNWIND CASE WHEN size(source_sms) = 0 THEN [null] ELSE source_sms END AS source_sm
        OPTIONAL MATCH (source_sm)<-[:of_diagnosis]-(sample_dg:diagnosis)

        WITH
          p,
          [dg IN (
            participant_diagnoses + collect(DISTINCT sample_dg)
          ) WHERE dg IS NOT NULL] AS diagnoses

        // Survival records
        OPTIONAL MATCH (p)<-[:of_survival]-(sur:survival)

        WITH
          p,
          diagnoses,
          collect(DISTINCT sur) AS survivals

        WITH
          p,
          [diagnosis IN diagnoses
            WHERE diagnosis.age_at_diagnosis IS NOT NULL |
            diagnosis.age_at_diagnosis] AS diag_ages,
          [survival IN survivals
            WHERE survival.age_at_last_known_survival_status IS NOT NULL |
            survival.age_at_last_known_survival_status] AS surv_ages,
          survivals

        WITH
          p,
          diag_ages,
          surv_ages,
          survivals,
          NOT ANY(a IN diag_ages WHERE a = -999)
            AND NOT ANY(a IN surv_ages WHERE a = -999)
            AND size(diag_ages) > 0
            AND size(surv_ages) > 0 AS has_required_ages

        WITH
          p,
          diag_ages,
          surv_ages,
          survivals,
          has_required_ages,
          CASE
            WHEN has_required_ages THEN
              REDUCE(
                m = NULL,
                a IN diag_ages |
                  CASE
                    WHEN m IS NULL OR a < m THEN a
                    ELSE m
                  END
              )
            ELSE NULL
          END AS min_diag_age,
          CASE
            WHEN has_required_ages THEN
              REDUCE(
                m = NULL,
                a IN surv_ages |
                  CASE
                    WHEN m IS NULL OR a > m THEN a
                    ELSE m
                  END
              )
            ELSE NULL
          END AS max_surv_age

        WITH
          p,
          survivals,
          min_diag_age,
          max_surv_age,
          has_required_ages
            AND min_diag_age IS NOT NULL
            AND max_surv_age IS NOT NULL
            AND min_diag_age <= max_surv_age AS is_valid

        WITH
          p,
          min_diag_age,
          max_surv_age,
          is_valid,
          CASE
            WHEN is_valid THEN
              [survival IN survivals
                WHERE survival.age_at_last_known_survival_status = max_surv_age]
            ELSE []
          END AS max_survs

        RETURN
          p.guid AS id,
          is_valid,
          CASE
            WHEN is_valid THEN max_surv_age - min_diag_age
            ELSE NULL
          END AS time,
          CASE
            WHEN is_valid AND ANY(surv IN max_survs WHERE surv.last_known_survival_status = "Dead") THEN 1
            WHEN is_valid THEN 0
            ELSE NULL
          END AS event
      page_size: 2000

  # participant index for table display and participant level facets
  - index_name: participants_table
    id_field: "id"
    mapping:
      keyword:
        - id
        - participant_id
        - race
        - race_str
        - sex_at_birth
        - study_id
        - dbgap_accession
        - study_phase
        - study_acronym
        - study_name
        - survival_filters.last_known_survival_status
        - survival_filters.cause_of_death
        - survival_filters.first_event
        - treatment_filters.treatment_type
        - treatment_filters.treatment_agent
        - treatment_response_filters.response
        - treatment_response_filters.response_category
        - treatment_response_filters.response_system
        - sample_diagnosis_genetic_analysis_file_filters.sample_anatomic_site
        - sample_diagnosis_genetic_analysis_file_filters.sample_tumor_status
        - sample_diagnosis_genetic_analysis_file_filters.tumor_spatial_extent
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_category
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_anatomic_site
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_classification_system
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_basis
        - sample_diagnosis_genetic_analysis_file_filters.disease_phase
        - sample_diagnosis_genetic_analysis_file_filters.reported_significance
        - sample_diagnosis_genetic_analysis_file_filters.reported_significance_system
        - sample_diagnosis_genetic_analysis_file_filters.gene_symbol
        - sample_diagnosis_genetic_analysis_file_filters.alteration
        - sample_diagnosis_genetic_analysis_file_filters.fusion_partner_gene
        - sample_diagnosis_genetic_analysis_file_filters.alteration_type
        - sample_diagnosis_genetic_analysis_file_filters.status
        - sample_diagnosis_genetic_analysis_file_filters.data_category
        - sample_diagnosis_genetic_analysis_file_filters.file_type
        - sample_diagnosis_genetic_analysis_file_filters.file_mapping_level
        - sample_diagnosis_genetic_analysis_file_filters.library_selection
        - sample_diagnosis_genetic_analysis_file_filters.library_source_material
        - sample_diagnosis_genetic_analysis_file_filters.library_source_molecule
        - sample_diagnosis_genetic_analysis_file_filters.library_strategy
      integer:
        - survival_filters.age_at_last_known_survival_status
        - treatment_filters.age_at_treatment_start
        - treatment_filters.age_at_treatment_end
        - treatment_response_filters.age_at_response
        - sample_diagnosis_genetic_analysis_file_filters.participant_age_at_collection
        - sample_diagnosis_genetic_analysis_file_filters.age_at_diagnosis
    # Cypher query will be used to retrieve data from Neo4j, and index into Elasticsearch
    initial_query:
      query: |
        MATCH (p:participant)
        WITH p
        SKIP $skip LIMIT $limit
        RETURN p.guid AS id
      page_size: 5000
    update_queries:
      - name: "study_properties"
        query: |
          MATCH
            // Participant -> Consent Group -> Study
            (p:participant),
            (cg:consent_group)<-[:of_participant]->(p),
            (stu:study)<-[:of_consent_group]-(cg)
          WITH p, stu
          SKIP $skip LIMIT $limit
          RETURN
            p.guid AS id,
            stu.study_id AS study_id,
            stu.dbgap_accession AS dbgap_accession,
            stu.study_phase AS study_phase,
            stu.study_acronym AS study_acronym,
            stu.study_name AS study_name
        page_size: 1000
      - name: "participant_properties"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          RETURN
            p.guid AS id,
            p.participant_id AS participant_id,
            p.race AS race,
            reduce(result = '', x IN p.race |
              CASE
                WHEN result = "" THEN x
                ELSE result + ";" + x
              END
            ) AS race_str,
            p.sex_at_birth AS sex_at_birth
        page_size: 5000
      - name: "survival_filters"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sur:survival)-[:of_survival]->(p)
          WITH
            p.guid AS id,
            count(sur) AS survival_count,
            [item IN collect(
              DISTINCT {
                last_known_survival_status: sur.last_known_survival_status,
                cause_of_death: sur.cause_of_death,
                first_event: sur.first_event,
                age_at_last_known_survival_status: sur.age_at_last_known_survival_status
              }
            ) WHERE item IS NOT NULL] AS survival_filters
          WITH
            id,
            CASE
              WHEN survival_count >0
              THEN survival_filters
              ELSE []
            END AS survival_filters
          RETURN id, survival_filters
        page_size: 5000
      - name: "treatment_filters"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit

          // Build a superlist of treatment filters
          CALL {
            // treatment_chemotherapy
            WITH p
            OPTIONAL MATCH (tchemo:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(p)
            WITH
              p.guid AS id,
              count(tchemo) AS tchemo_count,
              collect(DISTINCT {
                treatment_type: CASE
                  WHEN tchemo.chemotherapy_type IS NULL OR size(tchemo.chemotherapy_type) = 0 THEN ['chemotherapy']
                  ELSE [x IN tchemo.chemotherapy_type WHERE x IS NOT NULL | trim(x)]
                END,
                treatment_agent: tchemo.chemotherapy_agent,
                age_at_treatment_start: tchemo.age_at_treatment_start,
                age_at_treatment_end: tchemo.age_at_treatment_end
              }) AS treatment_filters
            WITH
              id,
              CASE
                WHEN tchemo_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters

            UNION

            // treatment_radiation
            WITH p
            OPTIONAL MATCH (trad:treatment_radiation)-[:of_treatment_radiation]->(p)
            WITH
              p.guid AS id,
              count(trad) AS trad_count,
              [item in collect(
                DISTINCT {
                  treatment_type: trad.treatment_type,
                  treatment_agent: trad.radiation_therapy_type,
                  age_at_treatment_start: trad.age_at_treatment_start,
                  age_at_treatment_end: trad.age_at_treatment_end
                }
              ) WHERE item IS NOT NULL] AS treatment_filters
            WITH
              id,
              CASE
                WHEN trad_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters

            UNION

            // treatment_surgery
            WITH p
            OPTIONAL MATCH (tsurg:treatment_surgery)-[:of_treatment_surgery]->(p)
            WITH
              p.guid AS id,
              count(tsurg) AS tsurg_count,
              [item in collect(
                DISTINCT {
                  treatment_type: tsurg.treatment_type,
                  treatment_agent: tsurg.surgery_type,
                  age_at_treatment_start: tsurg.age_at_treatment_start,
                  age_at_treatment_end: tsurg.age_at_treatment_end
                }
              ) WHERE item IS NOT NULL] AS treatment_filters
            WITH
              id,
              CASE
                WHEN tsurg_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters

            UNION

            // treatment_other
            WITH p
            OPTIONAL MATCH (toth:treatment_other)-[:of_treatment_other]->(p)
            WITH
              p.guid AS id,
              count(toth) AS toth_count,
              [item in collect(
                DISTINCT {
                  treatment_type: toth.treatment_type,
                  treatment_agent: toth.other_treatment_type,
                  age_at_treatment_start: toth.age_at_treatment_start,
                  age_at_treatment_end: toth.age_at_treatment_end
                }
              ) WHERE item IS NOT NULL] AS treatment_filters
            WITH
              id,
              CASE
                WHEN toth_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters
          }
          WITH
            id,
            collect(treatment_filters) AS treatment_filters_superlist
          WITH
            id,
            reduce(all_filters = [], filters IN treatment_filters_superlist | all_filters + filters) AS treatment_filters
          RETURN id, treatment_filters
        page_size: 5000
      - name: "treatment_response_filters"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (tresp:treatment_response)-[:of_treatment_response]->(p)
          WITH
            p.guid AS id,
            count(tresp) AS treatment_response_count,
            [item IN collect(
              DISTINCT {
                age_at_response: tresp.age_at_response,
                response: tresp.response,
                response_category: tresp.response_category,
                response_system: tresp.response_system
              }
            ) WHERE item IS NOT NULL] AS treatment_response_filters
          WITH
            id,
            CASE
              WHEN treatment_response_count > 0
              THEN treatment_response_filters
              ELSE []
            END AS treatment_response_filters
          RETURN id, treatment_response_filters
        page_size: 5000
      - name: "sample_diagnosis_genetic_analysis_file_filters"
        query: |
          MATCH (p:participant)
          WITH p
          SKIP $skip LIMIT $limit

          // Direct participant samples: self-context
          OPTIONAL MATCH (p)<--(s1:sample)
          WITH
            p,
            collect(DISTINCT { sm: s1, source_sm: s1 }) AS direct_contexts

          // Model-linked samples: p <- Sample_1 <- cell_line/pdx <- Sample_2
          OPTIONAL MATCH (p)<-[:of_sample]-(s1_model:sample)<--(model)<--(s2:sample)
          WHERE model:cell_line OR model:pdx

          WITH
            p,
            [ctx IN (
              direct_contexts
              + collect(DISTINCT { sm: s1_model, source_sm: s1_model })
              + collect(DISTINCT { sm: s2, source_sm: s2 })
              + collect(DISTINCT { sm: s1_model, source_sm: s2 })
              + collect(DISTINCT { sm: s2, source_sm: s1_model })
            )
            WHERE ctx.sm IS NOT NULL AND ctx.source_sm IS NOT NULL] AS sample_contexts

          WITH
            p,
            CASE
              WHEN size(sample_contexts) = 0 THEN [{ sm: null, source_sm: null }]
              ELSE sample_contexts
            END AS sample_contexts

          // Study-level files
          OPTIONAL MATCH (st:study)<--(:consent_group)<--(p)
          OPTIONAL MATCH (st)<--(study_file)
          WHERE study_file:clinical_measure_file
            OR study_file:generic_file

          WITH
            p,
            sample_contexts,
            collect(DISTINCT study_file) AS study_files

          OPTIONAL MATCH (p)<--(participant_file)
          WHERE participant_file:clinical_measure_file
            OR participant_file:generic_file
            OR participant_file:radiology_file

          WITH
            p,
            sample_contexts,
            study_files,
            collect(DISTINCT participant_file) AS participant_files

          OPTIONAL MATCH (p)<--(participant_dg:diagnosis)

          WITH
            p,
            sample_contexts,
            study_files,
            participant_files,
            collect(DISTINCT participant_dg) AS participant_diagnoses

          OPTIONAL MATCH (p)<--(participant_ga:genetic_analysis)

          WITH
            p,
            sample_contexts,
            study_files,
            participant_files,
            participant_diagnoses,
            collect(DISTINCT participant_ga) AS participant_gas

          UNWIND sample_contexts AS ctx

          WITH
            p,
            ctx.sm AS sm,
            ctx.source_sm AS source_sm,
            study_files,
            participant_files,
            participant_diagnoses,
            participant_gas

          // Source sample-level files
          OPTIONAL MATCH (source_sm)<--(sample_file)
          WHERE sample_file:sequencing_file
            OR sample_file:generic_file
            OR sample_file:methylation_array_file
            OR sample_file:pathology_file
            OR sample_file:cytogenomic_file

          WITH
            p,
            sm,
            source_sm,
            study_files,
            participant_files,
            participant_diagnoses,
            participant_gas,
            collect(DISTINCT sample_file) AS sample_files

          // Source sample-level diagnosis
          OPTIONAL MATCH (source_sm)<--(sample_dg:diagnosis)

          WITH
            p,
            sm,
            source_sm,
            study_files,
            participant_files,
            participant_diagnoses,
            participant_gas,
            sample_files,
            collect(DISTINCT sample_dg) AS sample_diagnoses

          // Source sample-level genetic analysis
          OPTIONAL MATCH (source_sm)<--(sample_ga:genetic_analysis)

          WITH
            p,
            sm,
            sample_files + participant_files + study_files AS files,
            sample_diagnoses + participant_diagnoses AS diagnoses,
            collect(DISTINCT sample_ga) + participant_gas AS gas

          CALL {
            WITH files

            UNWIND CASE WHEN size(files) = 0 THEN [null] ELSE files END AS file

            WITH DISTINCT
              file.data_category AS data_category,
              file.file_type AS file_type,
              file.file_mapping_level AS file_mapping_level,
              CASE
                WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_selection
                ELSE null
              END AS library_selection,
              CASE
                WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_source_material
                ELSE null
              END AS library_source_material,
              CASE
                WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_source_molecule
                ELSE null
              END AS library_source_molecule,
              CASE
                WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_strategy
                ELSE null
              END AS library_strategy

            RETURN collect([
              data_category,
              file_type,
              file_mapping_level,
              library_selection,
              library_source_material,
              library_source_molecule,
              library_strategy
            ]) AS file_filter_keys
          }

          CALL {
            WITH diagnoses

            UNWIND CASE WHEN size(diagnoses) = 0 THEN [null] ELSE diagnoses END AS dg

            WITH DISTINCT
              dg.age_at_diagnosis AS age_at_diagnosis,
              dg.anatomic_site AS diagnosis_anatomic_site,
              dg.disease_phase AS disease_phase,
              dg.diagnosis_classification_system AS diagnosis_classification_system,
              dg.diagnosis_category AS diagnosis_category,
              dg.diagnosis_basis AS diagnosis_basis,
              dg.diagnosis AS diagnosis

            RETURN collect([
              age_at_diagnosis,
              diagnosis_anatomic_site,
              disease_phase,
              diagnosis_classification_system,
              diagnosis_category,
              diagnosis_basis,
              diagnosis
            ]) AS diagnosis_filter_keys
          }

          CALL {
            WITH gas

            UNWIND CASE WHEN size(gas) = 0 THEN [null] ELSE gas END AS ga

            WITH DISTINCT
              ga.reported_significance AS reported_significance,
              ga.reported_significance_system AS reported_significance_system,
              ga.gene_symbol AS gene_symbol,
              ga.alteration AS alteration,
              ga.fusion_partner_gene AS fusion_partner_gene,
              ga.alteration_type AS alteration_type,
              ga.status AS status

            RETURN collect([
              reported_significance,
              reported_significance_system,
              gene_symbol,
              alteration,
              fusion_partner_gene,
              alteration_type,
              status
            ]) AS genetic_analysis_filter_keys
          }

          UNWIND file_filter_keys AS file_filter_key
          UNWIND diagnosis_filter_keys AS diagnosis_filter_key
          UNWIND genetic_analysis_filter_keys AS genetic_analysis_filter_key

          WITH DISTINCT
            p.guid AS id,

            // Sample facets
            sm.anatomic_site AS sample_anatomic_site,
            sm.participant_age_at_collection AS participant_age_at_collection,
            sm.sample_tumor_status AS sample_tumor_status,
            sm.tumor_spatial_extent AS tumor_spatial_extent,

            // Diagnosis facets
            diagnosis_filter_key[0] AS age_at_diagnosis,
            diagnosis_filter_key[1] AS diagnosis_anatomic_site,
            diagnosis_filter_key[2] AS disease_phase,
            diagnosis_filter_key[3] AS diagnosis_classification_system,
            diagnosis_filter_key[4] AS diagnosis_category,
            diagnosis_filter_key[5] AS diagnosis_basis,
            diagnosis_filter_key[6] AS diagnosis,

            // Genetic Analysis facets
            genetic_analysis_filter_key[0] AS reported_significance,
            genetic_analysis_filter_key[1] AS reported_significance_system,
            genetic_analysis_filter_key[2] AS gene_symbol,
            genetic_analysis_filter_key[3] AS alteration,
            genetic_analysis_filter_key[4] AS fusion_partner_gene,
            genetic_analysis_filter_key[5] AS alteration_type,
            genetic_analysis_filter_key[6] AS status,

            // File facets
            file_filter_key[0] AS data_category,
            file_filter_key[1] AS file_type,
            file_filter_key[2] AS file_mapping_level,
            file_filter_key[3] AS library_selection,
            file_filter_key[4] AS library_source_material,
            file_filter_key[5] AS library_source_molecule,
            file_filter_key[6] AS library_strategy

          RETURN
            id,
            collect({
              sample_anatomic_site: sample_anatomic_site,
              participant_age_at_collection: participant_age_at_collection,
              sample_tumor_status: sample_tumor_status,
              tumor_spatial_extent: tumor_spatial_extent,
              age_at_diagnosis: age_at_diagnosis,
              diagnosis_anatomic_site: diagnosis_anatomic_site,
              disease_phase: disease_phase,
              diagnosis_classification_system: diagnosis_classification_system,
              diagnosis_category: diagnosis_category,
              diagnosis_basis: diagnosis_basis,
              diagnosis: diagnosis,
              reported_significance: reported_significance,
              reported_significance_system: reported_significance_system,
              gene_symbol: gene_symbol,
              alteration: alteration,
              fusion_partner_gene: fusion_partner_gene,
              alteration_type: alteration_type,
              status: status,
              data_category: data_category,
              file_type: file_type,
              file_mapping_level: file_mapping_level,
              library_selection: library_selection,
              library_source_material: library_source_material,
              library_source_molecule: library_source_molecule,
              library_strategy: library_strategy
            }) AS sample_diagnosis_genetic_analysis_file_filters
        page_size: 5000

  - index_name: survivals_table
    id_field: "id"
    mapping:
      keyword:
        - id
        - survival_id
        - study_id
        - dbgap_accession
        - study_phase
        - study_acronym
        - study_name
        - participant_id
        - pid
        - race
        - sex_at_birth
        - age_at_event_free_survival_status_str
        - age_at_last_known_survival_status_str
        - cause_of_death
        - event_free_survival_status
        - first_event
        - last_known_survival_status
        - treatment_filters.treatment_type
        - treatment_filters.treatment_agent
        - treatment_response_filters.response
        - treatment_response_filters.response_category
        - treatment_response_filters.response_system
        - sample_diagnosis_genetic_analysis_file_filters.sample_anatomic_site
        - sample_diagnosis_genetic_analysis_file_filters.sample_tumor_status
        - sample_diagnosis_genetic_analysis_file_filters.tumor_spatial_extent
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_category
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_anatomic_site
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_classification_system
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_basis
        - sample_diagnosis_genetic_analysis_file_filters.disease_phase
        - sample_diagnosis_genetic_analysis_file_filters.reported_significance
        - sample_diagnosis_genetic_analysis_file_filters.reported_significance_system
        - sample_diagnosis_genetic_analysis_file_filters.gene_symbol
        - sample_diagnosis_genetic_analysis_file_filters.alteration
        - sample_diagnosis_genetic_analysis_file_filters.fusion_partner_gene
        - sample_diagnosis_genetic_analysis_file_filters.alteration_type
        - sample_diagnosis_genetic_analysis_file_filters.status
        - sample_diagnosis_genetic_analysis_file_filters.data_category
        - sample_diagnosis_genetic_analysis_file_filters.file_type
        - sample_diagnosis_genetic_analysis_file_filters.file_mapping_level
        - sample_diagnosis_genetic_analysis_file_filters.library_selection
        - sample_diagnosis_genetic_analysis_file_filters.library_source_material
        - sample_diagnosis_genetic_analysis_file_filters.library_source_molecule
        - sample_diagnosis_genetic_analysis_file_filters.library_strategy
      integer:
        - age_at_event_free_survival_status
        - age_at_last_known_survival_status
        - treatment_filters.age_at_treatment_start
        - treatment_filters.age_at_treatment_end
        - treatment_response_filters.age_at_response
        - sample_diagnosis_genetic_analysis_file_filters.participant_age_at_collection
        - sample_diagnosis_genetic_analysis_file_filters.age_at_diagnosis
    # Cypher query will be used to retrieve data from Neo4j, and index into Elasticsearch
    initial_query:
      query: |
        MATCH (sur:survival)
        WITH sur
        SKIP $skip LIMIT $limit
        RETURN sur.guid AS id
      page_size: 5000
    update_queries:
      - name: "study_properties"
        query: |
          MATCH
            (sur:survival)-[:of_survival]->(p:participant),
            (cg:consent_group)<-[:of_participant]->(p),
            (stu:study)<-[:of_consent_group]-(cg)
          WITH sur.guid AS id, stu
          SKIP $skip LIMIT $limit
          RETURN
            id,
            stu.study_id AS study_id,
            stu.dbgap_accession AS dbgap_accession,
            stu.study_phase AS study_phase,
            stu.study_acronym AS study_acronym,
            stu.study_name AS study_name
        page_size: 5000
      - name: "participant_properties"
        query: |
          MATCH (sur:survival)-[:of_survival]->(p:participant)
          WITH sur.guid AS id, p
          SKIP $skip LIMIT $limit
          RETURN
            id,
            p.guid AS pid,
            p.participant_id AS participant_id,
            p.race AS race,
            p.sex_at_birth AS sex_at_birth
        page_size: 5000
      - name: "survival_properties"
        query: |
          MATCH (sur:survival)
          WITH sur
          SKIP $skip LIMIT $limit
          RETURN
            sur.guid AS id,
            sur.survival_id AS survival_id,
            sur.age_at_event_free_survival_status AS age_at_event_free_survival_status,
            toString(sur.age_at_event_free_survival_status) AS age_at_event_free_survival_status_str,
            sur.age_at_last_known_survival_status AS age_at_last_known_survival_status,
            toString(sur.age_at_last_known_survival_status) AS age_at_last_known_survival_status_str,
            sur.cause_of_death AS cause_of_death,
            sur.event_free_survival_status AS event_free_survival_status,
            sur.first_event AS first_event,
            sur.last_known_survival_status AS last_known_survival_status
        page_size: 5000
      - name: "treatment_filters"
        query: |
          MATCH (sur:survival)-[:of_survival]->(p:participant)
          WITH sur, p
          SKIP $skip LIMIT $limit

          // Build a superlist of treatment filters
          CALL {
            // treatment_chemotherapy
            WITH sur.guid AS id, p
            OPTIONAL MATCH (tchemo:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(p)
            WITH
              id,
              count(tchemo) AS tchemo_count,
              collect(DISTINCT {
                treatment_type: CASE
                  WHEN tchemo.chemotherapy_type IS NULL OR size(tchemo.chemotherapy_type) = 0 THEN ['chemotherapy']
                  ELSE [x IN tchemo.chemotherapy_type WHERE x IS NOT NULL | trim(x)]
                END,
                treatment_agent: tchemo.chemotherapy_agent,
                age_at_treatment_start: tchemo.age_at_treatment_start,
                age_at_treatment_end: tchemo.age_at_treatment_end
              }) AS treatment_filters
            WITH
              id,
              CASE
                WHEN tchemo_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters

            UNION

            // treatment_radiation
            WITH sur.guid AS id, p
            OPTIONAL MATCH (trad:treatment_radiation)-[:of_treatment_radiation]->(p)
            WITH
              id,
              count(trad) AS trad_count,
              collect(DISTINCT {
                treatment_type: CASE
                  WHEN trad.treatment_type IS NULL OR size(trad.treatment_type) = 0 THEN ['radiation']
                  ELSE [x IN trad.treatment_type WHERE x IS NOT NULL | trim(x)]
                END,
                treatment_agent: trad.radiation_therapy_type,
                age_at_treatment_start: trad.age_at_treatment_start,
                age_at_treatment_end: trad.age_at_treatment_end
              }) AS treatment_filters
            WITH
              id,
              CASE
                WHEN trad_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters

            UNION

            // treatment_surgery
            WITH sur.guid AS id, p
            OPTIONAL MATCH (tsurg:treatment_surgery)-[:of_treatment_surgery]->(p)
            WITH
              id,
              count(tsurg) AS tsurg_count,
              collect(DISTINCT {
                treatment_type: CASE
                  WHEN tsurg.treatment_type IS NULL OR size(tsurg.treatment_type) = 0 THEN ['surgery']
                  ELSE [x IN tsurg.treatment_type WHERE x IS NOT NULL | trim(x)]
                END,
                treatment_agent: tsurg.surgery_type,
                age_at_treatment_start: tsurg.age_at_treatment_start,
                age_at_treatment_end: tsurg.age_at_treatment_end
              }) AS treatment_filters
            WITH
              id,
              CASE
                WHEN tsurg_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters

            UNION

            // treatment_other
            WITH sur.guid AS id, p
            OPTIONAL MATCH (toth:treatment_other)-[:of_treatment_other]->(p)
            WITH
              id,
              count(toth) AS toth_count,
              collect(DISTINCT {
                treatment_type: CASE
                  WHEN toth.treatment_type IS NULL OR size(toth.treatment_type) = 0 THEN ['other']
                  ELSE [x IN toth.treatment_type WHERE x IS NOT NULL | trim(x)]
                END,
                treatment_agent: toth.other_treatment_type,
                age_at_treatment_start: toth.age_at_treatment_start,
                age_at_treatment_end: toth.age_at_treatment_end
              }) AS treatment_filters
            WITH
              id,
              CASE
                WHEN toth_count > 0
                THEN treatment_filters
                ELSE []
              END AS treatment_filters
            RETURN id, treatment_filters
          }
          WITH
            id,
            collect(treatment_filters) AS treatment_filters_superlist
          WITH
            id,
            reduce(all_filters = [], filters IN treatment_filters_superlist | all_filters + filters) AS treatment_filters
          RETURN id, treatment_filters
        page_size: 5000
      - name: "treatment_response_filters"
        query: |
          MATCH (sur:survival)-[:of_survival]->(p:participant)
          WITH sur.guid AS id, p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (tresp:treatment_response)-[:of_treatment_response]->(p)
          WITH
            id,
            count(tresp) AS treatment_response_count,
            [item IN collect(
              DISTINCT {
                age_at_response: tresp.age_at_response,
                response: tresp.response,
                response_category: tresp.response_category,
                response_system: tresp.response_system
              }
            ) WHERE item IS NOT NULL] AS treatment_response_filters
          WITH
            id,
            CASE
              WHEN treatment_response_count > 0
              THEN treatment_response_filters
              ELSE []
            END AS treatment_response_filters
          RETURN id, treatment_response_filters
        page_size: 5000
      - name: "sample_diagnosis_genetic_analysis_file_filters"
        query: |
          MATCH (sur:survival)-[:of_survival]->(p:participant)
          WITH
            sur.guid AS id,
            p
          SKIP $skip LIMIT $limit

          // Direct participant samples: self-context
          OPTIONAL MATCH (p)<--(s1:sample)
          WITH
            id,
            p,
            collect(DISTINCT { sm: s1, source_sm: s1 }) AS direct_contexts

          // Model-linked samples: p <- Sample_1 <- cell_line/pdx <- Sample_2
          OPTIONAL MATCH (p)<-[:of_sample]-(s1_model:sample)<--(model)<--(s2:sample)
          WHERE model:cell_line OR model:pdx

          WITH
            id,
            p,
            [ctx IN (
              direct_contexts
              + collect(DISTINCT { sm: s1_model, source_sm: s1_model })
              + collect(DISTINCT { sm: s2, source_sm: s2 })
              + collect(DISTINCT { sm: s1_model, source_sm: s2 })
              + collect(DISTINCT { sm: s2, source_sm: s1_model })
            )
            WHERE ctx.sm IS NOT NULL AND ctx.source_sm IS NOT NULL] AS sample_contexts

          WITH
            id,
            p,
            CASE
              WHEN size(sample_contexts) = 0 THEN [{ sm: null, source_sm: null }]
              ELSE sample_contexts
            END AS sample_contexts

          // Study-level files
          OPTIONAL MATCH (st:study)<--(:consent_group)<--(p)
          OPTIONAL MATCH (st)<--(study_file)
          WHERE study_file:clinical_measure_file
            OR study_file:generic_file

          WITH
            id,
            p,
            sample_contexts,
            collect(DISTINCT study_file) AS study_files

          UNWIND sample_contexts AS ctx

          WITH
            id,
            p,
            ctx.sm AS sm,
            ctx.source_sm AS source_sm,
            study_files

          // Source sample-level files
          OPTIONAL MATCH (source_sm)<--(sample_file)
          WHERE sample_file:sequencing_file
            OR sample_file:generic_file
            OR sample_file:methylation_array_file
            OR sample_file:pathology_file
            OR sample_file:cytogenomic_file

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            collect(DISTINCT sample_file) AS sample_files

          // Participant-level files
          OPTIONAL MATCH (p)<--(participant_file)
          WHERE participant_file:clinical_measure_file
            OR participant_file:generic_file
            OR participant_file:radiology_file

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            collect(DISTINCT participant_file) AS participant_files

          // Source sample-level diagnosis
          OPTIONAL MATCH (source_sm)<--(sample_dg:diagnosis)

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            participant_files,
            collect(DISTINCT sample_dg) AS sample_diagnoses

          // Participant-level diagnosis
          OPTIONAL MATCH (p)<--(participant_dg:diagnosis)

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            participant_files,
            sample_diagnoses,
            collect(DISTINCT participant_dg) AS participant_diagnoses

          // Source sample-level genetic analysis
          OPTIONAL MATCH (source_sm)<--(sample_ga:genetic_analysis)

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            participant_files,
            sample_diagnoses,
            participant_diagnoses,
            collect(DISTINCT sample_ga) AS sample_gas

          // Participant-level genetic analysis
          OPTIONAL MATCH (p)<--(participant_ga:genetic_analysis)

          WITH
            id,
            sm,
            sample_files + participant_files + study_files AS files,
            sample_diagnoses + participant_diagnoses AS diagnoses,
            sample_gas + collect(DISTINCT participant_ga) AS gas

          UNWIND CASE WHEN size(files) = 0 THEN [null] ELSE files END AS file
          UNWIND CASE WHEN size(diagnoses) = 0 THEN [null] ELSE diagnoses END AS dg
          UNWIND CASE WHEN size(gas) = 0 THEN [null] ELSE gas END AS ga

          WITH
            id,
            collect(DISTINCT {
              sm: sm,
              file: file,
              dg: dg,
              ga: ga
            }) AS rows

          UNWIND rows AS row
          WITH DISTINCT
            id,

            // Sample facets
            row.sm.anatomic_site AS sample_anatomic_site,
            row.sm.participant_age_at_collection AS participant_age_at_collection,
            row.sm.sample_tumor_status AS sample_tumor_status,
            row.sm.tumor_spatial_extent AS tumor_spatial_extent,

            // Diagnosis facets
            row.dg.age_at_diagnosis AS age_at_diagnosis,
            row.dg.anatomic_site AS diagnosis_anatomic_site,
            row.dg.disease_phase AS disease_phase,
            row.dg.diagnosis_classification_system AS diagnosis_classification_system,
            row.dg.diagnosis_category AS diagnosis_category,
            row.dg.diagnosis_basis AS diagnosis_basis,
            row.dg.diagnosis AS diagnosis,

            // Genetic Analysis facets
            row.ga.reported_significance AS reported_significance,
            row.ga.reported_significance_system AS reported_significance_system,
            row.ga.gene_symbol AS gene_symbol,
            row.ga.alteration AS alteration,
            row.ga.fusion_partner_gene AS fusion_partner_gene,
            row.ga.alteration_type AS alteration_type,
            row.ga.status AS status,

            // File facets
            row.file.data_category AS data_category,
            row.file.file_type AS file_type,
            row.file.file_mapping_level AS file_mapping_level,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_selection
              ELSE null
            END AS library_selection,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_source_material
              ELSE null
            END AS library_source_material,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_source_molecule
              ELSE null
            END AS library_source_molecule,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_strategy
              ELSE null
            END AS library_strategy

          RETURN
            id,
            collect({
              sample_anatomic_site: sample_anatomic_site,
              participant_age_at_collection: participant_age_at_collection,
              sample_tumor_status: sample_tumor_status,
              tumor_spatial_extent: tumor_spatial_extent,
              age_at_diagnosis: age_at_diagnosis,
              diagnosis_anatomic_site: diagnosis_anatomic_site,
              disease_phase: disease_phase,
              diagnosis_classification_system: diagnosis_classification_system,
              diagnosis_category: diagnosis_category,
              diagnosis_basis: diagnosis_basis,
              diagnosis: diagnosis,
              reported_significance: reported_significance,
              reported_significance_system: reported_significance_system,
              gene_symbol: gene_symbol,
              alteration: alteration,
              fusion_partner_gene: fusion_partner_gene,
              alteration_type: alteration_type,
              status: status,
              data_category: data_category,
              file_type: file_type,
              file_mapping_level: file_mapping_level,
              library_selection: library_selection,
              library_source_material: library_source_material,
              library_source_molecule: library_source_molecule,
              library_strategy: library_strategy
            }) AS sample_diagnosis_genetic_analysis_file_filters
        page_size: 1000

  - index_name: treatments_table
    id_field: "id"
    mapping:
      keyword:
        - id
        - treatment_id
        - pid
        - participant_id
        - race
        - race_str
        - sex_at_birth
        - study_id
        - dbgap_accession
        - study_phase
        - study_acronym
        - study_name
        - age_at_treatment_end_str
        - age_at_treatment_start_str
        - treatment_agent
        - treatment_agent_str
        - treatment_type
        - treatment_type_str
        - survival_filters.last_known_survival_status
        - survival_filters.cause_of_death
        - survival_filters.first_event
        - treatment_response_filters.response
        - treatment_response_filters.response_category
        - treatment_response_filters.response_system
        - sample_diagnosis_genetic_analysis_file_filters.sample_anatomic_site
        - sample_diagnosis_genetic_analysis_file_filters.sample_tumor_status
        - sample_diagnosis_genetic_analysis_file_filters.tumor_spatial_extent
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_category
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_anatomic_site
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_classification_system
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_basis
        - sample_diagnosis_genetic_analysis_file_filters.disease_phase
        - sample_diagnosis_genetic_analysis_file_filters.reported_significance
        - sample_diagnosis_genetic_analysis_file_filters.reported_significance_system
        - sample_diagnosis_genetic_analysis_file_filters.gene_symbol
        - sample_diagnosis_genetic_analysis_file_filters.alteration
        - sample_diagnosis_genetic_analysis_file_filters.fusion_partner_gene
        - sample_diagnosis_genetic_analysis_file_filters.alteration_type
        - sample_diagnosis_genetic_analysis_file_filters.status
        - sample_diagnosis_genetic_analysis_file_filters.data_category
        - sample_diagnosis_genetic_analysis_file_filters.file_type
        - sample_diagnosis_genetic_analysis_file_filters.file_mapping_level
        - sample_diagnosis_genetic_analysis_file_filters.library_selection
        - sample_diagnosis_genetic_analysis_file_filters.library_source_material
        - sample_diagnosis_genetic_analysis_file_filters.library_source_molecule
        - sample_diagnosis_genetic_analysis_file_filters.library_strategy
      integer:
        - age_at_treatment_end
        - age_at_treatment_start
        - survival_filters.age_at_last_known_survival_status
        - treatment_response_filters.age_at_response
        - sample_diagnosis_genetic_analysis_file_filters.participant_age_at_collection
        - sample_diagnosis_genetic_analysis_file_filters.age_at_diagnosis
    # Cypher query will be used to retrieve data from Neo4j, and index into Elasticsearch
    initial_query:
      query: |
        CALL {
          MATCH (tchemo:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(:participant)
          RETURN 'treatment_chemotherapy:' + tchemo.guid AS id

          UNION

          MATCH (trad:treatment_radiation)-[:of_treatment_radiation]->(:participant)
          RETURN 'treatment_radiation:' + trad.guid AS id

          UNION

          MATCH (tsurg:treatment_surgery)-[:of_treatment_surgery]->(:participant)
          RETURN 'treatment_surgery:' + tsurg.guid AS id

          UNION

          MATCH (toth:treatment_other)-[:of_treatment_other]->(:participant)
          RETURN 'treatment_other:' + toth.guid AS id
        }
        WITH DISTINCT id
        SKIP $skip LIMIT $limit
        RETURN id
      page_size: 5000
    update_queries:
      - name: "treatment_and_participant_properties"
        query: |
          CALL {
            MATCH (tchemo:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(p:participant)
            WITH
              p,
              tchemo,
              CASE
                WHEN tchemo.chemotherapy_type IS NULL OR size(tchemo.chemotherapy_type) = 0 THEN ['chemotherapy']
                ELSE [item IN tchemo.chemotherapy_type WHERE item IS NOT NULL | trim(item)]
              END AS treatment_type
            RETURN
              'treatment_chemotherapy:' + tchemo.guid AS id,
              tchemo.guid AS treatment_id,
              p,
              tchemo.age_at_treatment_end AS age_at_treatment_end,
              tchemo.age_at_treatment_start AS age_at_treatment_start,
              tchemo.chemotherapy_agent AS treatment_agent,
              reduce(result = '', item IN coalesce(tchemo.chemotherapy_agent, []) |
                CASE
                  WHEN result = '' THEN item
                  ELSE result + ';' + item
                END
              ) AS treatment_agent_str,
              treatment_type,
              reduce(result = '', item IN treatment_type |
                CASE
                  WHEN result = '' THEN item
                  ELSE result + ';' + item
                END
              ) AS treatment_type_str

            UNION

            MATCH (trad:treatment_radiation)-[:of_treatment_radiation]->(p:participant)
            WITH
              p,
              trad,
              CASE
                WHEN trad.treatment_type IS NULL OR size(trad.treatment_type) = 0 THEN ['radiation']
                ELSE [item IN trad.treatment_type WHERE item IS NOT NULL | trim(item)]
              END AS treatment_type
            RETURN
              'treatment_radiation:' + trad.guid AS id,
              trad.guid AS treatment_id,
              p,
              trad.age_at_treatment_end AS age_at_treatment_end,
              trad.age_at_treatment_start AS age_at_treatment_start,
              trad.radiation_therapy_type AS treatment_agent,
              reduce(result = '', item IN coalesce(trad.radiation_therapy_type, []) |
                CASE
                  WHEN result = '' THEN item
                  ELSE result + ';' + item
                END
              ) AS treatment_agent_str,
              treatment_type,
              reduce(result = '', item IN treatment_type |
                CASE
                  WHEN result = '' THEN item
                  ELSE result + ';' + item
                END
              ) AS treatment_type_str

            UNION

            MATCH (tsurg:treatment_surgery)-[:of_treatment_surgery]->(p:participant)
            WITH
              p,
              tsurg,
              CASE
                WHEN tsurg.treatment_type IS NULL OR size(tsurg.treatment_type) = 0 THEN ['surgery']
                ELSE [item IN tsurg.treatment_type WHERE item IS NOT NULL | trim(item)]
              END AS treatment_type
            RETURN
              'treatment_surgery:' + tsurg.guid AS id,
              tsurg.guid AS treatment_id,
              p,
              tsurg.age_at_treatment_end AS age_at_treatment_end,
              tsurg.age_at_treatment_start AS age_at_treatment_start,
              tsurg.surgery_type AS treatment_agent,
              reduce(result = '', item IN coalesce(tsurg.surgery_type, []) |
                CASE
                  WHEN result = '' THEN item
                  ELSE result + ';' + item
                END
              ) AS treatment_agent_str,
              treatment_type,
              reduce(result = '', item IN treatment_type |
                CASE
                  WHEN result = '' THEN item
                  ELSE result + ';' + item
                END
              ) AS treatment_type_str

            UNION

            MATCH (toth:treatment_other)-[:of_treatment_other]->(p:participant)
            WITH
              p,
              toth,
              CASE
                WHEN toth.treatment_type IS NULL OR size(toth.treatment_type) = 0 THEN ['other']
                ELSE [item IN toth.treatment_type WHERE item IS NOT NULL | trim(item)]
              END AS treatment_type
            RETURN
              'treatment_other:' + toth.guid AS id,
              toth.guid AS treatment_id,
              p,
              toth.age_at_treatment_end AS age_at_treatment_end,
              toth.age_at_treatment_start AS age_at_treatment_start,
              toth.other_treatment_type AS treatment_agent,
              reduce(result = '', item IN coalesce(toth.other_treatment_type, []) |
                CASE
                  WHEN result = '' THEN item
                  ELSE result + ';' + item
                END
              ) AS treatment_agent_str,
              treatment_type,
              reduce(result = '', item IN treatment_type |
                CASE
                  WHEN result = '' THEN item
                  ELSE result + ';' + item
                END
              ) AS treatment_type_str
          }
          WITH id, treatment_id, p, age_at_treatment_end, age_at_treatment_start, treatment_agent, treatment_agent_str, treatment_type, treatment_type_str
          SKIP $skip LIMIT $limit
          RETURN
            id,
            treatment_id,
            p.guid AS pid,
            p.participant_id AS participant_id,
            p.race AS race,
            reduce(result = '', x IN p.race |
              CASE
                WHEN result = '' THEN x
                ELSE result + ';' + x
              END
            ) AS race_str,
            p.sex_at_birth AS sex_at_birth,
            age_at_treatment_end AS age_at_treatment_end,
            CASE age_at_treatment_end
              WHEN -999 THEN 'Not Reported'
              ELSE toString(age_at_treatment_end)
            END AS age_at_treatment_end_str,
            age_at_treatment_start AS age_at_treatment_start,
            CASE age_at_treatment_start
              WHEN -999 THEN 'Not Reported'
              ELSE toString(age_at_treatment_start)
            END AS age_at_treatment_start_str,
            treatment_agent,
            treatment_agent_str,
            treatment_type,
            treatment_type_str
        page_size: 1000
      - name: "study_properties"
        query: |
          CALL {
            MATCH (tchemo:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(p:participant)
            RETURN 'treatment_chemotherapy:' + tchemo.guid AS id, p

            UNION

            MATCH (trad:treatment_radiation)-[:of_treatment_radiation]->(p:participant)
            RETURN 'treatment_radiation:' + trad.guid AS id, p

            UNION

            MATCH (tsurg:treatment_surgery)-[:of_treatment_surgery]->(p:participant)
            RETURN 'treatment_surgery:' + tsurg.guid AS id, p

            UNION

            MATCH (toth:treatment_other)-[:of_treatment_other]->(p:participant)
            RETURN 'treatment_other:' + toth.guid AS id, p
          }
          MATCH
            (p)-[:of_participant]->(cg:consent_group),
            (cg)-[:of_consent_group]->(stu:study)
          WITH id, stu
          SKIP $skip LIMIT $limit
          RETURN
            id,
            stu.study_id AS study_id,
            stu.dbgap_accession AS dbgap_accession,
            stu.study_phase AS study_phase,
            stu.study_acronym AS study_acronym,
            stu.study_name AS study_name
        page_size: 1000
      - name: "survival_filters"
        query: |
          CALL {
            MATCH (tchemo:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(p:participant)
            RETURN 'treatment_chemotherapy:' + tchemo.guid AS id, p

            UNION

            MATCH (trad:treatment_radiation)-[:of_treatment_radiation]->(p:participant)
            RETURN 'treatment_radiation:' + trad.guid AS id, p

            UNION

            MATCH (tsurg:treatment_surgery)-[:of_treatment_surgery]->(p:participant)
            RETURN 'treatment_surgery:' + tsurg.guid AS id, p

            UNION

            MATCH (toth:treatment_other)-[:of_treatment_other]->(p:participant)
            RETURN 'treatment_other:' + toth.guid AS id, p
          }
          WITH id, p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sur:survival)-[:of_survival]->(p)
          WITH
            id,
            count(sur) AS survival_count,
            [item IN collect(
              DISTINCT {
                last_known_survival_status: sur.last_known_survival_status,
                cause_of_death: sur.cause_of_death,
                first_event: sur.first_event,
                age_at_last_known_survival_status: sur.age_at_last_known_survival_status
              }
            ) WHERE item IS NOT NULL] AS survival_filters
          WITH
            id,
            CASE
              WHEN survival_count > 0 THEN survival_filters
              ELSE []
            END AS survival_filters
          RETURN id, survival_filters
        page_size: 1000
      - name: "treatment_response_filters"
        query: |
          CALL {
            MATCH (tchemo:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(p:participant)
            RETURN 'treatment_chemotherapy:' + tchemo.guid AS id, p

            UNION

            MATCH (trad:treatment_radiation)-[:of_treatment_radiation]->(p:participant)
            RETURN 'treatment_radiation:' + trad.guid AS id, p

            UNION

            MATCH (tsurg:treatment_surgery)-[:of_treatment_surgery]->(p:participant)
            RETURN 'treatment_surgery:' + tsurg.guid AS id, p

            UNION

            MATCH (toth:treatment_other)-[:of_treatment_other]->(p:participant)
            RETURN 'treatment_other:' + toth.guid AS id, p
          }
          WITH id, p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (tresp:treatment_response)-[:of_treatment_response]->(p)
          WITH
            id,
            count(tresp) AS treatment_response_count,
            [item IN collect(
              DISTINCT {
                age_at_response: tresp.age_at_response,
                response: tresp.response,
                response_category: tresp.response_category,
                response_system: tresp.response_system
              }
            ) WHERE item IS NOT NULL] AS treatment_response_filters
          WITH
            id,
            CASE
              WHEN treatment_response_count > 0 THEN treatment_response_filters
              ELSE []
            END AS treatment_response_filters
          RETURN id, treatment_response_filters
        page_size: 1000
      - name: "sample_diagnosis_genetic_analysis_file_filters"
        query: |
          CALL {
            MATCH (tchemo:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(p:participant)
            RETURN 'treatment_chemotherapy:' + tchemo.guid AS id, p

            UNION

            MATCH (trad:treatment_radiation)-[:of_treatment_radiation]->(p:participant)
            RETURN 'treatment_radiation:' + trad.guid AS id, p

            UNION

            MATCH (tsurg:treatment_surgery)-[:of_treatment_surgery]->(p:participant)
            RETURN 'treatment_surgery:' + tsurg.guid AS id, p

            UNION

            MATCH (toth:treatment_other)-[:of_treatment_other]->(p:participant)
            RETURN 'treatment_other:' + toth.guid AS id, p
          }
          WITH id, p
          SKIP $skip LIMIT $limit

          // Direct participant samples: self-context
          OPTIONAL MATCH (p)<--(s1:sample)
          WITH
            id,
            p,
            collect(DISTINCT { sm: s1, source_sm: s1 }) AS direct_contexts

          // Model-linked samples: p <- Sample_1 <- cell_line/pdx <- Sample_2
          OPTIONAL MATCH (p)<-[:of_sample]-(s1_model:sample)<--(model)<--(s2:sample)
          WHERE model:cell_line OR model:pdx

          WITH
            id,
            p,
            [ctx IN (
              direct_contexts
              + collect(DISTINCT { sm: s1_model, source_sm: s1_model })
              + collect(DISTINCT { sm: s2, source_sm: s2 })
              + collect(DISTINCT { sm: s1_model, source_sm: s2 })
              + collect(DISTINCT { sm: s2, source_sm: s1_model })
            )
            WHERE ctx.sm IS NOT NULL AND ctx.source_sm IS NOT NULL] AS sample_contexts

          WITH
            id,
            p,
            CASE
              WHEN size(sample_contexts) = 0 THEN [{ sm: null, source_sm: null }]
              ELSE sample_contexts
            END AS sample_contexts

          // Study-level files
          OPTIONAL MATCH (st:study)<--(:consent_group)<--(p)
          OPTIONAL MATCH (st)<--(study_file)
          WHERE study_file:clinical_measure_file
            OR study_file:generic_file

          WITH
            id,
            p,
            sample_contexts,
            collect(DISTINCT study_file) AS study_files

          UNWIND sample_contexts AS ctx

          WITH
            id,
            p,
            ctx.sm AS sm,
            ctx.source_sm AS source_sm,
            study_files

          // Source sample-level files
          OPTIONAL MATCH (source_sm)<--(sample_file)
          WHERE sample_file:sequencing_file
            OR sample_file:generic_file
            OR sample_file:methylation_array_file
            OR sample_file:pathology_file
            OR sample_file:cytogenomic_file

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            collect(DISTINCT sample_file) AS sample_files

          // Participant-level files
          OPTIONAL MATCH (p)<--(participant_file)
          WHERE participant_file:clinical_measure_file
            OR participant_file:generic_file
            OR participant_file:radiology_file

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            collect(DISTINCT participant_file) AS participant_files

          // Source sample-level diagnosis
          OPTIONAL MATCH (source_sm)<--(sample_dg:diagnosis)

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            participant_files,
            collect(DISTINCT sample_dg) AS sample_diagnoses

          // Participant-level diagnosis
          OPTIONAL MATCH (p)<--(participant_dg:diagnosis)

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            participant_files,
            sample_diagnoses,
            collect(DISTINCT participant_dg) AS participant_diagnoses

          // Source sample-level genetic analysis
          OPTIONAL MATCH (source_sm)<--(sample_ga:genetic_analysis)

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            participant_files,
            sample_diagnoses,
            participant_diagnoses,
            collect(DISTINCT sample_ga) AS sample_gas

          // Participant-level genetic analysis
          OPTIONAL MATCH (p)<--(participant_ga:genetic_analysis)

          WITH
            id,
            sm,
            sample_files + participant_files + study_files AS files,
            sample_diagnoses + participant_diagnoses AS diagnoses,
            sample_gas + collect(DISTINCT participant_ga) AS gas

          UNWIND CASE WHEN size(files) = 0 THEN [null] ELSE files END AS file
          UNWIND CASE WHEN size(diagnoses) = 0 THEN [null] ELSE diagnoses END AS dg
          UNWIND CASE WHEN size(gas) = 0 THEN [null] ELSE gas END AS ga

          WITH
            id,
            collect(DISTINCT {
              sm: sm,
              file: file,
              dg: dg,
              ga: ga
            }) AS rows

          UNWIND rows AS row
          WITH DISTINCT
            id,

            // Sample facets
            row.sm.anatomic_site AS sample_anatomic_site,
            row.sm.participant_age_at_collection AS participant_age_at_collection,
            row.sm.sample_tumor_status AS sample_tumor_status,
            row.sm.tumor_spatial_extent AS tumor_spatial_extent,

            // Diagnosis facets
            row.dg.age_at_diagnosis AS age_at_diagnosis,
            row.dg.anatomic_site AS diagnosis_anatomic_site,
            row.dg.disease_phase AS disease_phase,
            row.dg.diagnosis_classification_system AS diagnosis_classification_system,
            row.dg.diagnosis_category AS diagnosis_category,
            row.dg.diagnosis_basis AS diagnosis_basis,
            row.dg.diagnosis AS diagnosis,

            // Genetic Analysis facets
            row.ga.reported_significance AS reported_significance,
            row.ga.reported_significance_system AS reported_significance_system,
            row.ga.gene_symbol AS gene_symbol,
            row.ga.alteration AS alteration,
            row.ga.fusion_partner_gene AS fusion_partner_gene,
            row.ga.alteration_type AS alteration_type,
            row.ga.status AS status,

            // File facets
            row.file.data_category AS data_category,
            row.file.file_type AS file_type,
            row.file.file_mapping_level AS file_mapping_level,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_selection
              ELSE null
            END AS library_selection,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_source_material
              ELSE null
            END AS library_source_material,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_source_molecule
              ELSE null
            END AS library_source_molecule,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_strategy
              ELSE null
            END AS library_strategy
          RETURN
            id,
            collect({
              sample_anatomic_site: sample_anatomic_site,
              participant_age_at_collection: participant_age_at_collection,
              sample_tumor_status: sample_tumor_status,
              tumor_spatial_extent: tumor_spatial_extent,
              age_at_diagnosis: age_at_diagnosis,
              diagnosis_anatomic_site: diagnosis_anatomic_site,
              disease_phase: disease_phase,
              diagnosis_classification_system: diagnosis_classification_system,
              diagnosis_category: diagnosis_category,
              diagnosis_basis: diagnosis_basis,
              diagnosis: diagnosis,
              reported_significance: reported_significance,
              reported_significance_system: reported_significance_system,
              gene_symbol: gene_symbol,
              alteration: alteration,
              fusion_partner_gene: fusion_partner_gene,
              alteration_type: alteration_type,
              status: status,
              data_category: data_category,
              file_type: file_type,
              file_mapping_level: file_mapping_level,
              library_selection: library_selection,
              library_source_material: library_source_material,
              library_source_molecule: library_source_molecule,
              library_strategy: library_strategy
            }) AS sample_diagnosis_genetic_analysis_file_filters
        page_size: 1000

  - index_name: treatment_responses_table
    id_field: "id"
    mapping:
      keyword:
        - id
        - treatment_response_id
        - pid
        - participant_id
        - race
        - race_str
        - sex_at_birth
        - study_id
        - dbgap_accession
        - study_phase
        - study_acronym
        - study_name
        - age_at_response_str
        - response
        - response_category
        - response_system
        - survival_filters.last_known_survival_status
        - survival_filters.cause_of_death
        - survival_filters.first_event
        - treatment_filters.treatment_type
        - treatment_filters.treatment_agent
        - sample_diagnosis_genetic_analysis_file_filters.sample_anatomic_site
        - sample_diagnosis_genetic_analysis_file_filters.sample_tumor_status
        - sample_diagnosis_genetic_analysis_file_filters.tumor_spatial_extent
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_category
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_anatomic_site
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_classification_system
        - sample_diagnosis_genetic_analysis_file_filters.diagnosis_basis
        - sample_diagnosis_genetic_analysis_file_filters.disease_phase
        - sample_diagnosis_genetic_analysis_file_filters.reported_significance
        - sample_diagnosis_genetic_analysis_file_filters.reported_significance_system
        - sample_diagnosis_genetic_analysis_file_filters.gene_symbol
        - sample_diagnosis_genetic_analysis_file_filters.alteration
        - sample_diagnosis_genetic_analysis_file_filters.fusion_partner_gene
        - sample_diagnosis_genetic_analysis_file_filters.alteration_type
        - sample_diagnosis_genetic_analysis_file_filters.status
        - sample_diagnosis_genetic_analysis_file_filters.data_category
        - sample_diagnosis_genetic_analysis_file_filters.file_type
        - sample_diagnosis_genetic_analysis_file_filters.file_mapping_level
        - sample_diagnosis_genetic_analysis_file_filters.library_selection
        - sample_diagnosis_genetic_analysis_file_filters.library_source_material
        - sample_diagnosis_genetic_analysis_file_filters.library_source_molecule
        - sample_diagnosis_genetic_analysis_file_filters.library_strategy
      integer:
        - age_at_response
        - survival_filters.age_at_last_known_survival_status
        - treatment_filters.age_at_treatment_start
        - treatment_filters.age_at_treatment_end
        - sample_diagnosis_genetic_analysis_file_filters.participant_age_at_collection
        - sample_diagnosis_genetic_analysis_file_filters.age_at_diagnosis
    # Cypher query will be used to retrieve data from Neo4j, and index into Elasticsearch
    initial_query:
      query: |
        MATCH (tresp:treatment_response)
        WITH tresp
        SKIP $skip LIMIT $limit
        RETURN tresp.guid AS id
      page_size: 5000
    update_queries:
      - name: "tresp_and_participant_properties"
        query: |
          MATCH (tresp:treatment_response)-[:of_treatment_response]->(p:participant)
          WITH tresp, p
          SKIP $skip LIMIT $limit
          RETURN
            tresp.guid AS id,
            tresp.treatment_response_id AS treatment_response_id,
            p.guid AS pid,
            p.participant_id AS participant_id,
            p.race AS race,
            reduce(result = '', x IN p.race |
              CASE
                WHEN result = '' THEN x
                ELSE result + ';' + x
              END
            ) AS race_str,
            p.sex_at_birth AS sex_at_birth,
            tresp.age_at_response AS age_at_response,
            CASE tresp.age_at_response
              WHEN -999 THEN 'Not Reported'
              ELSE toString(tresp.age_at_response)
            END AS age_at_response_str,
            tresp.response AS response,
            tresp.response_category AS response_category,
            tresp.response_system AS response_system
        page_size: 1000
      - name: "study_properties"
        query: |
          MATCH (tresp:treatment_response)-[:of_treatment_response]->(p:participant)
          WITH tresp, p
          SKIP $skip LIMIT $limit
          MATCH
            (p)-[:of_participant]->(cg:consent_group),
            (cg)-[:of_consent_group]->(stu:study)
          WITH tresp.guid AS id, stu
          RETURN
            id,
            stu.study_id AS study_id,
            stu.dbgap_accession AS dbgap_accession,
            stu.study_phase AS study_phase,
            stu.study_acronym AS study_acronym,
            stu.study_name AS study_name
        page_size: 1000
      - name: "survival_filters"
        query: |
          MATCH (tresp:treatment_response)-[:of_treatment_response]->(p:participant)
          WITH tresp, p
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sur:survival)-[:of_survival]->(p)
          WITH
            tresp.guid AS id,
            count(sur) AS survival_count,
            [item IN collect(
              DISTINCT {
                last_known_survival_status: sur.last_known_survival_status,
                cause_of_death: sur.cause_of_death,
                first_event: sur.first_event,
                age_at_last_known_survival_status: sur.age_at_last_known_survival_status
              }
            ) WHERE item IS NOT NULL] AS survival_filters
          WITH
            id,
            CASE
              WHEN survival_count > 0 THEN survival_filters
              ELSE []
            END AS survival_filters
          RETURN id, survival_filters
        page_size: 1000
      - name: "treatment_filters"
        query: |
          MATCH (tresp:treatment_response)-[:of_treatment_response]->(p:participant)
          WITH tresp, p
          SKIP $skip LIMIT $limit
          CALL {
            WITH p
            OPTIONAL MATCH (tchemo:treatment_chemotherapy)-[:of_treatment_chemotherapy]->(p)
            WITH
              count(tchemo) AS n_tchemo,
              collect(DISTINCT {
                treatment_type: CASE
                  WHEN tchemo.chemotherapy_type IS NULL OR size(tchemo.chemotherapy_type) = 0 THEN ['chemotherapy']
                  ELSE [x IN tchemo.chemotherapy_type WHERE x IS NOT NULL | trim(x)]
                END,
                treatment_agent: tchemo.chemotherapy_agent,
                age_at_treatment_start: tchemo.age_at_treatment_start,
                age_at_treatment_end: tchemo.age_at_treatment_end
              }) AS collected_filters
            RETURN
              CASE
                WHEN n_tchemo = 0 THEN []
                ELSE collected_filters
              END AS chemo_filters
          }
          CALL {
            WITH p
            OPTIONAL MATCH (trad:treatment_radiation)-[:of_treatment_radiation]->(p)
            WITH
              count(trad) AS n_trad,
              collect(DISTINCT {
                treatment_type: CASE
                  WHEN trad.treatment_type IS NULL OR size(trad.treatment_type) = 0 THEN ['radiation']
                  ELSE [x IN trad.treatment_type WHERE x IS NOT NULL | trim(x)]
                END,
                treatment_agent: trad.radiation_therapy_type,
                age_at_treatment_start: trad.age_at_treatment_start,
                age_at_treatment_end: trad.age_at_treatment_end
              }) AS collected_filters
            RETURN
              CASE
                WHEN n_trad = 0 THEN []
                ELSE collected_filters
              END AS rad_filters
          }
          CALL {
            WITH p
            OPTIONAL MATCH (tsurg:treatment_surgery)-[:of_treatment_surgery]->(p)
            WITH
              count(tsurg) AS n_tsurg,
              collect(DISTINCT {
                treatment_type: CASE
                  WHEN tsurg.treatment_type IS NULL OR size(tsurg.treatment_type) = 0 THEN ['surgery']
                  ELSE [x IN tsurg.treatment_type WHERE x IS NOT NULL | trim(x)]
                END,
                treatment_agent: tsurg.surgery_type,
                age_at_treatment_start: tsurg.age_at_treatment_start,
                age_at_treatment_end: tsurg.age_at_treatment_end
              }) AS collected_filters
            RETURN
              CASE
                WHEN n_tsurg = 0 THEN []
                ELSE collected_filters
              END AS surg_filters
          }
          CALL {
            WITH p
            OPTIONAL MATCH (toth:treatment_other)-[:of_treatment_other]->(p)
            WITH
              count(toth) AS n_toth,
              collect(DISTINCT {
                treatment_type: CASE
                  WHEN toth.treatment_type IS NULL OR size(toth.treatment_type) = 0 THEN ['other']
                  ELSE [x IN toth.treatment_type WHERE x IS NOT NULL | trim(x)]
                END,
                treatment_agent: toth.other_treatment_type,
                age_at_treatment_start: toth.age_at_treatment_start,
                age_at_treatment_end: toth.age_at_treatment_end
              }) AS collected_filters
            RETURN
              CASE
                WHEN n_toth = 0 THEN []
                ELSE collected_filters
              END AS other_filters
          }
          WITH
            tresp.guid AS id,
            chemo_filters + rad_filters + surg_filters + other_filters AS treatment_filters
          RETURN id, treatment_filters
        page_size: 1000
      - name: "sample_diagnosis_genetic_analysis_file_filters"
        query: |
          MATCH (tresp:treatment_response)-[:of_treatment_response]->(p:participant)
          WITH tresp.guid AS id, p
          SKIP $skip LIMIT $limit

          // Direct participant samples: self-context
          OPTIONAL MATCH (p)<--(s1:sample)
          WITH
            id,
            p,
            collect(DISTINCT { sm: s1, source_sm: s1 }) AS direct_contexts

          // Model-linked samples: p <- Sample_1 <- cell_line/pdx <- Sample_2
          OPTIONAL MATCH (p)<-[:of_sample]-(s1_model:sample)<--(model)<--(s2:sample)
          WHERE model:cell_line OR model:pdx

          WITH
            id,
            p,
            [ctx IN (
              direct_contexts
              + collect(DISTINCT { sm: s1_model, source_sm: s1_model })
              + collect(DISTINCT { sm: s2, source_sm: s2 })
              + collect(DISTINCT { sm: s1_model, source_sm: s2 })
              + collect(DISTINCT { sm: s2, source_sm: s1_model })
            )
            WHERE ctx.sm IS NOT NULL AND ctx.source_sm IS NOT NULL] AS sample_contexts

          WITH
            id,
            p,
            CASE
              WHEN size(sample_contexts) = 0 THEN [{ sm: null, source_sm: null }]
              ELSE sample_contexts
            END AS sample_contexts

          // Study-level files
          OPTIONAL MATCH (st:study)<--(:consent_group)<--(p)
          OPTIONAL MATCH (st)<--(study_file)
          WHERE study_file:clinical_measure_file
          OR study_file:generic_file

          WITH
            id,
            p,
            sample_contexts,
            collect(DISTINCT study_file) AS study_files

          UNWIND sample_contexts AS ctx

          WITH
            id,
            p,
            ctx.sm AS sm,
            ctx.source_sm AS source_sm,
            study_files

          // Source sample-level files
          OPTIONAL MATCH (source_sm)<--(sample_file)
          WHERE sample_file:sequencing_file
          OR sample_file:generic_file
          OR sample_file:methylation_array_file
          OR sample_file:pathology_file
          OR sample_file:cytogenomic_file

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            collect(DISTINCT sample_file) AS sample_files

          // Participant-level files
          OPTIONAL MATCH (p)<--(participant_file)
          WHERE participant_file:clinical_measure_file
          OR participant_file:generic_file
          OR participant_file:radiology_file

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            collect(DISTINCT participant_file) AS participant_files

          // Source sample-level diagnosis
          OPTIONAL MATCH (source_sm)<--(sample_dg:diagnosis)

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            participant_files,
            collect(DISTINCT sample_dg) AS sample_diagnoses

          // Participant-level diagnosis
          OPTIONAL MATCH (p)<--(participant_dg:diagnosis)

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            participant_files,
            sample_diagnoses,
            collect(DISTINCT participant_dg) AS participant_diagnoses

          // Source sample-level genetic analysis
          OPTIONAL MATCH (source_sm)<--(sample_ga:genetic_analysis)

          WITH
            id,
            p,
            sm,
            source_sm,
            study_files,
            sample_files,
            participant_files,
            sample_diagnoses,
            participant_diagnoses,
            collect(DISTINCT sample_ga) AS sample_gas

          // Participant-level genetic analysis
          OPTIONAL MATCH (p)<--(participant_ga:genetic_analysis)

          WITH
            id,
            sm,
            sample_files + participant_files + study_files AS files,
            sample_diagnoses + participant_diagnoses AS diagnoses,
            sample_gas + collect(DISTINCT participant_ga) AS gas

          UNWIND CASE WHEN size(files) = 0 THEN [null] ELSE files END AS file
          UNWIND CASE WHEN size(diagnoses) = 0 THEN [null] ELSE diagnoses END AS dg
          UNWIND CASE WHEN size(gas) = 0 THEN [null] ELSE gas END AS ga

          WITH
            id,
            collect(DISTINCT {
              sm: sm,
              file: file,
              dg: dg,
              ga: ga
            }) AS rows

          UNWIND rows AS row
          WITH DISTINCT
            id,

          // Sample facets
            row.sm.anatomic_site AS sample_anatomic_site,
            row.sm.participant_age_at_collection AS participant_age_at_collection,
            row.sm.sample_tumor_status AS sample_tumor_status,
            row.sm.tumor_spatial_extent AS tumor_spatial_extent,

          // Diagnosis facets
            row.dg.age_at_diagnosis AS age_at_diagnosis,
            row.dg.anatomic_site AS diagnosis_anatomic_site,
            row.dg.disease_phase AS disease_phase,
            row.dg.diagnosis_classification_system AS diagnosis_classification_system,
            row.dg.diagnosis_category AS diagnosis_category,
            row.dg.diagnosis_basis AS diagnosis_basis,
            row.dg.diagnosis AS diagnosis,

          // Genetic Analysis facets
            row.ga.reported_significance AS reported_significance,
            row.ga.reported_significance_system AS reported_significance_system,
            row.ga.gene_symbol AS gene_symbol,
            row.ga.alteration AS alteration,
            row.ga.fusion_partner_gene AS fusion_partner_gene,
            row.ga.alteration_type AS alteration_type,
            row.ga.status AS status,

          // File facets
            row.file.data_category AS data_category,
            row.file.file_type AS file_type,
            row.file.file_mapping_level AS file_mapping_level,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_selection
              ELSE null
            END AS library_selection,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_source_material
              ELSE null
            END AS library_source_material,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_source_molecule
              ELSE null
            END AS library_source_molecule,
            CASE
              WHEN row.file IS NOT NULL
              AND size(labels(row.file)) > 0
              AND labels(row.file)[0] = 'sequencing_file'
              THEN row.file.library_strategy
              ELSE null
            END AS library_strategy
          RETURN
            id,
            collect({
              sample_anatomic_site: sample_anatomic_site,
              participant_age_at_collection: participant_age_at_collection,
              sample_tumor_status: sample_tumor_status,
              tumor_spatial_extent: tumor_spatial_extent,
              age_at_diagnosis: age_at_diagnosis,
              diagnosis_anatomic_site: diagnosis_anatomic_site,
              disease_phase: disease_phase,
              diagnosis_classification_system: diagnosis_classification_system,
              diagnosis_category: diagnosis_category,
              diagnosis_basis: diagnosis_basis,
              diagnosis: diagnosis,
              reported_significance: reported_significance,
              reported_significance_system: reported_significance_system,
              gene_symbol: gene_symbol,
              alteration: alteration,
              fusion_partner_gene: fusion_partner_gene,
              alteration_type: alteration_type,
              status: status,
              data_category: data_category,
              file_type: file_type,
              file_mapping_level: file_mapping_level,
              library_selection: library_selection,
              library_source_material: library_source_material,
              library_source_molecule: library_source_molecule,
              library_strategy: library_strategy
            }) AS sample_diagnosis_genetic_analysis_file_filters
        page_size: 1000

  - index_name: diagnoses_table
    id_field: "id"
    mapping:
      keyword:
        - id
        - diagnosis_id
        - pid
        - participant_id
        - race
        - sex_at_birth
        - dbgap_accession
        - study_phase
        - study_id
        - study_acronym
        - study_name
        - diagnosis
        - diagnosis_category
        - diagnosis_category_str
        - diagnosis_anatomic_site
        - diagnosis_anatomic_site_str
        - diagnosis_classification_system
        - diagnosis_classification_system_str
        - diagnosis_basis
        - diagnosis_basis_str
        - diagnosis_comment
        - disease_phase
        - tumor_spatial_extent
        - toronto_childhood_cancer_staging
        - tumor_grade
        - tumor_stage_clinical_m
        - tumor_stage_clinical_n
        - tumor_stage_clinical_t
        - survival_filters.last_known_survival_status
        - survival_filters.cause_of_death
        - survival_filters.first_event
        - treatment_filters.treatment_type
        - treatment_filters.treatment_agent
        - treatment_response_filters.response
        - treatment_response_filters.response_category
        - treatment_response_filters.response_system
        - sample_genetic_analysis_file_filters.sample_anatomic_site
        - sample_genetic_analysis_file_filters.sample_tumor_status
        - sample_genetic_analysis_file_filters.tumor_spatial_extent
        - sample_genetic_analysis_file_filters.reported_significance
        - sample_genetic_analysis_file_filters.reported_significance_system
        - sample_genetic_analysis_file_filters.gene_symbol
        - sample_genetic_analysis_file_filters.alteration
        - sample_genetic_analysis_file_filters.fusion_partner_gene
        - sample_genetic_analysis_file_filters.alteration_type
        - sample_genetic_analysis_file_filters.status
        - sample_genetic_analysis_file_filters.data_category
        - sample_genetic_analysis_file_filters.file_type
        - sample_genetic_analysis_file_filters.file_mapping_level
        - sample_genetic_analysis_file_filters.library_selection
        - sample_genetic_analysis_file_filters.library_source_material
        - sample_genetic_analysis_file_filters.library_source_molecule
        - sample_genetic_analysis_file_filters.library_strategy
      integer:
        - age_at_diagnosis
        - survival_filters.age_at_last_known_survival_status
        - treatment_filters.age_at_treatment_start
        - treatment_filters.age_at_treatment_end
        - treatment_response_filters.age_at_response
        - sample_genetic_analysis_file_filters.participant_age_at_collection
    # Cypher query will be used to retrieve data from Neo4j, and index into Elasticsearch
    initial_query:
      query: |
        MATCH (dg:diagnosis)
        WITH dg
        SKIP $skip LIMIT $limit
        RETURN dg.guid AS id
      page_size: 1000
    update_queries:
      - name: "diagnosis_properties"
        query: |
          MATCH (dg:diagnosis)
          WITH dg
          SKIP $skip LIMIT $limit
          RETURN
            dg.guid AS id,
            dg.diagnosis_id AS diagnosis_id,
            dg.diagnosis AS diagnosis,
            dg.diagnosis_category AS diagnosis_category,
            reduce(
              c = '',
              category IN coalesce(dg.diagnosis_category, []) |
                CASE
                  WHEN c = '' THEN category
                  ELSE c + ';' + category
                END
            ) AS diagnosis_category_str,
            dg.age_at_diagnosis AS age_at_diagnosis,
            dg.anatomic_site AS diagnosis_anatomic_site,
            reduce(
              s = '',
              site IN coalesce(dg.anatomic_site, []) |
                CASE
                  WHEN s = '' THEN site
                  ELSE s + ';' + site
                END
            ) AS diagnosis_anatomic_site_str,
            dg.diagnosis_classification_system AS diagnosis_classification_system,
            reduce(
              s = '',
              system IN coalesce(dg.diagnosis_classification_system, []) |
                CASE
                  WHEN s = '' THEN system
                  ELSE s + ';' + system
                END
            ) AS diagnosis_classification_system_str,
            dg.diagnosis_basis AS diagnosis_basis,
            reduce(
              b = '',
              basis IN coalesce(dg.diagnosis_basis, []) |
                CASE
                  WHEN b = '' THEN basis
                  ELSE b + ';' + basis
                END
            ) AS diagnosis_basis_str,
            dg.diagnosis_comment AS diagnosis_comment,
            dg.disease_phase AS disease_phase,
            dg.tumor_spatial_extent AS tumor_spatial_extent,
            dg.toronto_childhood_cancer_staging AS toronto_childhood_cancer_staging,
            dg.tumor_grade AS tumor_grade,
            dg.tumor_stage_clinical_m AS tumor_stage_clinical_m,
            dg.tumor_stage_clinical_n AS tumor_stage_clinical_n,
            dg.tumor_stage_clinical_t AS tumor_stage_clinical_t
        page_size: 1000
      - name: "participant_properties"
        query: |
          MATCH (dg:diagnosis)
          WITH dg
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (dg)-[*..4]->(p:participant)
          RETURN
            dg.guid AS id,
            p.guid AS pid,
            p.participant_id AS participant_id,
            p.race AS race,
            p.sex_at_birth AS sex_at_birth
        page_size: 1000
      - name: "study_properties"
        query: |
          MATCH (dg:diagnosis)
          WITH dg
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (dg)-[*..7]->(st:study)
          RETURN
            dg.guid AS id,
            st.study_id AS study_id,
            st.dbgap_accession AS dbgap_accession,
            st.study_phase AS study_phase,
            st.study_acronym AS study_acronym,
            st.study_name AS study_name
        page_size: 1000
      - name: "survival_filters"
        query: |
          MATCH (dg:diagnosis)
          WITH dg
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (dg)-[*..4]->(:participant)<-[:of_survival]-(su:survival)
          WITH
            dg.guid AS id,
            count(su) AS su_count,
            [item IN collect(DISTINCT {
              last_known_survival_status: su.last_known_survival_status,
              cause_of_death: su.cause_of_death,
              first_event: su.first_event,
              age_at_last_known_survival_status: su.age_at_last_known_survival_status
            }) WHERE item.last_known_survival_status IS NOT NULL OR item.cause_of_death IS NOT NULL OR item.first_event IS NOT NULL OR item.age_at_last_known_survival_status IS NOT NULL] AS survival_filters
          WITH id, CASE WHEN su_count > 0 THEN survival_filters ELSE [] END AS survival_filters
          RETURN id, survival_filters
        page_size: 1000
      - name: "treatment_filters"
        query: |
          MATCH (dg:diagnosis)
          WITH dg
          SKIP $skip LIMIT $limit

          OPTIONAL MATCH (dg)-[*..4]->(p:participant)

          OPTIONAL MATCH (p)<-[:of_treatment_chemotherapy]-(tchemo:treatment_chemotherapy)
          WITH dg, p, COLLECT(DISTINCT {
            treatment_type: tchemo.chemotherapy_type,
            treatment_agent: tchemo.chemotherapy_agent,
            age_at_treatment_start: tchemo.age_at_treatment_start,
            age_at_treatment_end: tchemo.age_at_treatment_end
          }) AS chemo_filters

          OPTIONAL MATCH (p)<-[:of_treatment_radiation]-(trad:treatment_radiation)
          WITH dg, p, chemo_filters, COLLECT(DISTINCT {
            treatment_type: trad.treatment_type,
            treatment_agent: trad.radiation_therapy_type,
            age_at_treatment_start: trad.age_at_treatment_start,
            age_at_treatment_end: trad.age_at_treatment_end
          }) AS radiation_filters

          OPTIONAL MATCH (p)<-[:of_treatment_surgery]-(tsurg:treatment_surgery)
          WITH dg, p, chemo_filters, radiation_filters, COLLECT(DISTINCT {
            treatment_type: tsurg.treatment_type,
            treatment_agent: tsurg.surgery_type,
            age_at_treatment_start: tsurg.age_at_treatment_start,
            age_at_treatment_end: tsurg.age_at_treatment_end
          }) AS surgery_filters

          OPTIONAL MATCH (p)<-[:of_treatment_other]-(toth:treatment_other)
          WITH dg,
              chemo_filters + radiation_filters + surgery_filters + COLLECT(DISTINCT {
                treatment_type: toth.treatment_type,
                treatment_agent: toth.other_treatment_type,
                age_at_treatment_start: toth.age_at_treatment_start,
                age_at_treatment_end: toth.age_at_treatment_end
              }) AS raw_treatment_filters

          UNWIND raw_treatment_filters AS f

          WITH dg, COLLECT(DISTINCT f) AS deduped_treatment_filters

          WITH dg.guid AS id,
              [
                f IN deduped_treatment_filters
                WHERE f.treatment_type IS NOT NULL
                    OR f.treatment_agent IS NOT NULL
                    OR f.age_at_treatment_start IS NOT NULL
                    OR f.age_at_treatment_end IS NOT NULL
              ] AS treatment_filters

          RETURN id, treatment_filters
        page_size: 1000
      - name: "treatment_response_filters"
        query: |
          MATCH (dg:diagnosis)
          WITH dg
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (dg)-[*..4]->(:participant)<-[:of_treatment_response]-(tresp:treatment_response)
          WITH
            dg.guid AS id,
            count(tresp) AS tresp_count,
            [item IN collect(DISTINCT {
              age_at_response: tresp.age_at_response,
              response: tresp.response,
              response_category: tresp.response_category,
              response_system: tresp.response_system
            }) WHERE item.response IS NOT NULL OR item.age_at_response IS NOT NULL OR item.response_category IS NOT NULL OR item.response_system IS NOT NULL] AS treatment_response_filters
          WITH id, CASE WHEN tresp_count > 0 THEN treatment_response_filters ELSE [] END AS treatment_response_filters
          RETURN id, treatment_response_filters
        page_size: 1000
      - name: "sample_genetic_analysis_file_filters"
        query: |
          MATCH (dg:diagnosis)
          WITH dg
          SKIP $skip LIMIT $limit

          CALL {
            WITH dg

            // Case 1: dg -> sample_1 -> participant
            // Also include related sample_2 -> model -> sample_1 contexts
            OPTIONAL MATCH (dg)-->(s1:sample)-->(p1:participant)

            WITH dg, s1, p1,
                collect(DISTINCT {sm: s1, source_sm: s1, p: p1}) AS direct_contexts

            OPTIONAL MATCH (s2:sample)-->(model)-->(s1)
            WHERE model:cell_line OR model:pdx

            WITH direct_contexts
              + collect(DISTINCT {sm: s1, source_sm: s2, p: p1})
              + collect(DISTINCT {sm: s2, source_sm: s2, p: p1})
              + collect(DISTINCT {sm: s2, source_sm: s1, p: p1}) AS contexts

            UNWIND contexts AS ctx

            RETURN
              ctx.sm AS sm,
              ctx.source_sm AS source_sm,
              ctx.p AS p

            UNION

            WITH dg

            // Case 2: dg -> sample_2 -> model -> sample_1 -> participant
            OPTIONAL MATCH (dg)-->(s2:sample)-->(model)-->(s1:sample)-->(p2:participant)
            WHERE model:cell_line OR model:pdx

            WITH s1, s2, p2
            UNWIND [
              {sm: s1, source_sm: s1, p: p2},
              {sm: s1, source_sm: s2, p: p2},
              {sm: s2, source_sm: s2, p: p2},
              {sm: s2, source_sm: s1, p: p2}
            ] AS ctx

            RETURN
              ctx.sm AS sm,
              ctx.source_sm AS source_sm,
              ctx.p AS p

            UNION

            WITH dg

            // Case 3: dg -> sample -> model -> study
            OPTIONAL MATCH (dg)-->(s3:sample)-->(model)-->(st:study)
            WHERE model:cell_line OR model:pdx

            RETURN s3 AS sm, s3 AS source_sm, null AS p

            UNION

            WITH dg

            // Case 4: dg -> participant, then expand all sample contexts under participant
            OPTIONAL MATCH (dg)-->(p4:participant)

            OPTIONAL MATCH (p4)<--(direct_sm:sample)
            WITH dg, p4,
                collect(DISTINCT {sm: direct_sm, source_sm: direct_sm, p: p4}) AS direct_contexts

            OPTIONAL MATCH (p4)<-[:of_sample]-(s1_model:sample)<--(model)<--(s2_model:sample)
            WHERE model:cell_line OR model:pdx

            WITH direct_contexts
              + collect(DISTINCT {sm: s1_model, source_sm: s1_model, p: p4})
              + collect(DISTINCT {sm: s1_model, source_sm: s2_model, p: p4})
              + collect(DISTINCT {sm: s2_model, source_sm: s2_model, p: p4})
              + collect(DISTINCT {sm: s2_model, source_sm: s1_model, p: p4}) AS contexts

            UNWIND contexts AS ctx

            RETURN
              ctx.sm AS sm,
              ctx.source_sm AS source_sm,
              ctx.p AS p
          }

          WITH dg, sm, source_sm, p
          WHERE sm IS NOT NULL OR p IS NOT NULL

          // Sample-level files
          OPTIONAL MATCH (source_sm)<--(sample_file)
          WHERE sample_file:sequencing_file
            OR sample_file:generic_file
            OR sample_file:methylation_array_file
            OR sample_file:pathology_file
            OR sample_file:cytogenomic_file

          WITH dg, sm, source_sm, p,
              collect(DISTINCT sample_file) AS sample_files

          // Participant-level files
          OPTIONAL MATCH (p)<--(participant_file)
          WHERE participant_file:clinical_measure_file
            OR participant_file:generic_file
            OR participant_file:radiology_file

          WITH dg, sm, source_sm, p,
              sample_files,
              collect(DISTINCT participant_file) AS participant_files

          // Sample-level genetic analysis
          OPTIONAL MATCH (source_sm)<--(sample_ga:genetic_analysis)

          WITH dg, sm, p,
              sample_files,
              participant_files,
              collect(DISTINCT sample_ga) AS sample_gas

          // Participant-level genetic analysis
          OPTIONAL MATCH (p)<--(participant_ga:genetic_analysis)

          WITH dg, sm,
              sample_files + participant_files AS files,
              sample_gas + collect(DISTINCT participant_ga) AS gas

          UNWIND CASE WHEN size(files) = 0 THEN [null] ELSE files END AS file
          UNWIND CASE WHEN size(gas) = 0 THEN [null] ELSE gas END AS ga

          WITH DISTINCT
            dg.guid AS id,

            sm.anatomic_site AS sample_anatomic_site,
            sm.participant_age_at_collection AS participant_age_at_collection,
            sm.sample_tumor_status AS sample_tumor_status,
            sm.tumor_spatial_extent AS tumor_spatial_extent,

            ga.reported_significance AS reported_significance,
            ga.reported_significance_system AS reported_significance_system,
            ga.gene_symbol AS gene_symbol,
            ga.alteration AS alteration,
            ga.fusion_partner_gene AS fusion_partner_gene,
            ga.alteration_type AS alteration_type,
            ga.status AS status,

            file.data_category AS data_category,
            file.file_type AS file_type,
            file.file_mapping_level AS file_mapping_level,

            CASE WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_selection ELSE null END AS library_selection,

            CASE WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_source_material ELSE null END AS library_source_material,

            CASE WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_source_molecule ELSE null END AS library_source_molecule,

            CASE WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_strategy ELSE null END AS library_strategy

          WITH id,
              collect(DISTINCT {
                sample_anatomic_site: sample_anatomic_site,
                participant_age_at_collection: participant_age_at_collection,
                sample_tumor_status: sample_tumor_status,
                tumor_spatial_extent: tumor_spatial_extent,

                reported_significance: reported_significance,
                reported_significance_system: reported_significance_system,
                gene_symbol: gene_symbol,
                alteration: alteration,
                fusion_partner_gene: fusion_partner_gene,
                alteration_type: alteration_type,
                status: status,

                data_category: data_category,
                file_type: file_type,
                file_mapping_level: file_mapping_level,
                library_selection: library_selection,
                library_source_material: library_source_material,
                library_source_molecule: library_source_molecule,
                library_strategy: library_strategy
              }) AS raw_filters

          RETURN
            id,
            [
              f IN raw_filters
              WHERE f.sample_anatomic_site IS NOT NULL
                OR f.participant_age_at_collection IS NOT NULL
                OR f.sample_tumor_status IS NOT NULL
                OR f.tumor_spatial_extent IS NOT NULL
                OR f.file_type IS NOT NULL
            ] AS sample_genetic_analysis_file_filters
        page_size: 500

  - index_name: genetic_analyses_table
    id_field: "id"
    mapping:
      keyword:
        - id
        - genetic_analysis_id
        - pid
        - participant_id
        - race
        - sex_at_birth
        - dbgap_accession
        - study_phase
        - study_id
        - study_acronym
        - study_name
        - gene_symbol
        - gene_symbol_str
        - status
        - reported_significance
        - reported_significance_system
        - hgvs_genome
        - alteration
        - hgvs_coding
        - hgvs_protein
        - reference_genome
        - chromosome
        - cytoband
        - exon
        - fusion_partner_exon
        - fusion_partner_gene
        - alteration_type
        - alteration_effect
        - genomic_source_category
        - test
        - survival_filters.last_known_survival_status
        - survival_filters.cause_of_death
        - survival_filters.first_event
        - treatment_filters.treatment_type
        - treatment_filters.treatment_agent
        - treatment_response_filters.response
        - treatment_response_filters.response_category
        - treatment_response_filters.response_system
        - sample_diagnosis_file_filters.sample_anatomic_site
        - sample_diagnosis_file_filters.sample_tumor_status
        - sample_diagnosis_file_filters.tumor_spatial_extent
        - sample_diagnosis_file_filters.diagnosis
        - sample_diagnosis_file_filters.diagnosis_category
        - sample_diagnosis_file_filters.diagnosis_anatomic_site
        - sample_diagnosis_file_filters.diagnosis_classification_system
        - sample_diagnosis_file_filters.diagnosis_basis
        - sample_diagnosis_file_filters.disease_phase
        - sample_diagnosis_file_filters.data_category
        - sample_diagnosis_file_filters.file_type
        - sample_diagnosis_file_filters.file_mapping_level
        - sample_diagnosis_file_filters.library_selection
        - sample_diagnosis_file_filters.library_source_material
        - sample_diagnosis_file_filters.library_source_molecule
        - sample_diagnosis_file_filters.library_strategy
      integer:
        - survival_filters.age_at_last_known_survival_status
        - treatment_filters.age_at_treatment_start
        - treatment_filters.age_at_treatment_end
        - treatment_response_filters.age_at_response
        - sample_diagnosis_file_filters.participant_age_at_collection
        - sample_diagnosis_file_filters.age_at_diagnosis
    # Cypher queries will be used to retrieve data from Neo4j, and index into Elasticsearch.
    # Uses the GC initial_query + update_queries pattern: initial_query seeds the index with
    # primary keys, then each update_query is applied independently and merged into the
    # corresponding OS document by `id`.
    initial_query:
      query: |
        MATCH (ga:genetic_analysis)
        WITH ga
        SKIP $skip LIMIT $limit
        RETURN ga.guid AS id
      page_size: 1000
    update_queries:
      - name: "genetic_analysis_properties"
        query: |
          MATCH (ga:genetic_analysis)
          WITH ga
          SKIP $skip LIMIT $limit
          WITH ga,
               CASE WHEN ga.gene_symbol STARTS WITH '[' AND ga.gene_symbol ENDS WITH ']' THEN
                    [x IN split(replace(substring(ga.gene_symbol, 1, size(ga.gene_symbol)-2), "'", ""), ",") |
                CASE WHEN trim(x) = ''
                           THEN 'Not Reported'
                  ELSE trim(x) END
                    ]
                  ELSE
              [CASE WHEN trim(ga.gene_symbol) = ''
                          THEN 'Not Reported'
                 ELSE trim(ga.gene_symbol) END]
                END AS gene_symbol_arr
          UNWIND gene_symbol_arr AS gene
          WITH ga, collect(DISTINCT gene) AS gene_symbol
          RETURN
            ga.guid AS id,
            ga.genetic_analysis_id AS genetic_analysis_id,
            ga.alteration AS alteration,
            ga.cytoband AS cytoband,
            gene_symbol AS gene_symbol,
            CASE
              WHEN size(gene_symbol) = 0 THEN null
              ELSE reduce(s = '', item IN gene_symbol |
                CASE WHEN s = '' THEN item ELSE s + ';' + item END)
            END AS gene_symbol_str,
            ga.genomic_source_category AS genomic_source_category,
            ga.hgvs_coding AS hgvs_coding,
            ga.hgvs_genome AS hgvs_genome,
            ga.hgvs_protein AS hgvs_protein,
            ga.reported_significance AS reported_significance,
            ga.reported_significance_system AS reported_significance_system,
            ga.status AS status,
            ga.test AS test,
            ga.alteration_effect AS alteration_effect,
            ga.alteration_type AS alteration_type,
            ga.chromosome AS chromosome,
            ga.exon AS exon,
            ga.fusion_partner_exon AS fusion_partner_exon,
            ga.fusion_partner_gene AS fusion_partner_gene,
            ga.reference_genome AS reference_genome
        page_size: 1000
      - name: "participant_properties"
        query: |
          MATCH (ga:genetic_analysis)
          WITH ga
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (ga)-[*..4]->(p:participant)
          RETURN
            ga.guid AS id,
            p.guid AS pid,
            p.participant_id AS participant_id,
            p.race AS race,
            p.sex_at_birth AS sex_at_birth
        page_size: 1000
      - name: "study_properties"
        query: |
          MATCH (ga:genetic_analysis)
          WITH ga
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (ga)-[*..7]->(st:study)
          RETURN
            ga.guid AS id,
            st.study_id AS study_id,
            st.dbgap_accession AS dbgap_accession,
            st.study_phase AS study_phase,
            st.study_acronym AS study_acronym,
            st.study_name AS study_name
        page_size: 1000
      - name: "survival_filters"
        query: |
          MATCH (ga:genetic_analysis)
          WITH ga
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (ga)-[*..4]->(:participant)<-[:of_survival]-(su:survival)
          WITH
            ga.guid AS id,
            count(su) AS su_count,
            [item IN collect(DISTINCT {
              last_known_survival_status: su.last_known_survival_status,
              cause_of_death: su.cause_of_death,
              first_event: su.first_event,
              age_at_last_known_survival_status: su.age_at_last_known_survival_status
            }) WHERE item.last_known_survival_status IS NOT NULL OR item.cause_of_death IS NOT NULL OR item.first_event IS NOT NULL OR item.age_at_last_known_survival_status IS NOT NULL] AS survival_filters
          WITH id, CASE WHEN su_count > 0 THEN survival_filters ELSE [] END AS survival_filters
          RETURN id, survival_filters
        page_size: 1000
      - name: "treatment_filters"
        query: |
          MATCH (ga:genetic_analysis)
          WITH ga
          SKIP $skip LIMIT $limit

          OPTIONAL MATCH (ga)-[*..4]->(p:participant)

          OPTIONAL MATCH (p)<-[:of_treatment_chemotherapy]-(tchemo:treatment_chemotherapy)
          WITH ga, p, COLLECT(DISTINCT {
            treatment_type: tchemo.chemotherapy_type,
            treatment_agent: tchemo.chemotherapy_agent,
            age_at_treatment_start: tchemo.age_at_treatment_start,
            age_at_treatment_end: tchemo.age_at_treatment_end
          }) AS chemo_filters

          OPTIONAL MATCH (p)<-[:of_treatment_radiation]-(trad:treatment_radiation)
          WITH ga, p, chemo_filters, COLLECT(DISTINCT {
            treatment_type: trad.treatment_type,
            treatment_agent: trad.radiation_therapy_type,
            age_at_treatment_start: trad.age_at_treatment_start,
            age_at_treatment_end: trad.age_at_treatment_end
          }) AS radiation_filters

          OPTIONAL MATCH (p)<-[:of_treatment_surgery]-(tsurg:treatment_surgery)
          WITH ga, p, chemo_filters, radiation_filters, COLLECT(DISTINCT {
            treatment_type: tsurg.treatment_type,
            treatment_agent: tsurg.surgery_type,
            age_at_treatment_start: tsurg.age_at_treatment_start,
            age_at_treatment_end: tsurg.age_at_treatment_end
          }) AS surgery_filters

          OPTIONAL MATCH (p)<-[:of_treatment_other]-(toth:treatment_other)
          WITH ga,
              chemo_filters + radiation_filters + surgery_filters + COLLECT(DISTINCT {
                treatment_type: toth.treatment_type,
                treatment_agent: toth.other_treatment_type,
                age_at_treatment_start: toth.age_at_treatment_start,
                age_at_treatment_end: toth.age_at_treatment_end
              }) AS raw_treatment_filters

          UNWIND raw_treatment_filters AS f

          WITH ga, COLLECT(DISTINCT f) AS deduped_treatment_filters

          WITH ga.guid AS id,
              [
                f IN deduped_treatment_filters
                WHERE f.treatment_type IS NOT NULL
                    OR f.treatment_agent IS NOT NULL
                    OR f.age_at_treatment_start IS NOT NULL
                    OR f.age_at_treatment_end IS NOT NULL
              ] AS treatment_filters

          RETURN id, treatment_filters
        page_size: 1000
      - name: "treatment_response_filters"
        query: |
          MATCH (ga:genetic_analysis)
          WITH ga
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (ga)-[*..4]->(:participant)<-[:of_treatment_response]-(tresp:treatment_response)
          WITH
            ga.guid AS id,
            count(tresp) AS tresp_count,
            [item IN collect(DISTINCT {
              age_at_response: tresp.age_at_response,
              response: tresp.response,
              response_category: tresp.response_category,
              response_system: tresp.response_system
            }) WHERE item.response IS NOT NULL OR item.age_at_response IS NOT NULL OR item.response_category IS NOT NULL OR item.response_system IS NOT NULL] AS treatment_response_filters
          WITH id, CASE WHEN tresp_count > 0 THEN treatment_response_filters ELSE [] END AS treatment_response_filters
          RETURN id, treatment_response_filters
        page_size: 1000
      - name: "sample_diagnosis_file_filters"
        query: |
          MATCH (ga:genetic_analysis)
          WITH ga
          SKIP $skip LIMIT $limit

          CALL {
            WITH ga

            // Case 1: ga -> sample_1 -> participant
            // Also include related sample_2 -> model -> sample_1 contexts
            OPTIONAL MATCH (ga)-->(s1:sample)-->(p1:participant)

            WITH ga, s1, p1,
                collect(DISTINCT {sm: s1, source_sm: s1, p: p1}) AS direct_contexts

            OPTIONAL MATCH (s2:sample)-->(model)-->(s1)
            WHERE model:cell_line OR model:pdx

            WITH direct_contexts
              + collect(DISTINCT {sm: s1, source_sm: s2, p: p1})
              + collect(DISTINCT {sm: s2, source_sm: s2, p: p1})
              + collect(DISTINCT {sm: s2, source_sm: s1, p: p1}) AS contexts

            UNWIND contexts AS ctx

            RETURN
              ctx.sm AS sm,
              ctx.source_sm AS source_sm,
              ctx.p AS p

            UNION

            WITH ga

            // Case 2: ga -> sample_2 -> model -> sample_1 -> participant
            OPTIONAL MATCH (ga)-->(s2:sample)-->(model)-->(s1:sample)-->(p2:participant)
            WHERE model:cell_line OR model:pdx

            WITH s1, s2, p2
            UNWIND [
              {sm: s1, source_sm: s1, p: p2},
              {sm: s1, source_sm: s2, p: p2},
              {sm: s2, source_sm: s2, p: p2},
              {sm: s2, source_sm: s1, p: p2}
            ] AS ctx

            RETURN
              ctx.sm AS sm,
              ctx.source_sm AS source_sm,
              ctx.p AS p

            UNION

            WITH ga

            // Case 3: ga -> sample -> model -> study
            OPTIONAL MATCH (ga)-->(s3:sample)-->(model)-->(st:study)
            WHERE model:cell_line OR model:pdx

            RETURN s3 AS sm, s3 AS source_sm, null AS p

            UNION

            WITH ga

            // Case 4: ga -> participant, then expand all sample contexts under participant
            OPTIONAL MATCH (ga)-->(p4:participant)

            OPTIONAL MATCH (p4)<--(direct_sm:sample)
            WITH ga, p4,
                collect(DISTINCT {sm: direct_sm, source_sm: direct_sm, p: p4}) AS direct_contexts

            OPTIONAL MATCH (p4)<-[:of_sample]-(s1_model:sample)<--(model)<--(s2_model:sample)
            WHERE model:cell_line OR model:pdx

            WITH direct_contexts
              + collect(DISTINCT {sm: s1_model, source_sm: s1_model, p: p4})
              + collect(DISTINCT {sm: s1_model, source_sm: s2_model, p: p4})
              + collect(DISTINCT {sm: s2_model, source_sm: s2_model, p: p4})
              + collect(DISTINCT {sm: s2_model, source_sm: s1_model, p: p4}) AS contexts

            UNWIND contexts AS ctx

            RETURN
              ctx.sm AS sm,
              ctx.source_sm AS source_sm,
              ctx.p AS p
          }

          WITH ga, sm, source_sm, p
          WHERE sm IS NOT NULL OR p IS NOT NULL

          // Sample-level files
          OPTIONAL MATCH (source_sm)<--(sample_file)
          WHERE sample_file:sequencing_file
            OR sample_file:generic_file
            OR sample_file:methylation_array_file
            OR sample_file:pathology_file
            OR sample_file:cytogenomic_file

          WITH ga, sm, source_sm, p,
              collect(DISTINCT sample_file) AS sample_files

          // Participant-level files
          OPTIONAL MATCH (p)<--(participant_file)
          WHERE participant_file:clinical_measure_file
            OR participant_file:generic_file
            OR participant_file:radiology_file

          WITH ga, sm, source_sm, p,
              sample_files,
              collect(DISTINCT participant_file) AS participant_files

          // Sample-level diagnosis
          OPTIONAL MATCH (source_sm)<--(sample_dg:diagnosis)

          WITH ga, sm, p,
              sample_files,
              participant_files,
              collect(DISTINCT sample_dg) AS sample_diagnoses

          // Participant-level diagnosis
          OPTIONAL MATCH (p)<--(participant_dg:diagnosis)

          WITH ga, sm,
              sample_files + participant_files AS files,
              sample_diagnoses + collect(DISTINCT participant_dg) AS diagnoses

          UNWIND CASE WHEN size(files) = 0 THEN [null] ELSE files END AS file
          UNWIND CASE WHEN size(diagnoses) = 0 THEN [null] ELSE diagnoses END AS dg

          WITH DISTINCT
            ga.guid AS id,

            sm.anatomic_site AS sample_anatomic_site,
            sm.participant_age_at_collection AS participant_age_at_collection,
            sm.sample_tumor_status AS sample_tumor_status,
            sm.tumor_spatial_extent AS tumor_spatial_extent,

            dg.age_at_diagnosis AS age_at_diagnosis,
            dg.anatomic_site AS diagnosis_anatomic_site,
            dg.disease_phase AS disease_phase,
            dg.diagnosis_classification_system AS diagnosis_classification_system,
            dg.diagnosis_category AS diagnosis_category,
            dg.diagnosis_basis AS diagnosis_basis,
            dg.diagnosis AS diagnosis,

            file.data_category AS data_category,
            file.file_type AS file_type,
            file.file_mapping_level AS file_mapping_level,

            CASE WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_selection ELSE null END AS library_selection,

            CASE WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_source_material ELSE null END AS library_source_material,

            CASE WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_source_molecule ELSE null END AS library_source_molecule,

            CASE WHEN file IS NOT NULL AND file:sequencing_file
                THEN file.library_strategy ELSE null END AS library_strategy

          WITH id,
              collect(DISTINCT {
                sample_anatomic_site: sample_anatomic_site,
                participant_age_at_collection: participant_age_at_collection,
                sample_tumor_status: sample_tumor_status,
                tumor_spatial_extent: tumor_spatial_extent,

                age_at_diagnosis: age_at_diagnosis,
                diagnosis_anatomic_site: diagnosis_anatomic_site,
                disease_phase: disease_phase,
                diagnosis_classification_system: diagnosis_classification_system,
                diagnosis_category: diagnosis_category,
                diagnosis_basis: diagnosis_basis,
                diagnosis: diagnosis,

                data_category: data_category,
                file_type: file_type,
                file_mapping_level: file_mapping_level,
                library_selection: library_selection,
                library_source_material: library_source_material,
                library_source_molecule: library_source_molecule,
                library_strategy: library_strategy
              }) AS raw_filters

          RETURN
            id,
            [
              f IN raw_filters
              WHERE f.sample_anatomic_site IS NOT NULL
                OR f.participant_age_at_collection IS NOT NULL
                OR f.sample_tumor_status IS NOT NULL
                OR f.tumor_spatial_extent IS NOT NULL
                OR f.diagnosis IS NOT NULL
                OR f.age_at_diagnosis IS NOT NULL
                OR f.file_type IS NOT NULL
            ] AS sample_diagnosis_file_filters
        page_size: 500

  - index_name: studies_table
    id_field: "id"
    mapping:
      keyword:
        - id
        - study_id
        - grant_id
        - pubmed_ids
        - dbgap_accession
        - study_phase
        - study_name
        - study_description
        - study_acronym
        - PIs
        - consent_codes
      integer:
        - num_of_diagnoses
        - num_of_participants
        - num_of_samples
        - num_of_files
        - num_of_study_files
        - num_of_participant_files
        - num_of_sample_files
        - num_of_publications
      text:
        - diagnosis_cancer
        - diagnosis_anatomic_site
        - file_types
        - files
    # Cypher queries will be used to retrieve data from Neo4j, and index into Elasticsearch.
    # Uses the GC initial_query + update_queries pattern: initial_query seeds the index with
    # primary keys, then each update_query is applied independently and merged into the
    # corresponding OS document by `id`.
    initial_query:
      query: |
        MATCH (st:study)
        WITH st
        SKIP $skip LIMIT $limit
        RETURN st.guid AS id
      page_size: 1000
    update_queries:
      - name: "study_properties"
        query: |
          MATCH (st:study)
          WITH st
          SKIP $skip LIMIT $limit
          RETURN
            st.guid AS id,
            st.study_id AS study_id,
            st.dbgap_accession AS dbgap_accession,
            st.study_phase AS study_phase,
            st.study_name AS study_name,
            st.study_description AS study_description,
            st.study_acronym AS study_acronym
        page_size: 1000
      - name: "study_funding"
        query: |
          MATCH (st:study)
          WITH st
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (st)<-[:of_study_funding]-(stf:study_funding)
          WITH
            st,
            [g IN COLLECT(DISTINCT stf.grant_id) WHERE g IS NOT NULL] AS grant_ids
          RETURN
            st.guid AS id,
            reduce(
              s = '',
              g IN grant_ids |
                CASE
                  WHEN s = '' THEN g
                  ELSE s + ';' + g
                END
            ) AS grant_id
        page_size: 1000
      - name: "study_personnel"
        query: |
          MATCH (st:study)
          WITH st
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (st)<-[:of_study_personnel]-(stp:study_personnel)
          WHERE stp.personnel_type = 'PI'
          WITH
            st,
            [n IN COLLECT(DISTINCT stp.personnel_name) WHERE n IS NOT NULL] AS pis
          RETURN
            st.guid AS id,
            reduce(
              s = '',
              n IN pis |
                CASE
                  WHEN s = '' THEN n
                  ELSE s + ';' + n
                END
            ) AS PIs
        page_size: 1000
      - name: "study_publications"
        query: |
          MATCH (st:study)
          WITH st
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (st)<-[:of_publication]-(pub:publication)
          WITH st, [pid IN COLLECT(DISTINCT pub.pubmed_id) WHERE pid IS NOT NULL] AS pub_ids
          RETURN
            st.guid AS id,
            reduce(
              s = '',
              pid IN pub_ids |
                CASE
                  WHEN s = '' THEN pid
                  ELSE s + ';' + pid
                END
            ) AS pubmed_ids,
            size(pub_ids) AS num_of_publications
        page_size: 1000
      - name: "consent_codes"
        query: |
          MATCH (st:study)
          WITH st
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (st)<-[:of_consent_group]-(cg:consent_group)
          WITH
            st,
            [code IN COLLECT(DISTINCT cg.consent_group_name) WHERE code IS NOT NULL] AS consent_codes
          RETURN
            st.guid AS id,
            consent_codes AS consent_codes
        page_size: 1000
      - name: "num_of_participants"
        query: |
          MATCH (st:study)
          WITH st
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (st)<-[:of_consent_group]-(:consent_group)<-[:of_participant]-(p:participant)
          RETURN
            st.guid AS id,
            COUNT(DISTINCT p) AS num_of_participants
        page_size: 1000
      - name: "num_of_samples"
        query: |
          MATCH (st:study)
          WITH st
          SKIP $skip LIMIT $limit
          // direct-from-participant samples + cell_line/pdx-derived samples
          OPTIONAL MATCH (st)<-[*..2]-(pcp)<-[:of_sample]-(sm1:sample)
          WHERE (pcp:participant OR pcp:cell_line OR pcp:pdx)
          WITH st, COUNT(DISTINCT sm1.sample_id) AS num_samples_1
          OPTIONAL MATCH
            (st)<-[:of_consent_group]-(:consent_group)<-[:of_participant]-(:participant)<-[:of_sample]-(:sample)<--(cp)<--(sm2:sample)
          WHERE (cp:cell_line OR cp:pdx)
          WITH st, num_samples_1, COUNT(DISTINCT sm2.sample_id) AS num_samples_2
          RETURN
            st.guid AS id,
            num_samples_1 + num_samples_2 AS num_of_samples
        page_size: 1000
      - name: "diagnosis_aggregations"
        query: |
          MATCH (st:study)
          WITH st
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (st)<-[*..6]-(dg:diagnosis)
          WITH st, COUNT(DISTINCT dg.diagnosis) AS num_diag
          OPTIONAL MATCH (st)<-[*..6]-(dg:diagnosis)
          WITH st, num_diag, dg.diagnosis AS dg_cancer, COUNT(dg.diagnosis) AS num_cancers
          ORDER BY num_cancers DESC
          WITH st, num_diag, COLLECT(dg_cancer + ' (' + toString(num_cancers) + ')') AS cancers
          OPTIONAL MATCH (st)<-[*..6]-(diag:diagnosis)
          WITH st, num_diag, cancers, coalesce(diag.anatomic_site, []) AS dg_sites
          UNWIND dg_sites AS dg_site
          WITH st, num_diag, cancers, dg_site, COUNT(dg_site) AS num_sites
          ORDER BY num_sites DESC
          WITH st, num_diag, cancers, COLLECT(dg_site + ' (' + toString(num_sites) + ')') AS sites
          RETURN
            st.guid AS id,
            num_diag AS num_of_diagnoses,
            cancers AS diagnosis_cancer,
            sites AS diagnosis_anatomic_site
        page_size: 1000
      - name: "file_aggregations"
        query: |
          MATCH (st:study)
          WITH st
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (st)<-[*..6]-(fl)
          WHERE (fl:clinical_measure_file OR fl:generic_file OR fl:sequencing_file OR fl:pathology_file OR fl:radiology_file OR fl:methylation_array_file OR fl:cytogenomic_file)
          WITH st,
               fl.file_type AS ft,
               COUNT(DISTINCT fl.dcf_indexd_guid) AS num_ft
          ORDER BY num_ft DESC
          WITH st,
               COLLECT(ft + ' (' + toString(num_ft) + ')') AS file_types
          OPTIONAL MATCH (st)<-[*..6]-(file)
          WHERE (file:clinical_measure_file OR file:generic_file OR file:sequencing_file OR file:pathology_file OR file:radiology_file OR file:methylation_array_file OR file:cytogenomic_file)
          WITH st, file_types,
               file.dcf_indexd_guid AS file_id,
               file.file_mapping_level AS file_level
          WITH st, file_types,
               COLLECT(DISTINCT file_id) AS all_file_ids,
               COUNT(DISTINCT CASE WHEN file_level = 'Study'       THEN file_id END) AS num_study_files,
               COUNT(DISTINCT CASE WHEN file_level = 'Participant' THEN file_id END) AS num_participant_files,
               COUNT(DISTINCT CASE WHEN file_level = 'Sample'      THEN file_id END) AS num_sample_files
          RETURN
            st.guid AS id,
            file_types AS file_types,
            num_study_files + num_participant_files + num_sample_files AS num_of_files,
            all_file_ids AS files,
            num_study_files       AS num_of_study_files,
            num_participant_files AS num_of_participant_files,
            num_sample_files      AS num_of_sample_files
        page_size: 1

  - index_name: samples_table
    id_field: "id"
    mapping:
      keyword:
        - id
        - sample_id
        - pid
        - participant_id
        - race
        - sex_at_birth
        - study_id
        - dbgap_accession
        - study_phase
        - study_acronym
        - study_name
        - sample_anatomic_site
        - sample_anatomic_site_str
        - sample_tumor_status
        - tumor_spatial_extent
        - laterality
        - sample_description
        - pdx_id
        - cell_line_id
        - diagnosis_filters.diagnosis_anatomic_site
        - diagnosis_filters.disease_phase
        - diagnosis_filters.diagnosis_classification_system
        - diagnosis_filters.diagnosis_basis
        - diagnosis_filters.diagnosis
        - diagnosis_filters.diagnosis_category
        - genetic_analysis_filters.reported_significance
        - genetic_analysis_filters.reported_significance_system
        - genetic_analysis_filters.gene_symbol
        - genetic_analysis_filters.alteration
        - genetic_analysis_filters.fusion_partner_gene
        - genetic_analysis_filters.alteration_type
        - genetic_analysis_filters.status
        - file_filters.data_category
        - file_filters.file_type
        - file_filters.file_mapping_level
        - file_filters.library_selection
        - file_filters.library_source_material
        - file_filters.library_source_molecule
        - file_filters.library_strategy
        - survival_filters.last_known_survival_status
        - survival_filters.cause_of_death
        - survival_filters.first_event
        - treatment_filters.treatment_type
        - treatment_filters.treatment_agent
        - treatment_response_filters.response
        - treatment_response_filters.response_category
        - treatment_response_filters.response_system
        - files
      integer:
        - participant_age_at_collection
        - diagnosis_filters.age_at_diagnosis
        - survival_filters.age_at_last_known_survival_status
        - treatment_filters.age_at_treatment_start
        - treatment_filters.age_at_treatment_end
        - treatment_response_filters.age_at_response
        - file_count
        - direct_file_count
      float:
        - percent_tumor
        - percent_necrosis
    # Cypher queries will be used to retrieve data from Neo4j, and index into Elasticsearch.
    # Uses the GC initial_query + update_queries pattern: initial_query seeds the index with
    # primary keys, then each update_query is applied independently and merged into the
    # corresponding OS document by `id`.
    initial_query:
      query: |
        MATCH (sm:sample)
        WITH sm
        SKIP $skip LIMIT $limit
        RETURN sm.guid AS id
      page_size: 1000
    update_queries:
      - name: "sample_properties"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip LIMIT $limit
          RETURN
            sm.guid AS id,
            sm.sample_id AS sample_id,
            sm.anatomic_site AS sample_anatomic_site,
            reduce(
              s = '',
              site IN coalesce(sm.anatomic_site, []) |
                CASE
                  WHEN s = '' THEN site
                  ELSE s + ';' + site
                END
            ) AS sample_anatomic_site_str,
            sm.participant_age_at_collection AS participant_age_at_collection,
            sm.sample_tumor_status AS sample_tumor_status,
            sm.tumor_spatial_extent AS tumor_spatial_extent,
            sm.laterality AS laterality,
            sm.sample_description AS sample_description,
            sm.percent_tumor AS percent_tumor,
            sm.percent_necrosis AS percent_necrosis
        page_size: 1000
      - name: "cell_line_ids"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sm)-[:of_sample]->(out_cell:cell_line)
          WITH sm, collect(DISTINCT out_cell.cell_line_id) AS outgoing_cell_line_ids
          OPTIONAL MATCH (sm)<-[:of_cell_line]-(in_cell:cell_line)
          WITH
            sm,
            outgoing_cell_line_ids,
            collect(DISTINCT in_cell.cell_line_id) AS incoming_cell_line_ids
          RETURN
            sm.guid AS id,
            reduce(
              cell_line_ids = [],
              cell_line_id IN outgoing_cell_line_ids + incoming_cell_line_ids |
                CASE
                  WHEN cell_line_id IS NULL OR cell_line_id IN cell_line_ids THEN cell_line_ids
                  ELSE cell_line_ids + [cell_line_id]
                END
            ) AS cell_line_id
        page_size: 1000
      - name: "pdx_ids"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sm)-[:of_sample]->(out_pdx:pdx)
          WITH sm, collect(DISTINCT out_pdx.pdx_id) AS outgoing_pdx_ids
          OPTIONAL MATCH (sm)<-[:of_pdx]-(in_pdx:pdx)
          WITH
            sm,
            outgoing_pdx_ids,
            collect(DISTINCT in_pdx.pdx_id) AS incoming_pdx_ids
          RETURN
            sm.guid AS id,
            reduce(
              pdx_ids = [],
              pdx_id IN outgoing_pdx_ids + incoming_pdx_ids |
                CASE
                  WHEN pdx_id IS NULL OR pdx_id IN pdx_ids THEN pdx_ids
                  ELSE pdx_ids + [pdx_id]
                END
            ) AS pdx_id
        page_size: 1000
      - name: "participant_properties"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sm)-[*..3]->(p:participant)
          RETURN
            sm.guid AS id,
            p.guid AS pid,
            p.participant_id AS participant_id,
            p.race AS race,
            p.sex_at_birth AS sex_at_birth
        page_size: 1000
      - name: "study_properties"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip LIMIT $limit
          // study reachable via participant->consent_group OR via cell_line/pdx
          OPTIONAL MATCH (sm)-[*..5]->(st:study)
          WITH sm, st
          RETURN
            sm.guid AS id,
            st.study_id AS study_id,
            st.dbgap_accession AS dbgap_accession,
            st.study_phase AS study_phase,
            st.study_acronym AS study_acronym,
            st.study_name AS study_name
        page_size: 1000
      - name: "diagnosis_filters"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sm)-[*..3]->(:participant)<--(dg1:diagnosis)
          WITH sm, COLLECT(DISTINCT {
            age_at_diagnosis: dg1.age_at_diagnosis,
            diagnosis_anatomic_site: dg1.anatomic_site,
            disease_phase: dg1.disease_phase,
            diagnosis_classification_system: dg1.diagnosis_classification_system,
            diagnosis_category: dg1.diagnosis_category,
            diagnosis_basis: dg1.diagnosis_basis,
            diagnosis: dg1.diagnosis
          }) AS dg1_filters
          OPTIONAL MATCH (sm)<-[*..3]-(dg2:diagnosis)
          WITH sm, dg1_filters, COLLECT(DISTINCT {
            age_at_diagnosis: dg2.age_at_diagnosis,
            diagnosis_anatomic_site: dg2.anatomic_site,
            disease_phase: dg2.disease_phase,
            diagnosis_classification_system: dg2.diagnosis_classification_system,
            diagnosis_category: dg2.diagnosis_category,
            diagnosis_basis: dg2.diagnosis_basis,
            diagnosis: dg2.diagnosis
          }) AS dg2_filters
          WITH sm, dg1_filters + dg2_filters AS all_diag_filters
          UNWIND CASE WHEN size(all_diag_filters) = 0 THEN [NULL] ELSE all_diag_filters END AS f
          WITH sm, COLLECT(DISTINCT f) AS raw_diag_filters
          WITH sm,
               [f IN raw_diag_filters WHERE f IS NOT NULL AND (f.diagnosis IS NOT NULL OR f.age_at_diagnosis IS NOT NULL)] AS diagnosis_filters
          RETURN
            sm.guid AS id,
            diagnosis_filters AS diagnosis_filters
        page_size: 1000
      - name: "genetic_analysis_filters"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sm)-[*..3]->(:participant)<--(ga1:genetic_analysis)
          WITH sm, COLLECT(DISTINCT {
            reported_significance: ga1.reported_significance,
            reported_significance_system: ga1.reported_significance_system,
            gene_symbol: ga1.gene_symbol,
            alteration: ga1.alteration,
            fusion_partner_gene: ga1.fusion_partner_gene,
            alteration_type: ga1.alteration_type,
            status: ga1.status
          }) AS ga1_filters
          OPTIONAL MATCH (sm)<-[*..3]-(ga2:genetic_analysis)
          WITH sm, ga1_filters, COLLECT(DISTINCT {
            reported_significance: ga2.reported_significance,
            reported_significance_system: ga2.reported_significance_system,
            gene_symbol: ga2.gene_symbol,
            alteration: ga2.alteration,
            fusion_partner_gene: ga2.fusion_partner_gene,
            alteration_type: ga2.alteration_type,
            status: ga2.status
          }) AS ga2_filters
          WITH sm, ga1_filters + ga2_filters AS all_ga_filters
          UNWIND CASE WHEN size(all_ga_filters) = 0 THEN [NULL] ELSE all_ga_filters END AS f
          WITH sm, COLLECT(DISTINCT f) AS raw_ga_filters
          WITH sm,
               [f IN raw_ga_filters WHERE f IS NOT NULL AND (f.alteration IS NOT NULL OR f.gene_symbol IS NOT NULL OR f.alteration_type IS NOT NULL OR f.status IS NOT NULL OR f.reported_significance IS NOT NULL)] AS genetic_analysis_filters
          RETURN
            sm.guid AS id,
            genetic_analysis_filters AS genetic_analysis_filters
        page_size: 1000
      - name: "file_filters_and_files"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip LIMIT $limit
          // Participant-level files (clinical_measure / generic / radiology)
          OPTIONAL MATCH (sm)-[*..3]->(:participant)<--(pfile)
          WHERE (pfile:clinical_measure_file OR pfile:generic_file OR pfile:radiology_file)
          WITH sm, COLLECT(DISTINCT {
            data_category: pfile.data_category,
            file_type: pfile.file_type,
            file_mapping_level: pfile.file_mapping_level,
            library_selection: null,
            library_source_material: null,
            library_source_molecule: null,
            library_strategy: null
          }) AS f_participant_part
          // Sample-level files (sequencing_file / generic / pathology / cytogenomic / methylation_array)
          OPTIONAL MATCH (sm)<-[*..3]-(sfile)
          WHERE (sfile:sequencing_file OR sfile:generic_file OR sfile:pathology_file OR sfile:cytogenomic_file OR sfile:methylation_array_file)
          WITH sm, f_participant_part, COLLECT(DISTINCT {
            data_category: sfile.data_category,
            file_type: sfile.file_type,
            file_mapping_level: sfile.file_mapping_level,
            library_selection: sfile.library_selection,
            library_source_material: sfile.library_source_material,
            library_source_molecule: sfile.library_source_molecule,
            library_strategy: sfile.library_strategy
          }) AS f_sample_part
          // Sample-level files (sequencing_file / generic / pathology / cytogenomic / methylation_array)
          OPTIONAL MATCH (sm)-->(model)-->(sm1:sample)
          WHERE model:cell_line OR model:pdx
          OPTIONAL MATCH (sm1)<--(parent_sfile)
          WHERE (parent_sfile:sequencing_file OR parent_sfile:generic_file OR parent_sfile:pathology_file OR parent_sfile:cytogenomic_file OR parent_sfile:methylation_array_file)
          WITH sm, f_participant_part, f_sample_part, COLLECT(DISTINCT {
            data_category: parent_sfile.data_category,
            file_type: parent_sfile.file_type,
            file_mapping_level: parent_sfile.file_mapping_level,
            library_selection: parent_sfile.library_selection,
            library_source_material: parent_sfile.library_source_material,
            library_source_molecule: parent_sfile.library_source_molecule,
            library_strategy: parent_sfile.library_strategy
          }) AS f_sample_part_additional
          WITH sm,
               f_participant_part + f_sample_part + f_sample_part_additional AS all_file_filters
          UNWIND CASE WHEN size(all_file_filters) = 0 THEN [NULL] ELSE all_file_filters END AS f
          WITH sm, COLLECT(DISTINCT f) AS raw_file_filters
          WITH sm,
               [f IN raw_file_filters WHERE f IS NOT NULL AND f.file_type IS NOT NULL] AS file_filters
          WITH sm, file_filters
          RETURN
            sm.guid AS id,
            file_filters AS file_filters,
            [] AS files, // Not used in the current implementation
            0 AS file_count, // Not used in the current implementation
            0 AS direct_file_count
        page_size: 1000
      - name: "survival_filters"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sm)-[*..3]->(:participant)<-[:of_survival]-(su:survival)
          WITH
            sm.guid AS id,
            count(su) AS su_count,
            [item IN collect(DISTINCT {
              last_known_survival_status: su.last_known_survival_status,
              cause_of_death: su.cause_of_death,
              first_event: su.first_event,
              age_at_last_known_survival_status: su.age_at_last_known_survival_status
            }) WHERE item.last_known_survival_status IS NOT NULL OR item.cause_of_death IS NOT NULL OR item.first_event IS NOT NULL OR item.age_at_last_known_survival_status IS NOT NULL] AS survival_filters
          WITH
            id,
            CASE WHEN su_count > 0 THEN survival_filters ELSE [] END AS survival_filters
          RETURN id, survival_filters
        page_size: 1000
      - name: "treatment_filters"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip
          LIMIT $limit

          OPTIONAL MATCH (sm)-[*..3]->(p:participant)

          OPTIONAL MATCH (p)<-[:of_treatment_chemotherapy]-(tchemo:treatment_chemotherapy)
          WITH sm, p, COLLECT(DISTINCT {
            treatment_type: tchemo.chemotherapy_type,
            treatment_agent: tchemo.chemotherapy_agent,
            age_at_treatment_start: tchemo.age_at_treatment_start,
            age_at_treatment_end: tchemo.age_at_treatment_end
          }) AS chemo_filters

          OPTIONAL MATCH (p)<-[:of_treatment_radiation]-(trad:treatment_radiation)
          WITH sm, p, chemo_filters, COLLECT(DISTINCT {
            treatment_type: trad.treatment_type,
            treatment_agent: trad.radiation_therapy_type,
            age_at_treatment_start: trad.age_at_treatment_start,
            age_at_treatment_end: trad.age_at_treatment_end
          }) AS radiation_filters

          OPTIONAL MATCH (p)<-[:of_treatment_surgery]-(tsurg:treatment_surgery)
          WITH sm, p, chemo_filters, radiation_filters, COLLECT(DISTINCT {
            treatment_type: tsurg.treatment_type,
            treatment_agent: tsurg.surgery_type,
            age_at_treatment_start: tsurg.age_at_treatment_start,
            age_at_treatment_end: tsurg.age_at_treatment_end
          }) AS surgery_filters

          OPTIONAL MATCH (p)<-[:of_treatment_other]-(toth:treatment_other)
          WITH sm,
              chemo_filters + radiation_filters + surgery_filters + COLLECT(DISTINCT {
                treatment_type: toth.treatment_type,
                treatment_agent: toth.other_treatment_type,
                age_at_treatment_start: toth.age_at_treatment_start,
                age_at_treatment_end: toth.age_at_treatment_end
              }) AS raw_treatment_filters

          UNWIND raw_treatment_filters AS f

          WITH sm, COLLECT(DISTINCT f) AS deduped_treatment_filters

          WITH sm.guid AS id,
              [
                f IN deduped_treatment_filters
                WHERE f.treatment_type IS NOT NULL
                    OR f.treatment_agent IS NOT NULL
                    OR f.age_at_treatment_start IS NOT NULL
                    OR f.age_at_treatment_end IS NOT NULL
              ] AS treatment_filters

          RETURN id, treatment_filters
        page_size: 1000
      - name: "treatment_response_filters"
        query: |
          MATCH (sm:sample)
          WITH sm
          SKIP $skip LIMIT $limit
          OPTIONAL MATCH (sm)-[*..3]->(:participant)<-[:of_treatment_response]-(tresp:treatment_response)
          WITH
            sm.guid AS id,
            count(tresp) AS tresp_count,
            [item IN collect(DISTINCT {
              age_at_response: tresp.age_at_response,
              response: tresp.response,
              response_category: tresp.response_category,
              response_system: tresp.response_system
            }) WHERE item.response IS NOT NULL OR item.age_at_response IS NOT NULL OR item.response_category IS NOT NULL OR item.response_system IS NOT NULL] AS treatment_response_filters
          WITH
            id,
            CASE WHEN tresp_count > 0 THEN treatment_response_filters ELSE [] END AS treatment_response_filters
          RETURN id, treatment_response_filters
        page_size: 1000

  - index_name: files_table
    id_field: "id"
    mapping:
      keyword:
        - id
        - file_id
        - pid
        - participant_id
        - sample_id
        - study_id
        - dbgap_accession
        - study_phase
        - study_acronym
        - study_name
        - anatomic_site
        - sample_tumor_status
        - tumor_spatial_extent
        - sample_description
        - file_name
        - data_category
        - file_type
        - file_description
        - consent_codes
        - md5sum
        - file_mapping_level
        - file_access
        - library_selection
        - library_strategy
        - library_source_material
        - library_source_molecule
        - fixation_embedding_method
        - staining_method
        - combined_filters.participant_id
        - combined_filters.race
        - combined_filters.sex_at_birth
        - combined_filters.survival_filters.last_known_survival_status
        - combined_filters.survival_filters.cause_of_death
        - combined_filters.survival_filters.first_event
        - combined_filters.treatment_filters.treatment_type
        - combined_filters.treatment_filters.treatment_agent
        - combined_filters.treatment_response_filters.response
        - combined_filters.treatment_response_filters.response_category
        - combined_filters.treatment_response_filters.response_system
        - combined_filters.sample_diagnosis_genetic_analysis_filters.sample_anatomic_site
        - combined_filters.sample_diagnosis_genetic_analysis_filters.sample_tumor_status
        - combined_filters.sample_diagnosis_genetic_analysis_filters.tumor_spatial_extent
        - combined_filters.sample_diagnosis_genetic_analysis_filters.diagnosis_anatomic_site
        - combined_filters.sample_diagnosis_genetic_analysis_filters.disease_phase
        - combined_filters.sample_diagnosis_genetic_analysis_filters.diagnosis_classification_system
        - combined_filters.sample_diagnosis_genetic_analysis_filters.diagnosis_basis
        - combined_filters.sample_diagnosis_genetic_analysis_filters.diagnosis
        - combined_filters.sample_diagnosis_genetic_analysis_filters.diagnosis_category
        - combined_filters.sample_diagnosis_genetic_analysis_filters.reported_significance
        - combined_filters.sample_diagnosis_genetic_analysis_filters.reported_significance_system
        - combined_filters.sample_diagnosis_genetic_analysis_filters.gene_symbol
        - combined_filters.sample_diagnosis_genetic_analysis_filters.alteration
        - combined_filters.sample_diagnosis_genetic_analysis_filters.fusion_partner_gene
        - combined_filters.sample_diagnosis_genetic_analysis_filters.alteration_type
        - combined_filters.sample_diagnosis_genetic_analysis_filters.status
      integer:
        - participant_age_at_collection
        - combined_filters.survival_filters.age_at_last_known_survival_status
        - combined_filters.treatment_filters.age_at_treatment_start
        - combined_filters.treatment_filters.age_at_treatment_end
        - combined_filters.treatment_response_filters.age_at_response
        - combined_filters.sample_diagnosis_genetic_analysis_filters.participant_age_at_collection
        - combined_filters.sample_diagnosis_genetic_analysis_filters.age_at_diagnosis
      float:
        - percent_tumor
        - percent_necrosis
      long:
        - file_size
      text:
        - files
    initial_query:
      query: |
        MATCH (f:
          clinical_measure_file |
          generic_file |
          radiology_file |
          cytogenomic_file |
          sequencing_file |
          pathology_file |
          methylation_array_file
        )
        WITH DISTINCT f.dcf_indexd_guid AS id
        SKIP $skip LIMIT $limit
        RETURN
          id
      page_size: 5000
    update_queries:
      - name: "study_properties"
        query: |
          MATCH (f:
            clinical_measure_file |
            generic_file |
            radiology_file |
            cytogenomic_file |
            sequencing_file |
            pathology_file |
            methylation_array_file
          )
          WHERE f.dcf_indexd_guid IS NOT NULL

          WITH
            f.dcf_indexd_guid AS id,
            collect(DISTINCT f) AS file_nodes

          SKIP $skip
          LIMIT $limit

          UNWIND file_nodes AS f

          OPTIONAL MATCH (f)-[
            :of_clinical_measure_file |
            of_generic_file |
            of_radiology_file |
            of_cytogenomic_file |
            of_sequencing_file |
            of_pathology_file |
            of_methylation_array_file |
            of_sample |
            of_pdx |
            of_cell_line |
            of_participant |
            of_consent_group
            *..6
          ]->(st:study)

          WITH
            id,
            head(collect(DISTINCT st)) AS first_study

          RETURN
            id,
            first_study.study_id AS study_id,
            first_study.dbgap_accession AS dbgap_accession,
            first_study.study_phase AS study_phase,
            first_study.study_acronym AS study_acronym,
            first_study.study_name AS study_name
        page_size: 5000
      - name: "file_properties"
        query: |
          MATCH (f:
            clinical_measure_file |
            generic_file |
            radiology_file |
            cytogenomic_file |
            sequencing_file |
            pathology_file |
            methylation_array_file
          )
          WITH DISTINCT f.dcf_indexd_guid AS id, head(collect(f)) AS first_file
          SKIP $skip LIMIT $limit
          RETURN
            id,
            first_file.file_name AS file_name,
            first_file.data_category AS data_category,
            first_file.file_type AS file_type,
            first_file.file_description AS file_description,
            first_file.file_size AS file_size,
            first_file.md5sum AS md5sum,
            first_file.file_mapping_level AS file_mapping_level,
            first_file.file_access AS file_access,
            first_file.library_selection AS library_selection,
            first_file.library_source_material AS library_source_material,
            first_file.library_source_molecule AS library_source_molecule,
            first_file.library_strategy AS library_strategy,
            first_file.fixation_embedding_method AS fixation_embedding_method,
            first_file.staining_method AS staining_method,
            [id] as files
        page_size: 5000
      - name: "combined_filters_study_level"
        query: |
          MATCH (f:
            clinical_measure_file |
            generic_file
          )-->(st:study)
          WITH
            distinct f.dcf_indexd_guid AS file_guid

          SKIP $skip
          LIMIT $limit

          RETURN
            file_guid AS id,
            file_guid AS file_id,
            null AS pid,
            null AS participant_id,
            null AS sample_id,
            null As consent_codes, // study level files should not have consent codes
            null AS anatomic_site,
            null AS participant_age_at_collection,
            null AS sample_tumor_status,
            null AS tumor_spatial_extent,
            null AS sample_description,
            null AS percent_tumor,
            null AS percent_necrosis,
            [file_guid] AS files,
            [{
              participant_id: null,
              race: null,
              sex_at_birth: null,
              sample_diagnosis_genetic_analysis_filters: [],
              survival_filters: [],
              treatment_filters: [],
              treatment_response_filters: []
            }] AS combined_filters
        page_size: 10000 # There were only 140 results in my local Memgraph, so we can use page size 10k or more to collect all study level files
      - name: "combined_filters_participant_level"
        query: |
          MATCH (f:
            clinical_measure_file |
            generic_file |
            radiology_file
          )-->(:participant)

          WITH
            f.dcf_indexd_guid AS file_guid,
            collect(DISTINCT f) AS file_nodes

          SKIP $skip
          LIMIT $limit

          UNWIND file_nodes AS f

          MATCH (f)-[:of_clinical_measure_file|of_generic_file|of_radiology_file]->(p:participant)

          WITH DISTINCT
            file_guid,
            p

          // Start Sample-based paths
          OPTIONAL MATCH (p)<-[:of_sample]-(direct_sm:sample)

          WITH
            file_guid,
            p,
            collect(DISTINCT CASE
              WHEN direct_sm IS NOT NULL THEN {
                sm: direct_sm,
                source_sm: direct_sm
              }
            END) AS direct_sample_contexts

          // Sample->Sample
          OPTIONAL MATCH (p)<-[:of_sample]-(sm1_model:sample)<-[:of_cell_line|of_pdx]-(model)<-[:of_sample]-(sm2_model:sample)
          WHERE model:cell_line OR model:pdx

          WITH
            file_guid,
            p,
            direct_sample_contexts
            +
            collect(DISTINCT CASE
              WHEN sm1_model IS NOT NULL THEN {
                sm: sm1_model,
                source_sm: sm1_model
              }
            END)
            +
            collect(DISTINCT CASE
              WHEN sm2_model IS NOT NULL THEN {
                sm: sm2_model,
                source_sm: sm2_model
              }
            END)
            +
            collect(DISTINCT CASE
              WHEN sm1_model IS NOT NULL AND sm2_model IS NOT NULL THEN {
                sm: sm1_model,
                source_sm: sm2_model
              }
            END)
            +
            collect(DISTINCT CASE
              WHEN sm1_model IS NOT NULL AND sm2_model IS NOT NULL THEN {
                sm: sm2_model,
                source_sm: sm1_model
              }
            END) AS raw_sample_contexts

          WITH
            file_guid,
            p,
            [
              sample_ctx IN raw_sample_contexts
              WHERE sample_ctx IS NOT NULL
                AND sample_ctx.sm IS NOT NULL
                AND sample_ctx.source_sm IS NOT NULL
            ] AS sample_contexts

          WITH
            file_guid,
            p,
            CASE
              WHEN size(sample_contexts) = 0 THEN [{ sm: null, source_sm: null }]
              ELSE sample_contexts
            END AS sample_contexts

          UNWIND sample_contexts AS sample_ctx

          WITH
            file_guid,
            p,
            sample_ctx.sm AS sm,
            sample_ctx.source_sm AS source_sm

          // Participant<-Diagnosis and Sample<-Diagnosis paths
          OPTIONAL MATCH (source_sm)<-[:of_diagnosis]-(sample_dg:diagnosis)
          OPTIONAL MATCH (p)<-[:of_diagnosis]-(participant_dg:diagnosis)

          WITH
            file_guid,
            p,
            sm,
            source_sm,
            collect(DISTINCT sample_dg) + collect(DISTINCT participant_dg) AS diagnosis_nodes

          // Participant<-Genetic Analysis and Sample<-Genetic Analysis paths
          OPTIONAL MATCH (source_sm)<-[:of_genetic_analysis]-(sample_ga:genetic_analysis)
          OPTIONAL MATCH (p)<-[:of_genetic_analysis]-(participant_ga:genetic_analysis)

          WITH
            file_guid,
            p,
            sm,
            [x IN diagnosis_nodes WHERE x IS NOT NULL] AS diagnoses,
            collect(DISTINCT sample_ga) + collect(DISTINCT participant_ga) AS ga_nodes

          WITH
            file_guid,
            p,
            sm,
            diagnoses,
            [x IN ga_nodes WHERE x IS NOT NULL] AS gas

          UNWIND CASE
            WHEN size(diagnoses) = 0 THEN [null]
            ELSE diagnoses
          END AS dg

          UNWIND CASE
            WHEN size(gas) = 0 THEN [null]
            ELSE gas
          END AS ga

          WITH
            file_guid,
            p,
            sm,
            collect(DISTINCT {
              sample_anatomic_site: CASE WHEN sm IS NOT NULL THEN sm.anatomic_site ELSE null END,
              participant_age_at_collection: CASE WHEN sm IS NOT NULL THEN sm.participant_age_at_collection ELSE null END,
              sample_tumor_status: CASE WHEN sm IS NOT NULL THEN sm.sample_tumor_status ELSE null END,
              tumor_spatial_extent: CASE WHEN sm IS NOT NULL THEN sm.tumor_spatial_extent ELSE null END,

              age_at_diagnosis: CASE WHEN dg IS NOT NULL THEN dg.age_at_diagnosis ELSE null END,
              diagnosis_anatomic_site: CASE WHEN dg IS NOT NULL THEN dg.anatomic_site ELSE null END,
              disease_phase: CASE WHEN dg IS NOT NULL THEN dg.disease_phase ELSE null END,
              diagnosis_classification_system: CASE WHEN dg IS NOT NULL THEN dg.diagnosis_classification_system ELSE null END,
              diagnosis_category: CASE WHEN dg IS NOT NULL THEN dg.diagnosis_category ELSE null END,
              diagnosis_basis: CASE WHEN dg IS NOT NULL THEN dg.diagnosis_basis ELSE null END,
              diagnosis: CASE WHEN dg IS NOT NULL THEN dg.diagnosis ELSE null END,

              reported_significance: CASE WHEN ga IS NOT NULL THEN ga.reported_significance ELSE null END,
              reported_significance_system: CASE WHEN ga IS NOT NULL THEN ga.reported_significance_system ELSE null END,
              gene_symbol: CASE WHEN ga IS NOT NULL THEN ga.gene_symbol ELSE null END,
              alteration: CASE WHEN ga IS NOT NULL THEN ga.alteration ELSE null END,
              fusion_partner_gene: CASE WHEN ga IS NOT NULL THEN ga.fusion_partner_gene ELSE null END,
              alteration_type: CASE WHEN ga IS NOT NULL THEN ga.alteration_type ELSE null END,
              status: CASE WHEN ga IS NOT NULL THEN ga.status ELSE null END
            }) AS sample_diagnosis_genetic_analysis_filters

          OPTIONAL MATCH (p)<-[:of_survival]-(su:survival)

          WITH
            file_guid,
            p,
            sm,
            sample_diagnosis_genetic_analysis_filters,
            collect(DISTINCT su) AS survival_nodes

          WITH
            file_guid,
            p,
            sm,
            sample_diagnosis_genetic_analysis_filters,
            [x IN survival_nodes WHERE x IS NOT NULL |
              {
                last_known_survival_status: x.last_known_survival_status,
                cause_of_death: x.cause_of_death,
                first_event: x.first_event,
                age_at_last_known_survival_status: x.age_at_last_known_survival_status
              }
            ] AS survival_filters

          OPTIONAL MATCH (p)<-[:of_treatment_chemotherapy]-(tchemo:treatment_chemotherapy)

          WITH
            file_guid,
            p,
            sm,
            sample_diagnosis_genetic_analysis_filters,
            survival_filters,
            collect(DISTINCT tchemo) AS chemotherapy_nodes

          OPTIONAL MATCH (p)<-[:of_treatment_radiation]-(trad:treatment_radiation)

          WITH
            file_guid,
            p,
            sm,
            sample_diagnosis_genetic_analysis_filters,
            survival_filters,
            chemotherapy_nodes,
            collect(DISTINCT trad) AS radiation_nodes

          OPTIONAL MATCH (p)<-[:of_treatment_surgery]-(tsurg:treatment_surgery)

          WITH
            file_guid,
            p,
            sm,
            sample_diagnosis_genetic_analysis_filters,
            survival_filters,
            chemotherapy_nodes,
            radiation_nodes,
            collect(DISTINCT tsurg) AS surgery_nodes

          OPTIONAL MATCH (p)<-[:of_treatment_other]-(tother:treatment_other)

          WITH
            file_guid,
            p,
            sm,
            sample_diagnosis_genetic_analysis_filters,
            survival_filters,
            chemotherapy_nodes,
            radiation_nodes,
            surgery_nodes,
            collect(DISTINCT tother) AS other_treatment_nodes

          WITH
            file_guid,
            p,
            sm,
            sample_diagnosis_genetic_analysis_filters,
            survival_filters,
            [x IN chemotherapy_nodes WHERE x IS NOT NULL |
              {
                treatment_type: coalesce(x.treatment_type, x.chemotherapy_type, 'chemotherapy'),
                treatment_agent: coalesce(x.treatment_agent, x.chemotherapy_agent),
                age_at_treatment_start: x.age_at_treatment_start,
                age_at_treatment_end: x.age_at_treatment_end
              }
            ] AS chemo_filters,
            [x IN radiation_nodes WHERE x IS NOT NULL |
              {
                treatment_type: coalesce(x.treatment_type, x.radiation_type, 'radiation'),
                treatment_agent: coalesce(x.treatment_agent, x.radiation_agent),
                age_at_treatment_start: x.age_at_treatment_start,
                age_at_treatment_end: x.age_at_treatment_end
              }
            ] AS radiation_filters,
            [x IN surgery_nodes WHERE x IS NOT NULL |
              {
                treatment_type: coalesce(x.treatment_type, x.surgery_type, 'surgery'),
                treatment_agent: coalesce(x.treatment_agent, x.surgery_agent),
                age_at_treatment_start: x.age_at_treatment_start,
                age_at_treatment_end: x.age_at_treatment_end
              }
            ] AS surgery_filters,
            [x IN other_treatment_nodes WHERE x IS NOT NULL |
              {
                treatment_type: coalesce(x.treatment_type, x.other_treatment_type, 'other'),
                treatment_agent: coalesce(x.treatment_agent, x.other_treatment_agent),
                age_at_treatment_start: x.age_at_treatment_start,
                age_at_treatment_end: x.age_at_treatment_end
              }
            ] AS other_filters

          WITH
            file_guid,
            p,
            sm,
            sample_diagnosis_genetic_analysis_filters,
            survival_filters,
            chemo_filters + radiation_filters + surgery_filters + other_filters AS treatment_filters

          OPTIONAL MATCH (p)<-[:of_treatment_response]-(tresp:treatment_response)

          WITH
            file_guid,
            p,
            sm,
            sample_diagnosis_genetic_analysis_filters,
            survival_filters,
            treatment_filters,
            collect(DISTINCT tresp) AS response_nodes

          WITH
            file_guid,
            p,
            sm,
            sample_diagnosis_genetic_analysis_filters,
            survival_filters,
            treatment_filters,
            [x IN response_nodes WHERE x IS NOT NULL |
              {
                response_category: x.response_category,
                age_at_response: x.age_at_response,
                response: x.response,
                response_system: x.response_system
              }
            ] AS treatment_response_filters

          WITH
            file_guid,
            p,
            collect(DISTINCT sm.sample_id) AS raw_sample_ids,
            collect(DISTINCT sample_diagnosis_genetic_analysis_filters) AS sdga_filter_lists,
            collect(DISTINCT survival_filters) AS survival_filter_lists,
            collect(DISTINCT treatment_filters) AS treatment_filter_lists,
            collect(DISTINCT treatment_response_filters) AS treatment_response_filter_lists

          OPTIONAL MATCH (p)-[:of_participant]->(cg:consent_group)

          WITH
            file_guid,
            p,
            raw_sample_ids,
            sdga_filter_lists,
            survival_filter_lists,
            treatment_filter_lists,
            treatment_response_filter_lists,
            [x IN collect(DISTINCT cg.consent_group_name) WHERE x IS NOT NULL] AS consent_codes_for_participant

          WITH
            file_guid,
            p,
            consent_codes_for_participant,
            [x IN raw_sample_ids WHERE x IS NOT NULL] AS sample_ids_for_participant,
            reduce(acc = [], xs IN sdga_filter_lists | acc + xs) AS raw_sdga_filters,
            reduce(acc = [], xs IN survival_filter_lists | acc + xs) AS raw_survival_filters,
            reduce(acc = [], xs IN treatment_filter_lists | acc + xs) AS raw_treatment_filters,
            reduce(acc = [], xs IN treatment_response_filter_lists | acc + xs) AS raw_treatment_response_filters

          WITH
            file_guid,
            {
              pids: CASE WHEN p IS NOT NULL THEN [p.guid] ELSE [] END,
              participant_ids: CASE WHEN p IS NOT NULL THEN [p.participant_id] ELSE [] END,
              consent_codes: consent_codes_for_participant,
              sample_ids: sample_ids_for_participant,
              combined_filters: [{
                participant_id: CASE WHEN p IS NOT NULL THEN p.participant_id ELSE null END,
                race: CASE WHEN p IS NOT NULL THEN p.race ELSE null END,
                sex_at_birth: CASE WHEN p IS NOT NULL THEN p.sex_at_birth ELSE null END,

                sample_diagnosis_genetic_analysis_filters:
                  reduce(acc = [], x IN raw_sdga_filters |
                    CASE WHEN x IS NULL OR x IN acc THEN acc ELSE acc + [x] END
                  ),

                survival_filters:
                  reduce(acc = [], x IN raw_survival_filters |
                    CASE WHEN x IS NULL OR x IN acc THEN acc ELSE acc + [x] END
                  ),

                treatment_filters:
                  reduce(acc = [], x IN raw_treatment_filters |
                    CASE WHEN x IS NULL OR x IN acc THEN acc ELSE acc + [x] END
                  ),

                treatment_response_filters:
                  reduce(acc = [], x IN raw_treatment_response_filters |
                    CASE WHEN x IS NULL OR x IN acc THEN acc ELSE acc + [x] END
                  )
              }]
            } AS output_row

          WITH
            file_guid,
            collect(output_row) AS output_rows

          WITH
            file_guid,
            reduce(acc = [], r IN output_rows | acc + r.pids) AS raw_pids,
            reduce(acc = [], r IN output_rows | acc + r.participant_ids) AS raw_participant_ids,
            reduce(acc = [], r IN output_rows | acc + r.consent_codes) AS raw_consent_codes,
            reduce(acc = [], r IN output_rows | acc + r.sample_ids) AS raw_sample_ids,
            reduce(acc = [], r IN output_rows | acc + r.combined_filters) AS raw_combined_filters

          WITH
            file_guid,

            reduce(acc = [], x IN raw_pids |
              CASE WHEN x IS NULL OR x IN acc THEN acc ELSE acc + [x] END
            ) AS pids,

            reduce(acc = [], x IN raw_participant_ids |
              CASE WHEN x IS NULL OR x IN acc THEN acc ELSE acc + [x] END
            ) AS participant_ids,

            reduce(acc = [], x IN raw_consent_codes |
              CASE WHEN x IS NULL OR x IN acc THEN acc ELSE acc + [x] END
            ) AS consent_codes,

            reduce(acc = [], x IN raw_sample_ids |
              CASE WHEN x IS NULL OR x IN acc THEN acc ELSE acc + [x] END
            ) AS sample_ids,

            reduce(acc = [], x IN raw_combined_filters |
              CASE WHEN x IS NULL OR x IN acc THEN acc ELSE acc + [x] END
            ) AS combined_filters

          RETURN
            file_guid AS id,
            file_guid AS file_id,
            pids AS pid,
            participant_ids AS participant_id,
            null AS sample_id, // No sample ids for participant level files
            consent_codes AS consent_codes,
            null AS anatomic_site,
            null AS participant_age_at_collection,
            null AS sample_tumor_status,
            null AS tumor_spatial_extent,
            null AS sample_description,
            null AS percent_tumor,
            null AS percent_necrosis,
            [file_guid] AS files,
            combined_filters AS combined_filters
        page_size: 1000
      - name: "combined_filters_sample_level"
        query: |
          MATCH (f:
            clinical_measure_file |
            generic_file |
            cytogenomic_file |
            methylation_array_file |
            pathology_file |
            sequencing_file
          )-[:of_clinical_measure_file|of_generic_file|of_cytogenomic_file|of_methylation_array_file|of_pathology_file|of_sequencing_file]->(direct_sm:sample)
          WHERE f.dcf_indexd_guid IS NOT NULL

          WITH
            f.dcf_indexd_guid AS file_guid,
            collect(DISTINCT direct_sm) AS direct_samples

          SKIP $skip
          LIMIT $limit

          CALL {
            WITH direct_samples

            UNWIND direct_samples AS seed_sm
            OPTIONAL MATCH (seed_sm)-[:of_sample]->(out_cell:cell_line)

            WITH
              direct_samples,
              seed_sm,
              collect(DISTINCT out_cell) AS raw_models

            OPTIONAL MATCH (seed_sm)-[:of_sample]->(out_pdx:pdx)

            WITH
              direct_samples,
              seed_sm,
              raw_models + collect(DISTINCT out_pdx) AS raw_models

            OPTIONAL MATCH (seed_sm)<-[:of_cell_line]-(in_cell:cell_line)

            WITH
              direct_samples,
              seed_sm,
              raw_models + collect(DISTINCT in_cell) AS raw_models

            OPTIONAL MATCH (seed_sm)<-[:of_pdx]-(in_pdx:pdx)

            WITH
              direct_samples,
              raw_models + collect(DISTINCT in_pdx) AS raw_models

            WITH
              direct_samples,
              [m IN raw_models WHERE m IS NOT NULL] AS models

            UNWIND CASE WHEN size(models) = 0 THEN [null] ELSE models END AS model
            OPTIONAL MATCH (model)<-[:of_sample]-(same_model_sm:sample)

            WITH
              direct_samples,
              models,
              collect(DISTINCT same_model_sm) AS same_model_samples

            UNWIND CASE WHEN size(models) = 0 THEN [null] ELSE models END AS model
            OPTIONAL MATCH (model)-[:of_cell_line|of_pdx]->(model_edge_sm:sample)

            WITH
              direct_samples +
              same_model_samples +
              collect(DISTINCT model_edge_sm) AS raw_samples

            UNWIND raw_samples AS sm
            WITH DISTINCT sm
            WHERE sm IS NOT NULL

            RETURN collect(sm) AS associated_samples
          }

          UNWIND associated_samples AS sm

          CALL {
            WITH sm

            OPTIONAL MATCH (sm)-[:of_sample]->(direct_p:participant)

            WITH
              sm,
              collect(DISTINCT direct_p) AS direct_participants

            OPTIONAL MATCH (sm)<-[:of_diagnosis]-(sample_dg:diagnosis)-[:of_diagnosis]->(diagnosis_p:participant)

            WITH
              sm,
              direct_participants + collect(DISTINCT diagnosis_p) AS sample_and_diagnosis_participants

            OPTIONAL MATCH (sm)-[:of_sample]->(cell_model:cell_line)-[:of_cell_line]->(cell_p:participant)

            WITH
              sm,
              sample_and_diagnosis_participants,
              collect(DISTINCT cell_p) AS model_participants

            OPTIONAL MATCH (sm)-[:of_sample]->(pdx_model:pdx)-[:of_pdx]->(pdx_p:participant)

            WITH
              sm,
              sample_and_diagnosis_participants,
              model_participants + collect(DISTINCT pdx_p) AS model_participants

            OPTIONAL MATCH (sm)<-[:of_cell_line]-(incoming_cell_model:cell_line)-[:of_cell_line]->(incoming_cell_p:participant)

            WITH
              sm,
              sample_and_diagnosis_participants,
              model_participants + collect(DISTINCT incoming_cell_p) AS model_participants

            OPTIONAL MATCH (sm)<-[:of_pdx]-(incoming_pdx_model:pdx)-[:of_pdx]->(incoming_pdx_p:participant)

            WITH
              sample_and_diagnosis_participants +
              model_participants +
              collect(DISTINCT incoming_pdx_p) AS raw_participants

            WITH
              [p IN raw_participants WHERE p IS NOT NULL] AS participants

            RETURN
              CASE
                WHEN size(participants) = 0 THEN [null]
                ELSE participants
              END AS participants
          }

          UNWIND participants AS p

          CALL {
            WITH sm, p

            OPTIONAL MATCH (sm)<-[:of_diagnosis]-(sample_dg:diagnosis)
            OPTIONAL MATCH (p)<-[:of_diagnosis]-(participant_dg:diagnosis)

            WITH
              sm,
              collect(DISTINCT sample_dg) + collect(DISTINCT participant_dg) AS raw_diagnoses,
              p

            OPTIONAL MATCH (sm)<-[:of_genetic_analysis]-(sample_ga:genetic_analysis)
            OPTIONAL MATCH (p)<-[:of_genetic_analysis]-(participant_ga:genetic_analysis)

            WITH
              sm,
              [dg IN raw_diagnoses WHERE dg IS NOT NULL] AS diagnoses,
              [ga IN collect(DISTINCT sample_ga) + collect(DISTINCT participant_ga) WHERE ga IS NOT NULL] AS genetic_analyses

            UNWIND CASE WHEN size(diagnoses) = 0 THEN [null] ELSE diagnoses END AS dg
            UNWIND CASE WHEN size(genetic_analyses) = 0 THEN [null] ELSE genetic_analyses END AS ga

            WITH DISTINCT
              CASE WHEN sm IS NOT NULL THEN sm.anatomic_site ELSE null END AS sample_anatomic_site,
              CASE WHEN sm IS NOT NULL THEN sm.participant_age_at_collection ELSE null END AS participant_age_at_collection,
              CASE WHEN sm IS NOT NULL THEN sm.sample_tumor_status ELSE null END AS sample_tumor_status,
              CASE WHEN sm IS NOT NULL THEN sm.tumor_spatial_extent ELSE null END AS tumor_spatial_extent,
              CASE WHEN dg IS NOT NULL THEN dg.age_at_diagnosis ELSE null END AS age_at_diagnosis,
              CASE WHEN dg IS NOT NULL THEN dg.anatomic_site ELSE null END AS diagnosis_anatomic_site,
              CASE WHEN dg IS NOT NULL THEN dg.disease_phase ELSE null END AS disease_phase,
              CASE WHEN dg IS NOT NULL THEN dg.diagnosis_classification_system ELSE null END AS diagnosis_classification_system,
              CASE WHEN dg IS NOT NULL THEN dg.diagnosis_category ELSE null END AS diagnosis_category,
              CASE WHEN dg IS NOT NULL THEN dg.diagnosis_basis ELSE null END AS diagnosis_basis,
              CASE WHEN dg IS NOT NULL THEN dg.diagnosis ELSE null END AS diagnosis,
              CASE WHEN ga IS NOT NULL THEN ga.reported_significance ELSE null END AS reported_significance,
              CASE WHEN ga IS NOT NULL THEN ga.reported_significance_system ELSE null END AS reported_significance_system,
              CASE WHEN ga IS NOT NULL THEN ga.gene_symbol ELSE null END AS gene_symbol,
              CASE WHEN ga IS NOT NULL THEN ga.alteration ELSE null END AS alteration,
              CASE WHEN ga IS NOT NULL THEN ga.fusion_partner_gene ELSE null END AS fusion_partner_gene,
              CASE WHEN ga IS NOT NULL THEN ga.alteration_type ELSE null END AS alteration_type,
              CASE WHEN ga IS NOT NULL THEN ga.status ELSE null END AS status

            RETURN collect({
              sample_anatomic_site: sample_anatomic_site,
              participant_age_at_collection: participant_age_at_collection,
              sample_tumor_status: sample_tumor_status,
              tumor_spatial_extent: tumor_spatial_extent,
              age_at_diagnosis: age_at_diagnosis,
              diagnosis_anatomic_site: diagnosis_anatomic_site,
              disease_phase: disease_phase,
              diagnosis_classification_system: diagnosis_classification_system,
              diagnosis_category: diagnosis_category,
              diagnosis_basis: diagnosis_basis,
              diagnosis: diagnosis,
              reported_significance: reported_significance,
              reported_significance_system: reported_significance_system,
              gene_symbol: gene_symbol,
              alteration: alteration,
              fusion_partner_gene: fusion_partner_gene,
              alteration_type: alteration_type,
              status: status
            }) AS sample_diagnosis_genetic_analysis_filters
          }

          WITH
            file_guid,
            associated_samples,
            p,
            collect(DISTINCT sample_diagnosis_genetic_analysis_filters) AS sdga_filter_lists

          WITH
            file_guid,
            associated_samples,
            p,
            reduce(raw_sdga_filters = [], filter_list IN sdga_filter_lists |
              raw_sdga_filters + filter_list
            ) AS raw_sdga_filters

          CALL {
            WITH raw_sdga_filters

            WITH [filter_entry IN raw_sdga_filters WHERE filter_entry IS NOT NULL] AS raw_sdga_filters

            UNWIND raw_sdga_filters AS filter_entry

            WITH DISTINCT
              filter_entry.sample_anatomic_site AS sample_anatomic_site,
              filter_entry.participant_age_at_collection AS participant_age_at_collection,
              filter_entry.sample_tumor_status AS sample_tumor_status,
              filter_entry.tumor_spatial_extent AS tumor_spatial_extent,
              filter_entry.age_at_diagnosis AS age_at_diagnosis,
              filter_entry.diagnosis_anatomic_site AS diagnosis_anatomic_site,
              filter_entry.disease_phase AS disease_phase,
              filter_entry.diagnosis_classification_system AS diagnosis_classification_system,
              filter_entry.diagnosis_category AS diagnosis_category,
              filter_entry.diagnosis_basis AS diagnosis_basis,
              filter_entry.diagnosis AS diagnosis,
              filter_entry.reported_significance AS reported_significance,
              filter_entry.reported_significance_system AS reported_significance_system,
              filter_entry.gene_symbol AS gene_symbol,
              filter_entry.alteration AS alteration,
              filter_entry.fusion_partner_gene AS fusion_partner_gene,
              filter_entry.alteration_type AS alteration_type,
              filter_entry.status AS status

            RETURN collect({
              sample_anatomic_site: sample_anatomic_site,
              participant_age_at_collection: participant_age_at_collection,
              sample_tumor_status: sample_tumor_status,
              tumor_spatial_extent: tumor_spatial_extent,
              age_at_diagnosis: age_at_diagnosis,
              diagnosis_anatomic_site: diagnosis_anatomic_site,
              disease_phase: disease_phase,
              diagnosis_classification_system: diagnosis_classification_system,
              diagnosis_category: diagnosis_category,
              diagnosis_basis: diagnosis_basis,
              diagnosis: diagnosis,
              reported_significance: reported_significance,
              reported_significance_system: reported_significance_system,
              gene_symbol: gene_symbol,
              alteration: alteration,
              fusion_partner_gene: fusion_partner_gene,
              alteration_type: alteration_type,
              status: status
            }) AS sample_diagnosis_genetic_analysis_filters
          }

          CALL {
            WITH p
            OPTIONAL MATCH (p)<-[:of_survival]-(su:survival)
            RETURN [x IN collect(DISTINCT su) WHERE x IS NOT NULL | {
              last_known_survival_status: x.last_known_survival_status,
              cause_of_death: x.cause_of_death,
              first_event: x.first_event,
              age_at_last_known_survival_status: x.age_at_last_known_survival_status
            }] AS survival_filters
          }

          CALL {
            WITH p

            OPTIONAL MATCH (p)<-[:of_treatment_chemotherapy]-(tchemo:treatment_chemotherapy)

            WITH
              p,
              [x IN collect(DISTINCT tchemo) WHERE x IS NOT NULL | {
                treatment_type: coalesce(x.treatment_type, x.chemotherapy_type, 'chemotherapy'),
                treatment_agent: coalesce(x.treatment_agent, x.chemotherapy_agent),
                age_at_treatment_start: x.age_at_treatment_start,
                age_at_treatment_end: x.age_at_treatment_end
              }] AS chemo_filters

            OPTIONAL MATCH (p)<-[:of_treatment_radiation]-(trad:treatment_radiation)

            WITH
              p,
              chemo_filters,
              [x IN collect(DISTINCT trad) WHERE x IS NOT NULL | {
                treatment_type: coalesce(x.treatment_type, x.radiation_type, 'radiation'),
                treatment_agent: coalesce(x.treatment_agent, x.radiation_agent),
                age_at_treatment_start: x.age_at_treatment_start,
                age_at_treatment_end: x.age_at_treatment_end
              }] AS radiation_filters

            OPTIONAL MATCH (p)<-[:of_treatment_surgery]-(tsurg:treatment_surgery)

            WITH
              p,
              chemo_filters,
              radiation_filters,
              [x IN collect(DISTINCT tsurg) WHERE x IS NOT NULL | {
                treatment_type: coalesce(x.treatment_type, x.surgery_type, 'surgery'),
                treatment_agent: coalesce(x.treatment_agent, x.surgery_agent),
                age_at_treatment_start: x.age_at_treatment_start,
                age_at_treatment_end: x.age_at_treatment_end
              }] AS surgery_filters

            OPTIONAL MATCH (p)<-[:of_treatment_other]-(tother:treatment_other)

            RETURN
              chemo_filters +
              radiation_filters +
              surgery_filters +
              [x IN collect(DISTINCT tother) WHERE x IS NOT NULL | {
                treatment_type: coalesce(x.treatment_type, x.other_treatment_type, 'other'),
                treatment_agent: coalesce(x.treatment_agent, x.other_treatment_agent),
                age_at_treatment_start: x.age_at_treatment_start,
                age_at_treatment_end: x.age_at_treatment_end
              }] AS treatment_filters
          }

          CALL {
            WITH p
            OPTIONAL MATCH (p)<-[:of_treatment_response]-(tresp:treatment_response)
            RETURN [x IN collect(DISTINCT tresp) WHERE x IS NOT NULL | {
              response_category: x.response_category,
              age_at_response: x.age_at_response,
              response: x.response,
              response_system: x.response_system
            }] AS treatment_response_filters
          }

          WITH
            file_guid,
            associated_samples,
            p,
            {
              participant_id: CASE WHEN p IS NOT NULL THEN p.participant_id ELSE null END,
              race: CASE WHEN p IS NOT NULL THEN p.race ELSE null END,
              sex_at_birth: CASE WHEN p IS NOT NULL THEN p.sex_at_birth ELSE null END,
              sample_diagnosis_genetic_analysis_filters: sample_diagnosis_genetic_analysis_filters,
              survival_filters: survival_filters,
              treatment_filters: treatment_filters,
              treatment_response_filters: treatment_response_filters
            } AS combined_filter

          WITH
            file_guid,
            associated_samples,
            collect({
              participant_node: p,
              combined_filter: combined_filter
            }) AS raw_combined_filter_entries

          UNWIND raw_combined_filter_entries AS raw_combined_filter_entry

          WITH
            file_guid,
            associated_samples,
            raw_combined_filter_entry.participant_node AS participant_node,
            collect(raw_combined_filter_entry.combined_filter) AS participant_combined_filters

          WITH
            file_guid,
            associated_samples,
            participant_node,
            head(participant_combined_filters) AS participant_combined_filter,
            reduce(raw_sdga_filters = [], filter_entry IN participant_combined_filters |
              raw_sdga_filters + filter_entry.sample_diagnosis_genetic_analysis_filters
            ) AS raw_sdga_filters

          CALL {
            WITH raw_sdga_filters

            WITH [filter_entry IN raw_sdga_filters WHERE filter_entry IS NOT NULL] AS raw_sdga_filters

            UNWIND raw_sdga_filters AS filter_entry

            WITH DISTINCT
              filter_entry.sample_anatomic_site AS sample_anatomic_site,
              filter_entry.participant_age_at_collection AS participant_age_at_collection,
              filter_entry.sample_tumor_status AS sample_tumor_status,
              filter_entry.tumor_spatial_extent AS tumor_spatial_extent,
              filter_entry.age_at_diagnosis AS age_at_diagnosis,
              filter_entry.diagnosis_anatomic_site AS diagnosis_anatomic_site,
              filter_entry.disease_phase AS disease_phase,
              filter_entry.diagnosis_classification_system AS diagnosis_classification_system,
              filter_entry.diagnosis_category AS diagnosis_category,
              filter_entry.diagnosis_basis AS diagnosis_basis,
              filter_entry.diagnosis AS diagnosis,
              filter_entry.reported_significance AS reported_significance,
              filter_entry.reported_significance_system AS reported_significance_system,
              filter_entry.gene_symbol AS gene_symbol,
              filter_entry.alteration AS alteration,
              filter_entry.fusion_partner_gene AS fusion_partner_gene,
              filter_entry.alteration_type AS alteration_type,
              filter_entry.status AS status

            RETURN collect({
              sample_anatomic_site: sample_anatomic_site,
              participant_age_at_collection: participant_age_at_collection,
              sample_tumor_status: sample_tumor_status,
              tumor_spatial_extent: tumor_spatial_extent,
              age_at_diagnosis: age_at_diagnosis,
              diagnosis_anatomic_site: diagnosis_anatomic_site,
              disease_phase: disease_phase,
              diagnosis_classification_system: diagnosis_classification_system,
              diagnosis_category: diagnosis_category,
              diagnosis_basis: diagnosis_basis,
              diagnosis: diagnosis,
              reported_significance: reported_significance,
              reported_significance_system: reported_significance_system,
              gene_symbol: gene_symbol,
              alteration: alteration,
              fusion_partner_gene: fusion_partner_gene,
              alteration_type: alteration_type,
              status: status
            }) AS sample_diagnosis_genetic_analysis_filters
          }

          WITH
            file_guid,
            associated_samples,
            collect(DISTINCT participant_node) AS participant_nodes,
            collect({
              participant_id: participant_combined_filter.participant_id,
              race: participant_combined_filter.race,
              sex_at_birth: participant_combined_filter.sex_at_birth,
              sample_diagnosis_genetic_analysis_filters: sample_diagnosis_genetic_analysis_filters,
              survival_filters: participant_combined_filter.survival_filters,
              treatment_filters: participant_combined_filter.treatment_filters,
              treatment_response_filters: participant_combined_filter.treatment_response_filters
            }) AS combined_filters

          CALL {
            WITH participant_nodes

            UNWIND CASE
              WHEN size(participant_nodes) = 0 THEN [null]
              ELSE participant_nodes
            END AS participant_node_for_consent

            OPTIONAL MATCH (participant_node_for_consent)-[:of_participant]->(cg:consent_group)

            RETURN [x IN collect(DISTINCT cg.consent_group_name) WHERE x IS NOT NULL] AS consent_codes
          }

          WITH
            file_guid,
            associated_samples,
            participant_nodes,
            combined_filters,
            consent_codes

          RETURN
            file_guid AS id,
            file_guid AS file_id,
            [p IN participant_nodes WHERE p IS NOT NULL AND p.guid IS NOT NULL | p.guid] AS pid,
            [p IN participant_nodes WHERE p IS NOT NULL AND p.participant_id IS NOT NULL | p.participant_id] AS participant_id,
            reduce(sample_ids = [], sample_node IN associated_samples |
              CASE
                WHEN sample_node.sample_id IS NULL OR sample_node.sample_id IN sample_ids THEN sample_ids
                ELSE sample_ids + [sample_node.sample_id]
              END
            ) AS sample_id,
            consent_codes AS consent_codes,
            reduce(anatomic_sites = [], sample_node IN associated_samples |
              reduce(inner_sites = anatomic_sites, site IN coalesce(sample_node.anatomic_site, []) |
                CASE
                  WHEN site IS NULL OR site IN inner_sites THEN inner_sites
                  ELSE inner_sites + [site]
                END
              )
            ) AS anatomic_site,
            reduce(collection_ages = [], sample_node IN associated_samples |
              CASE
                WHEN sample_node.participant_age_at_collection IS NULL OR sample_node.participant_age_at_collection IN collection_ages THEN collection_ages
                ELSE collection_ages + [sample_node.participant_age_at_collection]
              END
            ) AS participant_age_at_collection,
            reduce(tumor_statuses = [], sample_node IN associated_samples |
              CASE
                WHEN sample_node.sample_tumor_status IS NULL OR sample_node.sample_tumor_status IN tumor_statuses THEN tumor_statuses
                ELSE tumor_statuses + [sample_node.sample_tumor_status]
              END
            ) AS sample_tumor_status,
            reduce(spatial_extents = [], sample_node IN associated_samples |
              CASE
                WHEN sample_node.tumor_spatial_extent IS NULL OR sample_node.tumor_spatial_extent IN spatial_extents THEN spatial_extents
                ELSE spatial_extents + [sample_node.tumor_spatial_extent]
              END
            ) AS tumor_spatial_extent,
            reduce(sample_descriptions = [], sample_node IN associated_samples |
              CASE
                WHEN sample_node.sample_description IS NULL OR sample_node.sample_description IN sample_descriptions THEN sample_descriptions
                ELSE sample_descriptions + [sample_node.sample_description]
              END
            ) AS sample_description,
            reduce(percent_tumors = [], sample_node IN associated_samples |
              CASE
                WHEN sample_node.percent_tumor IS NULL OR sample_node.percent_tumor IN percent_tumors THEN percent_tumors
                ELSE percent_tumors + [sample_node.percent_tumor]
              END
            ) AS percent_tumor,
            reduce(percent_necroses = [], sample_node IN associated_samples |
              CASE
                WHEN sample_node.percent_necrosis IS NULL OR sample_node.percent_necrosis IN percent_necroses THEN percent_necroses
                ELSE percent_necroses + [sample_node.percent_necrosis]
              END
            ) AS percent_necrosis,
            [file_guid] AS files,
            combined_filters AS combined_filters
        page_size: 1000
